#import "../note.typ": *


= 应用层

== 应用层概述
应用层协议的核心在于解决具体的应用问题。网络通信的本质并非仅仅是两台主机（Host）之间的数据传输，而是两台主机中运行的*应用进程（Application Process）*之间的逻辑通信。应用层协议即规定了这些应用进程在协同工作时必须遵循的规则（如报文格式、时序等）。
=== 应用进程通信方式
==== 客户/服务器方式 (Client/Server, C/S)

这是互联网中最典型的分布式应用架构，描述的是进程间“服务请求”与“服务提供”的不对等关系。

- *服务器 (Server)*：服务提供方。必须始终处于运行状态（高可用性），绑定在特定的熟知端口（Well-known Port），被动等待请求。服务器通常需要高性能硬件支持以处理高并发。
- *客户 (Client)*：服务请求方。由用户主动发起，在通信时临时运行。客户必须预先知道服务器的IP地址和端口号。

优点：
- *双向交互*：虽然连接建立由客户端发起，但一旦建立（特别是面向连接的TCP），通信是双向对等的。
- *不对称性*：服务器不需要预先知道客户的IP，利用系统调用（accept）被动捕获连接；客户机则需要动态分配临时端口。

==== 浏览器/服务器方式 (Browser/Server, B/S)
B/S 架构是 C/S 架构的一种特例，随着 Web 技术的发展而兴起。其核心是将客户端软件统一为通用的 Web 浏览器，从而形成三层架构（3-Tier Architecture）：
- *表现层 (Presentation Layer)*：位于客户端（浏览器）。负责用户交互、HTML/CSS 渲染及简单的前端逻辑。
- *处理层 (Business Logic Layer)*：位于 Web 服务器（如 Nginx, Apache, Tomcat）。负责核心业务逻辑的处理、页面生成及请求调度。
- *数据层 (Data Layer)*：位于数据库服务器。负责数据的持久化存储与查询。

这样架构的优点是：
- *零客户端维护*：客户端无需安装专用软件，升级仅需在服务器端进行。
- *高扩展性*：基于标准的 HTTP/HTTPS 协议，跨平台能力强。

==== 对等方式 (Peer-to-Peer, P2P)
P2P 架构打破了 C/S 的层级关系，网络中的每个节点（Peer）地位完全平等。

- 核心机制：每个节点既是下载数据的客户，又是向其他节点上传数据的服务器。
- 流量特征：具有网络自扩展性（Scalability）。在 C/S 模式下，用户越多服务器压力越大；而在 P2P 模式下，用户越多，提供资源的节点也越多，聚合带宽越大。
- 应用场景：常用于大文件分发（BitTorrent）、流媒体传输及区块链网络。
=== 服务器进程工作方式
服务器在处理客户端请求时，根据协议特性（TCP或UDP）及并发需求，主要分为循环方式和并发方式。

==== 循环方式 (Iterative Mode) —— 串行处理

这种方式常见于基于 UDP 的无连接服务（如早期的 DNS 实现或简单的 DHCP 服务）。

工作流程：
+ 服务器仅运行一个主进程，绑定服务端口。
+ 接收到一个客户请求后，服务器独占 CPU 处理该请求，直到生成响应并发送。
+ 在此期间，其他客户的请求会滞留在*内核缓冲区（Socket Buffer）*的队列中排队等待。
+ 处理完当前请求后，服务器循环回到队列头部处理下一个请求。

特点：
- *阻塞模型*：同一时刻只能服务一个客户。
- *低开销*：无需频繁创建/销毁进程或线程，上下文切换少。
- *适用性*：适用于处理时间极短、无连接状态保持的服务。

==== 并发方式 (Concurrent Mode) —— 并行处理

这种方式是基于 TCP 的面向连接服务的标准实现（如 Web 服务器、FTP 服务器）。

工作流程：
+ *主服务进程（Master Process）*：在熟知端口（如 80）监听连接请求（LISTEN 状态）。
+ *连接建立*：当通过三次握手建立 TCP 连接后，主进程接收请求。
+ *派生子进程*：主进程通过 fork 系统调用创建一个从属服务进程（Slave/Child Process）（或线程）来专门处理该特定客户的业务。
+ *套接字区分*：
  - 监听套接字 (Listening Socket)：主进程继续使用原套接字在熟知端口监听新的连接请求。
  - 已连接套接字 (Connected Socket)：子进程使用新创建的临时套接字与特定客户进行一对一通信。
特点：
- *非阻塞模型*：主进程不会因为处理具体业务而阻塞，保证了高并发接入能力。
- *资源隔离*：每个客户由独立的执行流（进程/线程）服务，互不干扰。
- *生命周期*：连接断开（FIN/RST）后，子进程终止并释放资源。

== 域名系统

=== 概述
域名系统（DNS, Domain Name System）是互联网核心的基础设施之一，属于应用层协议。它通过运行在专用节点上的分布式数据库系统，为互联网应用提供主机名（域名）到 IP 地址的映射服务。#index[DNS]

互联网通信的底层寻址依赖于 IP 地址（如 192.168.1.1 或 2001:db8::1），但此类数字串缺乏语义，难以记忆。

- 映射服务：DNS 的基本职能是建立“用户可读性好的主机名”（如 www.example.com）与“机器可处理的 IP 地址”之间的双向映射。
- 服务对象：虽然用户是域名的直接使用者，但 DNS 本质上是向所有需要域名解析的应用（如 HTTP、FTP、SMTP 等）提供底层支撑服务的。


与早期互联网使用单一的 HOSTS.TXT 文件进行集中式管理不同，现代互联网采用了*分布式（Distributed）*的设计理念。
- 分布式存储：整个互联网的域名信息并不存储在单一的主机中，而是分布在世界各地数以百万计的域名服务器上。
- 联机数据库：DNS 构建了一个覆盖全球的联机分布式数据库系统。这种设计解决了集中式系统存在的单点故障风险、流量瓶颈以及远距离传输延迟问题，确保了互联网的高可用性和可扩展性。

为了保证主机名的全局唯一性并便于管理，互联网采用了*层次树状结构（Hierarchical Tree Structure）*的命名空间。
- 树状结构：整个命名空间形成一棵倒置的树。树的根部称为“根”（Root）。
- 分层管理：根之下划分为顶级域名（TLD，如 .com, .cn）、二级域名（如 baidu.com）、三级域名等。
- 去中心化管理：这种结构允许将管理权限层层下放。顶级域名管理机构只需管理二级域名，而无需知晓三级及其子域名的具体信息，极大地提高了管理的灵活性。

DNS 的逻辑功能通过具体的软件和硬件实体来实现。
- *名字服务器*：运行域名服务器程序的专用节点被称为名字服务器（Name Server）。#index(initial: "N")[名字服务器]
- *解析过程*：名字到 IP 地址的解析（Resolution）并非由单一服务器完成，而是由分布在不同层级（根域名服务器、顶级域名服务器、权限域名服务器等）的若干个域名服务器协同完成。
- *C/S 模式*：在实际解析中，客户端（解析器 Resolver）向本地 DNS 服务器发送查询请求，若本地无法解析，则由服务器端根据层次结构向其他服务器迭代或递归查询，最终返回结果。

=== 域名系统名字空间和层次结构

Internet 的域名系统采用了一种层次树状结构（Hierarchical Tree Structure）的命名方法。这种设计类似于计算机文件系统的目录树，但它是倒置的：根（Root）在最上方，没有对应的名字；叶子节点在下方，代表具体的单台主机。这种逻辑结构被称为*域名空间（Domain Name Space）*。

域名空间由无数个节点组成，每一个节点都有一个标号（Label）。从树的叶子节点向上直到根节点，将沿途所有节点的标号连接起来，就构成了一个完整的域名。我们可以将域名划分为以下几个层级：

- *根域 (Root Domain)* 虽然在日常输入中常被省略，但在技术上，所有域名的终点都是“根”。它由一个点 . 表示（例如 www.example.com. 最后的那个点）。根下面是顶级域名。
- *顶级域名 (Top-Level Domain, TLD)* 这是域名树中最重要的一层分支，由互联网名称与数字地址分配机构（ICANN）管理。#index[TLD]主要分为三类：
  - 国家/地区顶级域名 (ccTLD)：如 .cn (中国)、.us (美国)、.uk (英国)。
  - 通用顶级域名 (gTLD)：如 .com (公司企业)、.net (网络服务机构)、.org (非营利性组织)、.edu (美国教育机构)。
  - 基础结构域名：只有一个，即 arpa，用于反向域名解析。
- *二级域名 (Second-Level Domain, SLD)*#index[SLD] 这是位于 TLD 之下的子域，通常是组织、公司或个人向注册机构申请的名称（如 baidu.com 中的 baidu）。这是互联网上体现“品牌”或“归属”的核心部分。
- *三级、四级域名 (Subdomains)* 这是由二级域名的拥有者自行分配和管理的。例如，在 tieba.baidu.com 中，tieba 是三级域名；理论上，只要总长度允许，这种分级可以无限延伸，但通常不超过 5 级。

为了确保全球互通，域名的构成必须遵循严格的语法规则：
- *标号（Label）的构成*：域名的每一个分量称为一个“标号”。标号由英文字母（a-z，不区分大小写）、数字（0-9）和连字符（-）组成。标号的长度不得超过 63个字符。
- *分隔符*：各级标号之间使用小数点 . (dot/point) 进行分隔。合法性校验：一个合法的注册域名，其中必须至少包含一个点（即至少包含 TLD 和 SLD，如 abc.com）。
- *总长度限制*：一个完整的域名（所有标号 + 分隔点），其总长度不得超过 255个字符。

Internet 采用这种分层树状结构而非扁平化结构，主要基于以下考量：
- 全局唯一性：通过分层授权，只要保证同一层级下的子节点名称不重复，就能保证全网域名的唯一性。
- 分布式管理（授权机制）：顶级域名由 ICANN 管理。二级域名由具体的注册者管理。三级及以下域名由二级域名的拥有者自行分配。这种机制避免了中心化管理的瓶颈，赋予了域名持有者极大的自治权。

#note[
  从结构上看，域名是一串点分字符串；从逻辑上看，它是一棵倒置的树。用户在浏览器输入 www.wiki.cn 时，实际上是在请求访问 cn 域 下的 wiki 域 下的 www 主机。这种设计完美平衡了机器的高效解析需求与人类的记忆习惯。
]
=== 域名服务器
域名服务器系统在逻辑上严格遵循域名空间的层次树状结构，但在实际管理与运维中，引入了*“区”（Zone）*的概念来划分管理权限。

- *域 (Domain)*：逻辑概念，指域名树中的某个分支及其下属的所有子树。#index[Domain]
- 区 (Zone)：物理和管理概念，指一个名字服务器所负责的管辖范围。#index[Zone]
  - *集合关系*：管辖区是域的子集
  - *划分原则*：各单位可根据具体情况划分管辖区。一个区可能包含一个完整的域，也可能只包含该域的上层部分（*若将子域授权给其他服务器管理*）。
  - *连通性约束*：同一个区内的所有节点必须在网络层是连通的。
  - 服务器职责：名字服务器的权限范围以*“区”*为单位。它负责维护该区内所有主机的域名到 IP 地址的映射关系。

根据功能定位和服务方式，域名服务器主要分为*权威名字服务器*和*递归解析器*两大类。

*权威名字服务器 (Authoritative Name Server)*是DNS信息的数据源头。#index(initial: "Q")[权威名字服务器]
  - *定义*：根据本地配置（Local Knowledge）存储并维护特定 DNS 区数据的服务器。
  - *功能*：它可以直接响应该区的查询请求，无需咨询其他服务器。
  - *部署要求*：为了保证可用性，每个 DNS 区必须至少配备一个可通过 IPv4 访问的权威名字服务器。

*递归解析器 (Recursive Resolver) / 递归服务器*是用户进行域名解析的代理端。#index(initial: "D")[递归解析器]
  - *定义*：代表用户主机（Stub Resolver）向域名系统发起查询，并负责将最终结果返回给用户的程序。
  - *归属*：通常由互联网服务提供商（ISP）或公共 DNS 服务商（如 Google, Cloudflare）提供，部署位置距离用户主机较近。
#remark[
  ISP（互联网服务提供商）是指为用户提供网络接入、分配IP地址和管理流量的公司。常见的ISP包括电信、联通和移动等。ISP在数字时代中扮演着重要角色，帮助个人和企业连接到互联网资源，并维护网络的完整性和安全性。选择合适的ISP可以提升业务的安全性和通过率。
]#index[ISP]
权威名字服务器按照域名树的层级进行部署，形成协同工作的分布式系统。

*根名字服务器 (Root Name Server)*
- 根服务器是 DNS 层次结构的最高层，也是解析过程的入口点。
- *实例架构*：全球共有 13 套（Sets）根服务器系统，域名标识为 a.rootservers.net 至 m.rootservers.net。这并非指 13 台物理机器，而是 13 个服务标识。
- *数据同步*：更改操作仅在主根服务器（a.rootservers.net）进行，随后同步至其余 12 套，以确保数据一致性。
- *任意播 (Anycast) 技术*：根服务器利用任意播技术在全球部署了 1000 多台镜像（Mirrored）实例。路由器会自动将查询转发至逻辑距离最近的根服务器实例，从而降低时延并提高可靠性。
- *功能边界*：根服务器不直接进行域名解析（即不直接返回 IP），它存储的是所有顶级域（TLD）名字服务器的域名及其 IP 地址，负责指引查询转向 TLD 服务器。

*顶级域名字服务器 (TLD Name Server)*
- *职责*：负责管理在该顶级域（如 .com, .cn, .org）下注册的所有二级域名。
- *响应机制*：当收到查询时，它返回该域名的最终解析结果，或者返回管理该域名的下一级（二级）名字服务器的 IP 地址。

*二级及本地域名服务器*
- *二级域名字服务器*：存储该域下具体主机的映射记录（如 baidu.com 的服务器知道 www.baidu.com 的 IP）。
- *本地域名服务器*：通常指三级域及以下（如 gzhu.edu.cn）的服务器。在企业或校园网环境中，各单位根据自身情况划分“区”，设置相应的权威服务器来保存内部主机的映射。


#note[查询交互流程][
+ *初始查询*：用户主机发出 DNS 查询报文，首先发送至本地 ISP 提供的递归服务器。
+ *本地解析*：若查询目标在递归服务器的缓存或本地 ISP 管辖范围内，则直接返回结果。
+ *迭代查询*：若递归服务器无法解析，且本地层级无法解决，它将向根服务器发起请求。根服务器指引其访问 TLD 服务器，TLD 服务器指引其访问二级域服务器，以此类推，逐级向下，直至获得最终 IP。
]
- *多层级部署*：实际网络中，本地域名服务器可能存在多层结构。当本层无法解析时，通常采用逐级向上递归的策略。
- *顶层出口*：当请求到达本地域的最上层服务器仍无法解析时，该服务器会转向根服务器开始迭代查询。
- *性能优化案例*：Google 公共 DNS 等现代系统采用了优化策略，将根区文件（Root Zone）信息直接同步至其递归解析器中。这意味着解析器自身已具备根区知识，无需产生实际的根服务器查询流量，从而显著提高了查询效率。

#example[
  一个部署在局域网（比如实验室或学院）内的本地域名服务器，通常身兼两职：
  - *作为权威服务器*：它确实只管辖自己的小圈子。对于这个范围内的查询，它绝对能解析，因为它有数据。
  - *作为递归/缓存服务器*：它同时要服务于这个网段内的用户（老师、同学）。用户不仅会访问实验室内部网站，还会访问百度、谷歌、图书馆官网等。对于这些外部域名，这台服务器的数据库里是空白的。

  场景：计算机学院的学生在机房请求访问 www.baidu.com。
  + 请求发送给4级服务器（计算机学院）。
  + 4级服务器查询自己的数据库（Zone File），发现自己只管 cs.whu.edu.cn 下的内容，里边没有 baidu.com 的记录。这就是“本层无法解析”。
  + 逐级向上：4级服务器配置了一个“默认转发器（Forwarder）”指向 3级服务器（校级）。它把问题扔给校级服务器。
  + 如果校级服务器也不认识，它会继续向外面的 ISP 递归服务器或根服务器查询。
]

=== 域名解析过程
域名解析始于应用层，通过运输层协议进行承载。
+ *解析触发*：当某一应用进程（如 Web 浏览器、邮件客户端）需要与其他主机通信，但仅知晓其域名时，该应用进程会触发域名解析请求。
+ 报文封装与传输：应用进程将待解析的域名封装进 DNS 请求报文中。为了降低通信开销（Overhead）并减少时延，*DNS 协议主要使用无连接的 UDP 协议进行封装*。目标端口：53。目标地址：网络中配置的递归服务器（或称本地域名服务器）IP 地址。
+ *响应返回*：递归服务器完成查询后，将解析出的 IP 地址封装在 DNS 应答报文中，同样通过 UDP 返回给发起请求的应用进程。

DNS 架构定义了两种核心查询行为，区别在于“*谁负责发起下一次查询*”。

*递归查询 (Recursive Query)*
- *定义*：指查询请求发出后，接收请求的服务器承担后续查询的全部负载。
- *行为逻辑*：如果被查询的域名服务器（服务器 A）不知道被查询域名的 IP 地址，服务器 A 就以 *DNS 客户* 的身份，向下一个域名服务器（服务器 B）发起查询。在此期间，初始请求者只需等待最终结果。
- *责任方*：被查询的服务器。
- *适用场景*：主要用于 *主机（Host）向递归解析器/本地域名服务器* 的查询阶段。主机通常只发出一次请求，然后被动等待结果。

*迭代查询 (Iterative Query)*
- *定义*：指接收请求的服务器不直接去索取最终结果，而是返回一个“指引”。
- *行为逻辑*：当域名服务器收到查询请求但无法解析时，它不会替对方去查询，而是将自己知道的 *下一步应当查询的域名服务器的 IP 地址* 告知发起者。发起者需要根据这个新地址，自己向下一级服务器发出新的查询。
- *责任方*：发起查询的服务器（通常是递归解析器）。
- *适用场景*：主要用于 *递归解析器向 DNS 层次结构（根、TLD、权威）* 查询的阶段。

#example[
  在实际的互联网环境中，域名解析通常是 “递归查询”与“迭代查询”的混合模式。以下以主机解析 www.example.com 为例：

  *第一阶段：主机到本地域名服务器（递归模式）*
  + *主机发起*：用户主机向 递归解析器（本地域名服务器） 发送 DNS 请求报文（UDP 53）。
  + *模式*：采用 *递归查询*。主机只发一次请求，要求递归服务器返回最终的 IP 地址或错误信息。

  *第二阶段：本地域名服务器到互联网核心（迭代模式）*

  当递归服务器的本地缓存中没有该域名的记录时，它将代表主机向 DNS 层次结构发起 迭代查询：
  + *查询根域*：递归服务器首先向 根名字服务器 (Root Server) 发送查询。响应：根服务器不负责具体解析，而是返回 顶级域（TLD）名字服务器（即 .com 服务器）的 IP 地址。
  + *查询顶级域*：递归服务器根据获得的 IP，向 .com TLD 服务器发送查询。响应：TLD 服务器返回负责管理 example.com 的 权威名字服务器 的 IP 地址。
  + *查询权威域*：递归服务器向 example.com 的权威名字服务器发送查询。响应：权威服务器在其管辖区（Zone）内找到了 www 的 A 记录（IP 地址），将其返回给递归服务器。

  *第三阶段：结果回传, 缓存与响应*

  递归解析器收到最终 IP 后，通常会先在本地缓存（Caching）一份，以便后续快速响应；随后将结果封装在应答报文中返回给用户主机应用进程。
]

#note[
  在实际的企业或校园网部署中，并不是每一台 DNS 服务器都有资格直接去问根服务器。

  - *内部层级（院级/部门级）*：通常配置为*转发模式（Forwarding Mode）*。当它遇到无法解析的域名时，它不去问根服务器，而是直接把请求“扔”给上级（校级）DNS 服务器。此时，它扮演的是一个中继者的角色。
  - *网络边界（校级/ISP级）*：这才是真正的递归解析器（Recursive Resolver）。当它收到下级转来的请求且无法解析时，*因为它已经处于网络的边缘（Exit），没有更上级了*，所以它必须根据根提示文件（Root Hints），直接去问根服务器。

  - *“逐级向上”*：发生在*内网*。是因为下级服务器被配置为“只问上级”，此时还没轮到根服务器出场。
  - *“向根查询”*：发生在*内网出口（递归服务器）*。当请求到达了最后一级 DNS（如校级或 ISP DNS），它没法再向上“推卸责任”了，这时它才必须依据标准的 DNS 协议，去敲响根服务器的大门。
]

=== 域名系统高速缓存
DNS 是一种典型的分布式数据库系统，其性能瓶颈主要存在于*广域网传输时延*和*核心层级服务器（如根服务器）的负载*。为了解决这些问题，DNS 引入了高速缓存机制。

其核心目标主要体现在以下三个维度：
- *降低解析延迟*：利用局部性原理，直接从临近节点的缓存中获取结果，避免重复进行跨网络的递归或迭代查询。
- *减轻根及顶级域服务器负荷*：根域名服务器（Root Servers）是全球互联网的枢纽。通过缓存，大量的重复请求被拦截在边缘网络，防止根服务器因过载而崩溃。
- *减少网络流量开销*：显著减少 Internet 骨干网上的 DNS 查询与响应报文数量，优化带宽利用率。

DNS 缓存并非仅存在于某一处，而是贯穿于整个解析链路：
- *主机缓存 (Host Cache)*：
  - 操作系统（如 Windows, Linux, macOS）会在本地维护一个 DNS 解析器缓存（Stub Resolver Cache）。
  - 应用层软件（如 Web 浏览器）通常也会维护独立的 DNS 缓存。
  - 作用：这是最快的一级缓存，若命中，无需产生任何网络流量。
- 本地域名服务器/递归解析器缓存 (Recursive Resolver Cache)：
  - 这是 ISP、企业或校园网出口 DNS 服务器的核心功能。
  - 作用：服务于局域网内的大量用户，缓存命中率极高，是减轻互联网 DNS 流量的主力。

缓存不仅仅存储最终的“域名到 IP”的映射，还会根据查询过程中获得的中间信息进行分层存储，以优化后续的迭代查询路径。

*全路径命中 (Full Cache Hit)*
- 场景：用户查询 y.abc.com。
- 逻辑：若本地域名服务器的高速缓存中已存在 y.abc.com 的有效记录（A 记录）。
- 行为：服务器直接将缓存结果封装在响应报文中返回给用户，完全终止后续向根或权威服务器的查询流程。

*中间路径优化 (Intermediate Infrastructure Caching)*
- 场景：本地域名服务器缓存中没有 y.abc.com 的 IP，但之前解析过其他 .com 域名，因此缓存了 .com 顶级域名服务器（TLD Server）的 IP 地址。
- 逻辑：服务器利用缓存中的 TLD 信息，跳过查询根域名服务器的步骤。
- 行为：直接向 .com 顶级域名服务器发起迭代查询。
- 意义：这种机制使得根域名服务器只需要处理针对新顶级域的极少数初始查询，极大地“卸载”了根服务器的压力。

由于域名与 IP 地址的绑定关系并非永久不变（例如服务器迁移、负载均衡调度），DNS 必须引入过期机制来维护*缓存一致性 (Cache Consistency)*。

*生存时间 (Time To Live, TTL)*#index[TTL]
- 定义：权威域名服务器在返回查询结果时，会在资源记录（Resource Record）中包含一个 TTL 字段（以秒为单位）。
- 作用：该字段规定了该条记录在其他服务器缓存中允许存放的最大时长。
- 典型值：根据业务稳定性不同，TTL 值差异巨大。静态基础设施可能设置为 48 小时（172800秒），而此时动态 CDN 域名可能仅为 60 秒或更短。

*超时处理与重新验证*
- 倒计时：缓存服务器收到记录后，会启动计时器。
- 失效 (Expiration)：当驻留时间超过 TTL 值，该记录被标记为“陈旧（Stale）”并从缓存中丢弃。
- 再查询：当用户再次请求该域名时，服务器必须重新向权威服务器发起完整的查询流程，以获取最新的绑定信息及新的 TTL。

*权衡策略 (Trade-off)*

TTL 的设定是一个典型的工程权衡问题：
- *增加 TTL 值*：提高缓存命中率，减少网络开销，减轻权威服务器负载。降低解析准确性。若 IP 变更，在 TTL 过期前，用户仍会访问旧 IP，导致服务不可用。
- *减少 TTL 值*: 提高解析准确性，能够快速响应 IP 变更（如故障切换）。显著增加网络 DNS 报文数量，加重权威服务器负担。

除了缓存“存在的记录”，现代 DNS 系统通常还会缓存“不存在的记录”（NXDOMAIN）。
- 逻辑：如果用户查询了一个不存在的域名（如 error.abc.com），权威服务器返回“该域名不存在”。
- 应用：递归服务器会将这个“不存在”的事实也缓存一段时间（Negative TTL）。
- 意义：防止用户或恶意程序针对不存在的域名发起重复的高频查询（DNS Flood），从而保护根和权威服务器。

== 电子邮件
电子邮件系统是一个典型的*异步通信系统*，其运作依赖于用户代理、邮件服务器以及一系列标准化的应用层协议。

=== 体系结构与核心组件
电子邮件系统的逻辑架构主要由三个核心组件构成，它们各司其职，共同完成邮件的创建、传输与交付。

*用户代理 (User Agent, UA)*#index(initial: "Y")[用户代理]#index[UA]
- 定义：用户与电子邮件系统交互的接口软件，即我们常说的“邮件客户端”（如 Outlook, Thunderbird, Apple Mail）。
- 功能：提供撰写、显示、处理（回复、转发）和管理邮件的图形化或命令行界面。
- 在发送时，负责将用户输入的内容封装成符合格式要求的报文，并推送到发送端邮件服务器。在接收时，负责从接收端邮件服务器拉取邮件，并解析（如 MIME 解码）供用户阅读。

*邮件传输代理 (Message Transfer Agent, MTA)*#index[MTA]#index(initial: "Y")[邮件传输代理]
- 定义：运行在邮件服务器上的后台进程（如 Postfix, Exchange, Sendmail）。这是电子邮件系统的核心基础设施。
- 24小时运行：作为守护进程始终在线，维护本地用户的邮箱（Mailbox）。
- 路由与中继：根据邮件目标地址，将邮件转发给下一个 MTA。
- 队列管理：当网络故障或对方服务器不可达时，将邮件暂存入发送队列并尝试重发。
- 通信模式：MTA 之间采用*客户/服务器（C/S）*模式。发起发送的 MTA 是客户，接收邮件的 MTA 是服务器。

*协议簇*

电子邮件并非由单一协议支撑，而是分为发送协议（SMTP）和读取协议（POP3/IMAP/HTTP）。

=== 邮件传输机制：SMTP 协议

简单邮件传输协议 (SMTP, Simple Mail Transfer Protocol) 是互联网邮件传输的事实标准，主要用于*MTA 之间的邮件中继*以及*UA 向 MTA 提交邮件*。#index[SMTP]

*协议特性*
- *推（Push）协议*：SMTP 设计用于将数据从发送方推送到接收方。
- *传输层依赖*：使用 TCP 连接提供可靠的数据传输服务。
- *端口号*：标准端口为 25（现代加密传输常使用 465 或 587）。
- *编码限制*：原始 SMTP 是一个基于 7位 ASCII 码的文本协议。这意味着它原生不支持二进制文件（图片、视频）或非英文字符（中文），必须依赖 MIME 扩展。

*通信阶段*

SMTP 的交互过程严格遵循“命令-响应”模式：
+ *连接建立 (Connection Establishment)*：
  - SMTP 客户端与服务器建立 TCP 连接。
  - 握手确认，验证双方身份。
+ *邮件传送 (Data Transfer)*：
  - MAIL FROM：指定发件人地址。
  - RCPT TO：指定收件人地址（可多个）。
  - DATA：传输邮件内容（首部+主体）。
+ *连接关闭 (Connection Termination)*：
  - 使用 QUIT 命令释放 TCP 连接。

*响应机制*

类似于 HTTP，SMTP 服务器对每个命令返回一个状态码（Status Code）和短语（Phrase）。例如：250 OK：请求成功。550 No such user here：无此用户，投递失败。

=== 邮件报文格式与 MIME 扩展

邮件的传输“管道”（SMTP）和传输的“内容”（报文格式）是分离的。

早期的邮件格式标准，定义了邮件由信封 (Envelope) 和 内容 (Content) 组成。内容分为两部分，由一个空行隔开：*首部 (Header)*：包含 To:, From:, Subject: 等元数据。*主体 (Body)：*纯 ASCII 文本内容。

为了解决 SMTP 仅支持 7位 ASCII 的限制，*MIME (Multipurpose Internet Mail Extensions)* 应运而生。#index[MIME]
- *核心逻辑*：MIME 不是一种新的传输协议，而是对邮件格式的扩展。它定义了如何将非 ASCII 数据（中文、图像、音频）编码为 ASCII 字符串，以便通过 SMTP 传输，到达后再由 UA 解码还原。
- 新增首部：
  - Content-Type：描述数据类型（如 text/html, image/jpeg, multipart/mixed）。
  - Content-Transfer-Encoding：描述编码方式（如 Base64, Quoted-printable）。
- *多部分结构*：MIME 允许一封邮件包含多个部分（Multipart），从而实现正文与附件的打包发送。

=== 邮件最终交付：读取协议

由于 SMTP 是“推”协议，且接收端用户电脑不可能 24 小时在线，因此邮件最终是存储在接收端的邮件服务器中的。用户需要使用“拉（Pull）”协议获取邮件。

*POP3 (邮局协议版本3)*#index[POP3]
- RFC 1939 定义的简单协议，使用 TCP 端口 110。
- *工作模式*：“下载并删除” (Download-and-Delete)。
- *流程*：认证（登录） $->$ 事务处理（读取、标记删除） $->$ 更新（服务器真正执行删除）。
- *局限性*：它是无状态的。一旦邮件被下载到本地 UA，服务器端通常会删除（或不保留状态）。这导致在多台设备（手机、电脑）上切换时，邮件不同步，管理困难。

*IMAP (互联网邮件访问协议)*#index[IMAP]
- RFC 2060/3501 定义的复杂协议，使用 TCP 端口 143。
- *工作模式*：“远程管理”。
- *状态同步*：服务器维护会话状态。用户在客户端A上标记“已读”或“删除”，在客户端B上能同步看到结果。
- *文件夹管理*：允许在服务器端创建文件夹分类邮件。
- *按需获取*：可以只下载邮件首部，只有当用户点击打开时才下载正文或附件（节省带宽）。
- *适用性*：是现代多设备办公环境下的首选协议。

*Webmail (基于 Web 的电子邮件)*
- 这并非一种独立的底层邮件协议，而是一种应用架构。
- 用户代理：Web 浏览器。
- 用户 $<->$ 邮件服务器：使用 HTTP/HTTPS 协议。
- 邮件服务器 $<->$ 其他邮件服务器：依然使用 SMTP 协议发送。

#note[一封邮件的生命周期][
我们梳理一封邮件从发送到接收的全过程：
+ *封装*：发件人 UA 使用 MIME 格式将内容编码。
+ *提交*：发件人 UA 通过 SMTP 将邮件推送到发送端邮件服务器。
+ *中继*：发送端邮件服务器通过 DNS 查询（MX记录）找到接收端服务器 IP，使用 SMTP 将邮件推送到接收端邮件服务器。
+ *存储*：接收端服务器将邮件存入收件人的邮箱（Mailbox）。
+ *获取*：收件人 UA 通过 IMAP (或 POP3/HTTP) 连接服务器，拉取并解码邮件供阅读。
]

#remark[
  MX 记录 (Mail Exchanger Record) 是 DNS 资源记录（Resource Record, RR）中的一种关键类型。用于在域名系统（DNS）中指定负责接收发往该域名电子邮件的邮件服务器（MTA）。
  - *功能定位*：它建立了域名（Domain Name）到邮件服务器主机名（Mail Server Hostname）之间的映射关系。
  - *服务分离*：通过 MX 记录，网络管理员可以将域名的 Web 服务（通常由 A 记录指向）与邮件服务（由 MX 记录指向）分离到不同的服务器上。例如，example.com 的网站可以托管在服务器 A，而发送给 user\@example.com 的邮件则被路由到服务器 B。
]
== WWW
=== WWW 体系结构概述
万维网（World Wide Web）是一个大规模的、分布式的超媒体信息系统。其核心架构基于 *客户/服务器 (Client/Server) 模型*，由资源、标识符、交互协议和代理四个关键要素组成。#index[WWW]

*Web 页面*（Web Page）并非单一文件，而是由一个基础的 HTML 文档和若干引用的*Web 对象*组成的集合。

- 对象类型：
  - 静态对象：存储于服务器文件系统中，内容相对固定的文件，如 HTML 文件、JPEG/PNG 图像、CSS 样式表、JavaScript 脚本、PDF 文档等。
  - 动态对象：由服务器端应用程序（如 PHP, JSP, ASP.NET, Python）根据请求实时生成的响应内容。
- 引用机制：HTML 文档通过标签（如 \<img>, \<script>, \<link>）引用其他对象，这些对象在渲染时需单独请求。

URL (Uniform Resource Locator) 是 WWW 上的地址编码标准，用于唯一标识互联网上的资源位置。#index[URL]
- 通用格式：$"protocol":\/\/"hostname"[:"port"]\/"path"\/"filename"$
- 组件解析:
  - 协议类型：指定应用层协议，如 http, https, ftp。
  - 主机名：服务器的域名（如 www.phdcomics.com）或 IP 地址。
  - 端口号：可选。HTTP 缺省为 80，HTTPS 缺省为 443。
  - 路径和文件名：资源在服务器文件系统或逻辑路由中的位置（如 /comics.php）。

*代理 (Agents)*
- 客户端 (Client/User Agent)：
  - 通常指 Web 浏览器。
  - 职责：发起 HTTP 请求、接收服务器响应、解析 HTML 文档、构建 DOM 树并渲染页面。
  - 扩展性：对于浏览器无法原生处理的对象类型（如特定的视频格式或文档），通常通过插件 (Plugins) 或扩展程序进行处理。
- 服务器 (Server)：
  - 职责：监听特定端口（通常为 TCP 80），接收请求报文，处理业务逻辑，返回包含请求对象的响应报文。

=== HTTP
*超文本传输协议 (HTTP, HyperText Transfer Protocol)* 是 WWW 的应用层核心协议，定义了浏览器与 Web 服务器之间交换报文的格式和规则。#index[HTTP]
- *传输层依赖*：HTTP 依赖 TCP 提供可靠的数据传输服务。通信前必须先进行 TCP 三次握手。
- *端口*：默认使用 TCP 端口 80。
- *无状态性 (Stateless)*：HTTP 服务器不维护任何关于客户端过去所发请求的记录。服务器处理每个请求都是独立的。
- 优势：服务器设计简单（无需复杂的会话状态管理机），高可用性/可伸缩性（服务器故障重启后无需恢复旧的状态信息）。
- 劣势：无法原生关联同一用户的连续请求（例如购物车功能）。实际应用中通常通过 Cookie/Session 技术在应用层模拟状态。

标准的“请求-响应”模型：客户端发起 HTTP Request。服务器处理并返回 HTTP Response。

针对一个典型的 Web 页面（例如：1个 HTML 文档 + 2个 JPG 图像），不同的连接管理策略对性能影响巨大。

*非持久连接 (Non-persistent Connection)*
- 标准：HTTP/1.0 的默认方式。
- 机制：每个 Web 对象的传输都需要建立一个独立的 TCP 连接。
- 流程分析（以请求含2个图像的页面为例）：
  + 建立 TCP 连接 (1) $->$ 请求/接收 HTML $->$ 关闭 TCP 连接 (1)。
  + 解析 HTML 发现图像 A $->$ 建立 TCP 连接 (2) $->$ 请求/接收图像 A $->$ 关闭 TCP 连接 (2)。
  + 解析 HTML 发现图像 B $->$ 建立 TCP 连接 (3) $->$ 请求/接收图像 B $->$ 关闭 TCP 连接 (3)。
- 连接开销大：服务器需为每个对象分配 TCP 缓冲区和变量，增加了 CPU 和内存负担。
- 延迟高：每个对象都要经历 TCP 三次握手和 TCP 慢启动 (Slow Start) 阶段，导致往返时间 (RTT) 倍增。

*持久连接 (Persistent Connection)*
- 标准：HTTP/1.1 的默认方式（Connection: keep-alive）。
- 机制：服务器在发送响应后，保持 TCP 连接打开。同一客户端的后续请求（如页面内的图片、脚本）通过该已建立的连接发送。
- 减少了握手次数（只需一次 TCP 连接建立）。
- 拥塞控制优化：避免了每个对象传输都重新经历 TCP 慢启动，充分利用已达到的吞吐量。
- HTTP/1.1 允许客户端在未收到前一个请求的响应前，连续发送多个请求。
- 约束：服务器必须按请求的顺序（FCFS）发送响应。这导致了 队头阻塞 (Head-of-Line Blocking) 问题（即第一个请求处理慢会阻塞后续所有响应）。

#table(
  columns: (15%, 20%, 35%, 30%),
  inset: 8pt,
  align: (center, center, left, left),
  stroke: 0.5pt,
  fill: (col, row) => if row == 0 { luma(230) } else { white },
  
  [*版本/年份*], [*连接管理机制*], [*核心特性与改进*], [*性能与局限性*],

  [*HTTP/1.0* \ (1996) \ RFC 1945],
  [非持久连接 \ (Non-persistent)],
  [
    - 无状态协议基础
    - 简单的请求-响应模型
    - 缺乏连接复用机制
  ],
  [
    - *高延迟*：每个对象传输需建立独立TCP连接（多次三次握手）
    - *慢启动*：每次传输都经历TCP慢启动阶段，效率低下
  ],

  [*HTTP/1.1* \ (1999) \ RFC 2616],
  [持久连接 \ (Persistent) \ 默认 Keep-Alive],
  [
    - *流水线机制* (Pipelining)
    - *缓存优化*：引入 ETag, If-None-Match
    - *断点续传*：Range 头域支持
    - *虚拟主机*：支持 Host 头域
  ],
  [
    - *延迟降低*：复用TCP连接，减少握手和慢启动次数
    - *局限*：存在*队头阻塞* (HoL Blocking) 问题，流水线须按序响应
  ],

  [*HTTPS* \ (HTTP+SSL/TLS)],
  [持久连接 + 加密层],
  [
    - *安全性增强*：在TCP之上增加SSL/TLS层
    - *三大要素*：数据保密性、完整性校验、身份认证
  ],
  [
    - *开销*：增加了TLS握手的RTT和加解密计算开销
    - *必要性*：现代Web安全的基石
  ],

  [*HTTP/2* \ (2015) \ RFC 7540],
  [多路复用 \ (Multiplexing)],
  [
    - *二进制分帧*：取代文本格式，解析更高效
    - *多路复用*：单TCP连接并行传输，解决应用层HoL阻塞
    - *头部压缩 (HPACK)*：减少Header带宽消耗
    - *服务端推送* (Server Push)
  ],
  [
    - *高吞吐*：显著提高带宽利用率
    - *低延迟*：彻底解决了HTTP层面的队头阻塞
    - *依赖*：主要运行在HTTPS之上
  ]
)

== FTP
在计算机网络环境中，实现文件共享和传输面临着“异构性”的复杂挑战。不同的操作系统（如 Windows, Linux, macOS, Mainframe）在底层实现上存在显著差异：
- 数据存储格式：不同系统对文本文件（如换行符 CR/LF 的处理）、二进制数据的字节序（大端/小端）处理不同。
- 文件系统差异：目录结构（树状 vs 扁平）、路径分隔符（\\ vs /）、命名规则（长度限制、大小写敏感性）各不相同。
- 访问控制：权限管理模型（RWX, ACL等）存在差异。

*FTP (File Transfer Protocol)* 的设计初衷正是为了屏蔽这些底层系统的细节差异。作为 Internet 上使用最广泛的应用层协议之一（RFC 959, 1985年标准），它提供了一个通用的抽象层，使得用户可以在异构网络中的任意计算机之间进行可靠的文件传输。#index[FTP]

FTP 基于 *C/S (Client/Server)* 架构，采用 TCP 作为传输层协议，保证数据的可靠性。其最显著的架构特点是采用了 *双通道协议（Dual-channel Protocol）* 设计。

与 HTTP 等协议将控制信息（头部）和数据放在同一个连接中传输不同，FTP 将命令交互和数据传输在物理连接上进行了分离：

- *控制连接 (Control Connection)*
  - 端口：服务器端 TCP 21。
  - 生命周期：持久连接。在整个会话期间一直保持打开状态。
  - 作用：用于传输控制命令（如登录认证、改变目录 CWD、列出文件 LIST、请求传输 RETR/STOR）和响应状态码。不传输文件内容。
  - 特性：基于 Telnet 协议，使用 NVT ASCII 字符集进行命令交互。
- *数据连接 (Data Connection)*
  - 端口：服务器端 TCP 20（主动模式下）。
  - 生命周期：临时连接。每当有文件传输请求或目录列表请求时创建，传输完毕后立即关闭。
  - 作用：专门用于传输文件内容（支持 ASCII 模式和二进制 Binary 模式）。

在实际网络中，FTP 数据连接的建立方式分为两种：
- 主动模式 (Active Mode/PORT)：客户端在控制信道告知服务器自己的临时端口 $N$，服务器从 TCP 20 主动连接客户端的端口 $N$。
- 被动模式 (Passive Mode/PASV)：客户端请求被动模式，服务器开启一个随机高位端口 $P$ 并告知客户端，客户端主动连接服务器的端口 $P$。（此模式常用于解决客户端位于防火墙/NAT 后面的问题）。

*TFTP (Trivial File Transfer Protocol)* 是一个轻量级的文件传输协议，设计目标是小巧、易实现，常用于无盘工作站启动（PXE Boot）或网络设备（路由器/交换机）配置文件的读写。#index[TFTP]

- 传输层协议：使用 UDP（端口 69），这与使用 TCP 的 FTP 有本质区别。
- 仅支持文件的读写。不支持交互式命令（如列目录）。不支持用户身份鉴别和复杂的权限控制。
- 数据模式：支持 ASCII（Netascii）和八位二进制（Octet）传输。

TFTP 不依赖 TCP 的流式传输，而是自己在应用层实现了类似 *停止-等待协议 (Stop-and-Wait)* 的可靠传输机制。
- PDU 结构：TFTP 将文件分割为一个个数据协议数据单元（PDU），称为文件块 (Block)。
- 块编号：每个块从 1 开始按序编号。
- 块大小：标准数据块包含 512字节 的数据。

由于 UDP 不提供可靠性，TFTP 必须自行处理：
- 确认机制：发送方每发送一个 512 字节的数据块，必须等待接收方发送对应块编号的 ACK (确认 PDU) 后，才能发送下一个块。
- 超时重传：如果在规定时间内未收到 ACK，发送方会重传该数据块。同理，如果接收方发送 ACK 后未收到下一个数据块，也会重传 ACK。

TFTP 没有专门的“传输结束”命令，而是通过数据块的大小来隐式判断：
- Case A：若最后收到的数据块大小 < 512 字节 $->$ 文件传输结束。
- Case B：若文件大小恰好是 512 的整数倍 $->$ 发送方在发完最后一个满块后，必须再发送一个 数据长度为 0 的数据 PDU，接收方收到这个空块后判定传输结束。

#table(
  columns: (18%, 41%, 41%),
  inset: 10pt,
  align: (center, left, left),
  stroke: 0.5pt,
  fill: (col, row) => if row == 0 { luma(230) } else { white },
  
  [*比较维度*], [*FTP (文件传输协议)*], [*TFTP (简单文件传输协议)*],

  [*RFC 标准*],
  [RFC 959 (1985)],
  [RFC 1350 (1992)],

  [*传输层协议*],
  [*TCP* (面向连接，提供可靠传输)],
  [*UDP* (无连接，需应用层处理可靠性)],

  [*端口占用*],
  [控制连接: *21* \ 数据连接: *20* (主动模式)],
  [初始连接: *69* \ 数据传输: 动态临时端口],

  [*体系架构*],
  [*双通道机制* \ - 控制连接：会话期间持续保持 \ - 数据连接：传输文件时动态建立/销毁],
  [*单通道机制* \ - 类似停止-等待协议 (Stop-and-Wait) \ - 发送512字节块 -> 等待 ACK],

  [*身份鉴别*],
  [支持。允许设置用户名/密码权限控制],
  [不支持。无登录过程，开放访问],

  [*交互功能*],
  [强大且复杂：\ - 列出目录清单 (ls/dir) \ - 切换目录 (cd) \ - 文件重命名/删除],
  [极其精简：\ - 仅支持文件读取 (Read) \ - 仅支持文件写入 (Write)],

  [*传输模式*],
  [Stream (流模式), Block, Compressed \ 支持 ASCII 与 Binary],
  [Octet (二进制), Netascii (文本) \ 固定 512 字节数据块],

  [*应用场景*],
  [广域网/局域网内的通用文件共享、网站发布、异构系统大文件传输],
  [无盘工作站启动 (PXE Boot)、网络设备 (路由器/交换机) 配置文件备份与固件升级]
)
