#import "../note.typ": *


= 数据链路层

== 数据链路层的设计问题

- 向下: 利用物理层提供的位流服务
- 向上: 向网络层提供明确的 (well-defined) 服务接口

对于局域网, 数据链路层被划分成介质访问控制层和逻辑链路控制层。

数据链路层的功能有: 
- *成帧(Framing)*
  - 将比特流划分成“帧”的主要目的是为了检测和纠正物理层在比特传输中可能出现的错误, 数据链路层功能需借助“帧”的各个域来实现; 数据链路层收到网络层下发的数据包 (如IP数据报) 后, 并不会直接发送, 而是在数据的前后加上特定的帧首部和帧尾部, 从而形成一个完整的“帧”
- *差错控制 (Error Control)*
  - 处理传输中出现的差错, 如位错误、丢失等
- *流量控制 (Flow Control)*
  - 确保发送方的发送速率, 不大于接收方的处理速率
  - 避免接收缓冲区溢出
- *访问控制(Access Control)*
  - 当多条链路共享同一个传输介质时 (例如传统的以太网) , 数据链路层还需要协调哪个节点有权在何时发送数据, 以避免信号冲突。这项功能主要由媒体访问控制 (MAC)  子层负责

数据链路层提供的服务: 
- *无确认 无连接 服务* ( Unacknowledged connectionless ) 
  - 接收方不对收到的帧进行确认
  - 适用场景: 误码率低的可靠信道；实时通信；
  - 网络实例: *以太网*
  - 追求最高效率和最低延迟, 适用于像以太网这样非常可靠的信道, 或者像实时音视频这样可以容忍少量丢失但要求低延迟的应用。
- *有确认 无连接 服务* ( Acknowledged connectionless) 
  - 每一帧都得到单独的确认
  - 适用场景: 不可靠的信道 (无线信道) 
  - 网络实例: *802.11*
  - 在可靠性和效率之间取得平衡。它不为整个会话预先建立通道, 而是为每一帧独立负责, 适用于像Wi-Fi这样可能不太稳定的无线信道。
- *有确认 有连接 服务* ( Acknowledged connection-oriented ) 
  - 适用场景: 长延迟的不可靠信道。
  - 提供最高级别的可靠性, 保证数据按序、无误地交付。适用于对错误零容忍、时延不敏感但要求绝对可靠的场景。
  
#remark[
  - *连接*: 好比两个人打电话前需要先拨号并等待对方接听。这个“建立通话”的过程就是建立连接。它意味着在正式传输数据前, 通信双方需要先进行“握手”, 协商好一些参数 (如初始序列号) , 为后续的通信做好准备。通信结束后, 还有一个“挂断电话” (释放连接) 的过程。
  - *确认*: 好比在微信聊天中, 你发出一条重要消息后, 希望对方回复 “收到”。这个“收到”就是确认。它意味着接收方在成功收到一个数据帧后, 会向发送方返回一个特定的反馈信号 (确认帧) , 告知发送方“我已成功收到”。
]

#note[
  *“有连接”和“无确认”在根本目标上是相互矛盾的。*

  + *“连接”的价值在于保障可靠传输*

    建立连接本身是需要开销的 (交换控制帧、维护连接状态等) 。之所以愿意付出这个开销, 其根本目的就是为了实现可靠的、有序的数据传输。如果建立连接后, 却不对传输的数据帧进行确认 (即“无确认”) , 那么发送方就无法知道帧是否成功到达接收方, 一旦帧在传输中丢失或损坏, 系统将无法察觉, 也更谈不上重传和恢复。这就使得“建立连接”所追求的可靠性目标完全落空了。这种“高投入、零保障”的设计在工程上是没有意义的。

  + *确认是维持连接状态同步的关键*

    在面向连接的通信中, 连接双方需要维护一些共同的状态, 其中最关键的就是帧的序列号, 它用于保证帧的有序接收和避免重复。确认机制是确保发送方和接收方在这些状态上保持同步的核心手段。例如, 接收方通过确认告知发送方“我已正确收到序列号为n的帧, 期待接收下一帧n+1”。如果没有确认, 发送方和接收方的状态很容易失步, 这条“连接”也就名存实亡了。

*“面向连接”的设计初衷就是为了实现可靠传输, 而“确认”机制是达成这一目标不可或缺的技术手段。*
]

数据从网络层下来, 是一个个分组。数据链路层需要将这些分组打包, 加上自己的“包装”, 形成帧, 才能在物理链路上传输。接收方从物理层收到一串连续的比特流, 它如何能准确地从这串比特流中识别出每一个帧的开始和结束位置？这个问题也叫*帧同步*或*帧定界*。

+ *字节计数法*
  - 原理: 在每个帧的开头用一个特殊的字段 (计数字节) 来明确告知接收方: “这个帧一共包含多少字节”。接收方根据这个数字来读取相应长度的数据, 自然就知道下一个帧从哪里开始。
  - 优点: 简单直接。
  - 致命缺点: 异常脆弱。如果这个计数字节本身在传输中出错, 那么接收方不仅会错误地划分当前帧的边界, 还会导致后续所有帧的划分全部错乱, 造成“一连串帧的错误”。
  #remark[
    注意计数字节是总字节数, 计数要包括他自己的。

    而计数字节占几个字节, 具体按照协议来确定, 例如DDCMP协议的计数字节有14位, 而如果是只用1字节 (8位) 来计数的话, 确实最多只有255字节。真正的数据字节也只有254个。
  ]

+ *带字节填充的定界符法*
  - 为了解决字节计数法的脆弱性, 提出了使用“定界符”的方法。
  - 原理: 选用一个特殊的字节作为帧的开始和结束标志, 如 01111110(十六进制 0x7E), 这个字节就叫做 FLAG (标志字节) 。帧的结构变为: FLAG + 头标 + 有效载荷 + 尾标 + FLAG。
  - 新问题: 如果有效载荷 (数据部分) 里恰好包含和 FLAG相同的字节怎么办？接收方会误以为帧提前结束了。
  - 解决方案: 字节填充: 
    - 发送方在发送数据前, 会检查有效载荷。
    - 如果数据中出现 FLAG字节, 就在它前面插入一个特殊的转义字节。
    - 如果数据中本身就有转义字节 ESC, 就在它前面也插入一个 ESC。
  - 不足: 这种方法基于字节操作, 需要字符边界对齐, 与物理层传输的原始比特流特性不太匹配。
+ *带比特填充的定界符法*(也叫零比特填充法)
  - 这是目前最常用的方法, 它直接在比特级别进行操作。
  - 原理: 定界符仍然是 01111110 (6个连续的1两边是0) 。
  - 新问题: 如果有效载荷的比特流中, 恰好出现了连续6个“1”的位模式, 接收方也会误判为帧边界。
  - 解决方案: 比特填充
    - 发送方: 在发送数据时, 会持续监视有效载荷的比特流。只要出现连续5个“1”, 就自动插入一个“0”。
    - 这样, 有效载荷中永远不可能出现6个连续的“1”, 从而与定界符 01111110完美区分开。
    - 接收方: 在接收时, 进行相反操作。它持续监视比特流, 遇到连续5个“1”后, 如果下一个比特是“0”, 就认定这个“0”是填充的, 直接将其删除；如果下一个比特是“1”, 再结合前一位看是否是定界符。
    #remark[
      HDLC 协议就是这种方式。
    ]
+ *物理层编码违例*
  - 这种方法利用物理层信号编码中一些无效的、不会出现在数据部分的电信号序列来作为帧的边界。例如, 在曼彻斯特编码中, 某些电平跳变模式是非法的, 这些非法模式就可以用来标记帧的起始和结束。
  - *4B/5B 编码方案*
    - 原理: 不直接传输4位的数据, 而是将其映射为一个5位的编码。这样会有32种可能的5位组合, 但只使用其中的16种来表示数据 (0-15) 。剩下的16种组合是“闲置”的、不会出现在正常数据中的非法码字。
    - 成帧应用: 可以挑选一个或多个这样的闲置码字 (例如00110) 作为帧的起始或结束标志。因为数据部分根本不会出现这些码字, 所以接收方能毫无歧义地识别出帧边界。
  - *前导码*
    - 原理: 在帧的真正数据内容之前, 先发送一长段固定的、已知的比特模式 (例如0101交替的序列) , 这个模式就是前导码。它的主要作用是让接收方的时钟与发送方同步。同时, 这个独特的、冗长的模式本身也起到了帧开始定界的作用。
    - 成帧应用: 接收方通过识别这个特定的前导码模式, 就知道紧接着的就是帧的有效载荷。传统以太网和802.11 (Wi-Fi)  都采用这种方式。
  - *曼彻斯特编码违例*
    - 正常编码规则: 在曼彻斯特编码中, 每个比特中间必须有一次电平跳变。例如, 低电平跳变到高电平表示0, 高电平跳变到低电平表示1。
    - 违例码: 如果出现一个比特周期内电平维持高或低不变, 没有中间跳变, 这就违反了曼彻斯特编码的基本规则, 称为“违例码”。
    - 成帧应用: 这种在正常数据中绝对不可能出现的“违例码”, 就可以被用作帧的起始或结束定界符。IEEE 802.5令牌环网就使用了这种技术。

链路层存在的一个问题: 信道的噪声导致数据传输问题。
- 差错 ( incorrect ) : 数据发生错误。
- 丢失 ( lost ) : 接收方未收到。
- 乱序 (out of order) : 先发后到, 后发先到。
- 重复 (repeatedly delivery) : 一次发送, 多次接收。

解决方案: 差错检测与纠正、确认重传。
- 确认: 接收方校验数据 (差错校验) , 并给发送方应答, 防止差错。
- 定时器: 发送方启动定时器, 防止丢失。
- 顺序号: 接收方检查序号, 防止乱序递交、重复递交。

链路层存在的另一个问题: 接收方的处理速率。接收方的接收缓冲区溢出。
- 基于反馈 (feedback-based) 的流量控制: 接收方反馈, 发送方调整发送速率。
- 基于速率 (rate-based) 的流量控制: 发送方根据内建机制, 自行限速。

== 差错检测和纠正
通常采用增加冗余信息 (或称校验信息) 的策略解决信道传输差错问题。保证一定差错检测和纠错能力的前提下, 如何减少冗余信息量？需要同时考虑: 信道的特征和传输需求、冗余信息的计算方法、携带的冗余信息量、计算的复杂度等。两种主要策略为*检错码 (error-detecting code)* 和*纠错码 (error-correcting code)*。

- 检错码: 在被发送的数据块中, 包含一些冗余信息, 但这些信息只能使接收方推断是否发生错误, 但不能推断哪位发生错误, 接收方可以请求发送方重传数据。主要用在高可靠、误码率较低的信道上, 例如光纤链路。偶尔发生的差错, 可以通过重传解决差错问题。
- 纠错码: 发送方在每个数据块中加入足够的冗余信息, 使得接收方能够判断接收到的数据是否有错, 并能纠正错误 (定位出错的位置) 主要用于错误发生比较频繁的信道上, 如无线链路, 也经常用于物理层, 以及更高层。使用纠错码的技术通常称为*前向纠错* (FEC, Forward Error Correction)。

典型的检错码有: 
- *奇偶检验 (Parity Check)*: 1位奇偶校验是最简单、最基础的检错码
- *校验和 (Checksum)*: 主要用于TCP/IP体系中的网络层和传输层
  - 发送方: 进行 16 位二进制补码求和运算, 计算结果取反, 随数据一同发送
  - 接收方: 进行 16 位二进制补码求和运算 (包含校验和) , 结果非全1, 则检测到错误
- *循环冗余校验 (Cyclic Redundancy Check, CRC)*: 数据链路层广泛使用的校验方法#index[CRC]
  - 发送方根据要发送的原始数据, 通过一个特定的计算, 生成一小段“校验码” (可以理解为数据的“指纹”或“摘要”) , 并附加在数据末尾一起发送。接收方收到数据后, 用同样的算法再计算一遍。如果接收方计算出的“指纹”与发送方发来的“指纹”不一致, 就断定数据在传输中发生了错误。
  + 确定生成多项式G: 
    - 发送方和接收方必须事先约定一个生成多项式G。它是一个n+1位的二进制数, 最高位必须是1。
    - 例如: $G=110101$, 对应的代数形式是$G(x)=x^5+x^4+x^2+1$.
  + 构造被除数:
    - 在原始数据 D的末尾添加 n个0。这相当于将 D乘以$2^n$ (即左移 n位) 。
  + 进行模2除法: 
    - 这是CRC计算的核心。用上一步得到的数 (D后面加n个0) 作为被除数, 用生成多项式 G作为除数, 进行 模2除法 (或称“异或除法”) 。
    - 模2运算规则: 加减法都等同于异或 (XOR)  运算, 即相同为0, 不同为1。不借位, 不进位。
    - 最终结果: 最后得到的余数 R就是CRC校验码。如果余数位数不足 n位, 要在前面用0补足。
  + 组成实际发送的数据: 
    - 将原始数据 D和计算得到的CRC校验码 R拼接在一起, 就是实际在信道中传输的数据 T。
  + 接收方收到数据 T (即 D+ R) 。
  + 用同样的生成多项式G对收到的整个 T再做一次模2除法。
  + 判断余数: 如果余数为0: 则认为数据传输正确无误, 否则传输错误。

#remark[
  CRC 检验可以使用硬件来完成, 主要得益于其核心的数学运算——模2除法 (即异或运算), 这种运算非常适合用简单的数字逻辑电路来实现。
]
#note[
  CRC校验码是对“目的MAC地址+源MAC地址+类型/长度+数据区”进行计算的, 不包括前导码和帧定界符。
]

#example[
  若甲向乙发送数据时采用CRC检验, 生成多项式是G=10011, 则乙方收到比特串()时, 可以判定传输过程无错误。
  A. 10111 0000 B.10111 0100 C. 10111 1000 D. 10111 1100

  在解决这道题时, 不要一个个傻试, 要学会观察发现传递的数据是10111, 这样我们只需要检验这个对应的余数是多少就可以了, 答案为*D. 10111 1100*
]

#remark[
  错误图样E(x)的出现, 是为了从数学上描述和解释 接收端收到的数据 T'和发送方实际发送的是原始数据 D和校验码 R组合而成的 T 之间的差异。
  $
    T'=T+E
  $
  接收端计算得到的余数, 实际上完全取决于错误图样 E除以生成多项式 G的余数。
  $
    "余数" = T' / G "的余数" = (T + E) / G "的余数" = 0 + E / G "的余数" = E / G "的余数"
  $

  所以一个良好的G, 需要在面对多种错误时, 都能使得这个余数不为0, 这样就能把错误检测出来了。

  生成多项式G的作用: 决定校验能力, 因为不同的生成多项式 G(x)具有不同的数学特性, 这直接决定了CRC校验码的检错能力。一个设计良好的 G(x)可以确保: 
  - *能检测所有奇数个比特的错误*: 如果 G(x)包含因子 (x+1), 就能具备此能力。(是否包含因子(x+1)等价于G(1)是否为0)
  - *能检测所有长度小于等于n位的突发错误 (突发错误指连续多个比特出错) 。*
    - 一个长度为 L的突发错误, 其错误多项式 E(x)的形式为$x^i times B(x)$其中$B(x)$是一个次数为 L-1的多项式, 并且它的首项和尾项系数都是1 (因为错误发生在连续的一段上) 。
    - 假设$G(x)$不能被 $x$整除 (即常数项为1, 所有实用的 $G(x)$都满足) , 那么 $G(x)$和 $x^i$是互质的。
    - 要使 $E(x) = x^i times B(x)$能被 $G(x)$整除, $G(x)$必须能整除$ B(x)$。
    - 但是, $B(x)$的次数是 $L-1$, 而 $G(x)$的次数是 $r$。如果 $L lt.eq.slant r$, 即 $L-1 < r$, 那么 $B(x)$的次数严格小于 $G(x)$的次数。
    - 因此, 只要突发错误长度 $L lt.eq.slant r$, $E(x)$就不可能被 $G(x)$整除, 错误100% 会被检测到。

  - *以极高的概率检测更长的突发错误。*
    - 当突发错误长度 $L > r$时, $B(x)$的次数 $L-1 gt.eq.slant r$, 此时 $B(x)$有可能被 $G(x)$整除。
    - 我们关心的是其中有多少个 $B(x)$恰好能被 $G(x)整$除。在数学上, 一个随机多项式能被一个固定的 $r$次多项式整除的概率大约是 $1 / 2^r$。
    - 更精确地说, 不能被检测到的突发错误模式数量与 $2^(L-2) / 2^r = 1 / 2^(r - L + 2)$成正比。
    - 当 $L$很大时, 这个概率趋近于 $1 / 2^r$。

  四个国际标准生成多项式
  - CRC-12: $x^12 +x^11 + x^3+x^2+x+1$
  - CRC-16: $x^16 +x^15+x^2+1$
  - CRC-CCITT: $x^16+x^12+x^5+1$
  - CRC-32: $x^32+x^26+x^23+x^22+x^16+x^12+x^11+x^10+x^8+x^7+x^5+x^4+x^2+x+1$
例如, 以太网、无线局域网使用CRC-32生成多项式

]

== 基本的数据链路层协议
物理层进程和某些数据链路层进程运行在专用硬件上 (网络接口卡) 数据链路层进程的其他部分和网络层进程作为操作系统的一部分运行在CPU上, 数据链路层进程的软件通常以设备驱动的形式存在。

关键假设
- *分层进程独立假设*
  - 网络层、数据链路层、物理层为独立的进程
  - 进程间通过传递消息实现通信
- *提供可靠服务假设*
  - 提供可靠的、面向连接的服务
  - 数据链路层发送的数据随时可向网络层获得
- *只处理通信错误假设*
  - 仅处理通信错误
  - 假设机器不会崩溃, 不考虑断电、重启等引起的问题
  

=== 乌托邦式单工协议
立在最理想假设上的理论模型, 用于建立基本概念。
- 核心特征: 
  - *信道绝对可靠*: 数据帧在传输中既不会出错也不会丢失。
  - *接收方处理能力无限*: 接收方总能及时处理任何到达的帧。
- 工作方式: 
  - 发送方源源不断地将帧发送到信道上。
  - 接收方源源不断地接收帧, 并交给上层网络层。
=== 无错信道单工停止-等待协议
在“乌托邦协议”的基础上, 引入了第一个现实约束: 接收方的处理速度是有限的。
- 核心问题: 如果发送方发送帧的速度快于接收方处理的速度, 接收方的缓冲区将会溢出, 导致帧丢失。
- 解决方案: 停止-等待。
  - 发送方每发送一帧后, 就停止发送, 等待接收方的一个肯定确认。
  - 只有当收到对当前帧的确认后, 发送方才发送下一帧。
- 本质: 引入了流量控制, 确保发送速率不超过接收方的处理能力。但它仍然假设信道是无错的, 即帧和ACK永远不会损坏或丢失。
=== 有错信道单工停止-等待协议
信道错误导致两个新问题: 数据帧损坏或丢失, 确认帧 (ACK) 损坏或丢失。解决方案是引入一个核心机制: 超时重传。发送方在发送一帧后, 启动一个计时器。如果在该帧的确认 (ACK) 返回之前计时器超时, 发送方就认为该帧或其ACK已丢失, 于是重新发送该帧。

#index(initial: "Y")[有错信道单工停止-等待协议]

超时重传机制带来了一个新问题: 重复帧。发送方发送帧A1, 接收方收到后发回ACK, 但ACK丢失。发送方超时后重发A1。此时, 接收方会收到两个完全相同的A1帧。接收方无法区分这是新帧还是重传的旧帧, 可能导致上层应用重复接收数据。解决方案是引入序号, 在每个发送的帧的头部加入一个序号 (SEQ) 。

接收方检查到达帧的序号。如果序号与期望收到的下一帧序号相同, 则接收该帧, 并上交网络层, 同时发送ACK。如果序号是已经接收过的 (即重复帧) , 则直接丢弃该帧, 但仍为这个重复帧发送一个ACK (ACK中包含被确认的帧序号) , 以便让发送方继续推进。

#remark[
  1比特序号 (0和1) 就足够了。因为停止-等待协议同一时刻只有一个帧未被确认。接收方只需要能区分“当前帧”和“下一帧”就足够了。序号在0和1之间交替, 足以识别出是新的还是重复的帧。
]

这个机制的完整过程是: 
- 发送方: 
  - 初始化序号 (如0) , 从网络层获取数据, 组装帧并发送, 同时启动计时器。
  - 等待事件 (三种可能) : 
    - 收到正确ACK: 序号加1, 准备发送下一帧。
    - 收到错误ACK或超时: 重发当前帧, 并重启计时器。
- 接收方: 
  - 初始化期望的序号 (如0) 。
  - 等待帧到达。
    - 序号正确: 上交网络层, 回送ACK, 期望序号加1。
    - 序号错误 (重复帧) : 丢弃帧, 但仍回送ACK (确认最近正确收到的帧) 。
这个完整的协议也被称为 ARQ (自动重复请求)  或 PAR (带重传的肯定确认) 。


#remark[
  长肥网络 (LFN, Long Fat Network): 如果一个网络的带宽-延迟乘积 (bandwidth-delay product) 很明显的大于 $10^5$ bits (12 kB), 则可以被认为是长肥网络
]#index[LFN]#index(initial: "C")[长肥网络]

该协议的主要缺点: *信道利用率低, 尤其在长肥网络环境下*。发送方在大部分时间处于“等待”状态。发送一帧的时间很短 (F/R) , 但等待确认的时间很长 (包括往返传播延迟 2I和处理时间) 。
$
  "信道利用率"="发送时间"/ ("发送时间"+"等待时间")="数据帧发送时延"/("数据帧发送时延"+"往返时延"+"确认帧发送时延")
$
#note[
  注意确认帧发送时延是在分母中的。

  当源主机和目的主机之间的距离很远而且数据传输速率很高时, 停等协议的效率会很低。
]

#example[
  一个信道的数据传输速率是4 kb/s, 单向传播时延是30 ms, 若使停等协议的信道最大利用率达到80%, 则要求的数据帧长至少是多少?

  $
    L/C / (L/C+2R)=80%
  $
  解得$L=960 "bit"$

  特别注意, 要考虑确认帧回传的传播时间, 所以分母是2R. 因为确认帧长度很短, 所以忽略了他的发送时间。 
]
等待时间中还会包含往返传播延迟和处理时间。使用更大的帧可以提高效率, 但帧越大, 传输中出错的概率也越高, 会导致更多重传, 效率反而可能下降, 它本身的效率问题也引出了后续更复杂的滑动窗口协议。


== 滑动窗口协议
停止-等待机制降低了信道利用率。设数据速率记为R, 帧长度记为F, 往返延迟记为D, 则采用停-等协议的线路效率为: 
$
  F/(F+R dot D)
$
解决方案: 流水线协议或管道协议: 允许发送方在没收到确认前连续发送多个帧


=== 滑动窗口协议
当发送窗口和接收窗口大小都为1时, 滑动窗口协议退化成了“停止-等待协议”。它一次只能发送一帧, 等待确认, 然后再发下一帧。

#remark[
  序号循环使用, 默认从0开始编号。
]
=== 回退N协议 (Go-Back-N, GBN) 
GBN协议的核心策略是“出错全部重发”。#index[GBN]
- *发送方*: 可以连续发送窗口内的所有帧
- *接收方*: 非常“刻板”, 它只按顺序接收帧 (接收窗口大小为1) 。
  - 如果收到的帧正是它期望的那一帧, 它就接收, 并返回一个累积确认 (例如, 确认帧3意味着0,1,2,3号帧都已正确收到) 。
  - 如果收到的帧不是它期望的 (比如乱序或出错) , 它会丢弃这个帧以及之后所有的帧, 并不为这些丢弃的帧发送任何确认。
  #note[
    特别注意, 确认帧可能会因为超时而导致缺失, 例如发送方收到了1,2,5的确认帧, 不需要重传3,4, 只要收到了5的确认帧, 就证明1-5都被接收方接收了。
  ]
- *发送方*: 为每一帧启动定时器。如果某个帧 (比如帧2) 的定时器超时了 (意味着帧2或其确认丢失) , 发送方会退回到帧2的位置, 重新发送帧2以及其后所有已发送但未确认的帧。
- 优点: *实现简单, 连续发送大大提高了信道利用率。*
- 缺点: 一旦出错, 即使后面有正确送达的帧也会被丢弃重传, 浪费了带宽。适用于信道质量较好的环境。

如果帧序号用$n$比特表示, 则有$2^n$个序号。为了保证能正确区分新帧和旧帧 (避免序号重复带来的混淆) , 发送窗口的大小 $W_T$必须满足: $1 < W_T lt.eq.slant 2^n-1$.

- *缓存*: 发送方必须能够缓存整个窗口内已发送但未确认的帧, 以便超时重传。
- 响应三事件: 发送方的逻辑需要处理三种事件: 
  - 上层调用: 有数据要发, 且窗口未满, 则发送。
  - 收到ACK: 采用累积确认。
  - 超时事件: 任何一个已发送帧的超时, 都会触发整个窗口的回退重传。

- *接受方*: 窗口尺寸: $W_R = 1$, 表示接收方在同一时刻, 只准备接收一个特定的数据帧, 极大地简化了接收方的设计。它不需要为多个乱序的帧准备缓存空间和管理逻辑。

- 接收方只接受序号完全符合它期望的那个帧。如果收到的帧序号不对 (可能是传输出错导致帧无效, 也可能是正确的帧但顺序乱了) , 接收方会直接丢弃这个帧以及其后到来的所有帧。保证上交到上层的数据绝对是按顺序的。这是GBN协议可靠性的基础, 但也是其可能浪费带宽的根源 (因为会丢弃可能正确的乱序帧) 。

#note[
  对于滑动窗口协议, *序列号个数大于等于窗口数 (发送窗口+接收窗口)*.

  而对于GBN协议中, 接收窗口固定为1.但在SR协议中, 有$1<W_R lt.eq.slant W_T$.
]


#example[
  两台主机之间的数据链路层采用后退N帧协议 (GBN) 传输数据, 数据传输速率为16kb/s, 单向传播时延为270ms, 数据帧长范围是128~512字节, 接收方总是以与数据帧等长的帧进行确认。为使信道利用率达到最高, 帧序号的比特数至少是多少?

  在该题中, 确认帧帧长等于数据帧帧长。则有连续ARQ的信道利用率: 
  $
    ("发送窗口大小"times "数据帧长"/"数据传输速率")/("数据帧长"/"数据传输速率"times 2+"RTT")=("发送窗口大小"/"数据传输速率")/(2/"数据传输速率"+"RTT"/"数据帧长")
  $
可知, 数据帧越长, 信道利用率越高, 为了保证无论数据帧长如何变化, 信道利用率都最高, 应该以128B进行运算。发送一帧时间为64ms, 发生一帧到收到确认帧总时间是64+270*2+64=668ms, 这段时间可以发送668/64=10.4帧, 所以发送窗口大小至少是11, 总窗口大小至少是12, 所以至少用*4*位进行编码。
]

#note[
  协议也会影响到数据传输速率。首先, 信道带宽能直接制约数据的传输速率, 传输速率一定是小于或等于信道带宽的。其次, 要考虑发送平均数据传输速率一个数据到接收到它的确认之前，最多能发送多少数据。
]

#example[
  主机甲与主机乙之间使用后退N帧协议(GBN)传输数据, 主机甲的发送窗口尺寸为1000, 数据帧长为1000字节, 信道带宽为100Mb/s, 主机乙每收到一个数据帧, 就立即利用一个短帧(忽略其传输延迟)进行确认, 若主机甲和主机乙之间的单向传播时延是50ms, 则主机甲可以达到的最大平均数据传输速率约为多少?

  甲方的发送窗口尺寸为1000, 即收到第一个数据的确认前, 最多能发送1000个数据帧, 即1000×1000B=1MB的内容, 而从发送第一个帧到接收到它的确认的时间是一个帧的发送时间加上往返时间, 即$1000B÷100"Mb/s"+50"ms"+50"ms"=0.10008s$, 此时的最大传输速率为$1"MB""/"0.10008"s"≈10"MB/s"=80"Mb/s"$。信道带宽为$100"Mb/s"$, 因此答案为$min{80"Mb/s",100"Mb/s"}=80"Mb/s"$。
]
=== 选择重传协议 (Selective Repeat, SR) 
#index[SR]
GBN协议在遇到数据帧出错或丢失时, 会重传该帧及之后所有已发送但未确认的帧。这种“一刀切”的方式在信道质量差时会造成*大量的带宽浪费*。“谁出错, 就重传谁”。发送方只重传那些真正出错 (超时或收到否认) 的帧, 而对已经正确到达接收方的帧不予干涉。

为了实现这一目标, SR协议对接收方提出了新的要求: 
- 接收窗口大于1: 接收方必须能够缓存那些虽然乱序到达但正确的帧。
- 逐个确认: 接收方需要为每个正确收到的帧发送确认, 使发送方能精确知道每个帧的状态。

优点: 极大提高了信道利用率, 避免了GBN协议中因单个帧错误而重传大量正确帧的带宽浪费。

缺点: 实现更复杂, 接收方需要更大的缓存空间来存放乱序的帧。

- 发送方: 为每一个数据帧维护一个独立的定时器。如果某帧的定时器超时, 发送方就只重传这一帧。
  - 为了保证能正确区分新帧和旧帧 (避免序号重复带来的混淆) , 发送窗口的大小 $W_T$必须满足: $1 < W_T lt.eq.slant 2^(n-1)$.
  - 收到对某个帧k的否定确认 (NAK) 或该帧超时, 则只重传帧k。
  - 收到ACK(k)后, 标记帧k为已确认。如果k是窗口中最小的序号, 则滑动窗口。
- 接收方: 
  - 如果收到一个帧出错, 则丢弃它。
  - 如果收到一个帧正确但序号乱序 (即不是期望的下一个帧) , 则将其缓存起来, 并为其发送一个确认 (ACK) 。
  - 只有当按序收到某个帧时 (比如收到了期望的帧k) , 接收方才会将帧k以及之前缓存的所有连续序号帧 (如k, k+1, k+2...) 一起按序上交网络层, 然后滑动接收窗口。
  - 接收窗口大小 $W_R$通常等于发送窗口 $W_T$
  - 对落在接收窗口内的帧, 无论是否按序, 只要正确就缓存并发送ACK。对落在接收窗口外的帧, 一律丢弃。
  - 当接收窗口底部 (即期望的下一个帧) 到达时, 将其连同之后已缓存的连续帧一起上交网络层, 然后滑动接收窗口。
  - 使用肯定确认 (ACK)  和否定确认 (NAK) 。NAK(k)用于显式地告诉发送方: “帧k出错, 请重传”。

#example[
  情景: 发送方发送了帧0, 1, 2, 3, 4。但帧2在传输中丢失了。

  - 如果是GBN: 接收方收到帧3 (乱序) 后会丢弃它, 并持续发送对帧1的确认 (ACK1) 。发送方最终会因帧2超时, 而重传帧2, 3, 4。
  - 而SR协议中: 接收方收到帧3和帧4后, 虽然它们乱序, 但会被缓存起来, 并分别发送ACK3和ACK4。发送方在帧2超时后, 只重传帧2。一旦接收方收到帧2, 它就可以将已按序的帧2, 3, 4一起上交网络层。
  - SR协议在这种情况下节省了重传帧3和帧4的带宽。
]

#example[
  - 情景: 接收方收到了帧2, 并发送了ACK2, 但ACK2丢失了。
  - 处理: 发送方会因为收不到ACK2而超时, 并重传帧2。
  - 此时接收方已经缓存了帧2, 当收到重传的帧2时, 它会发现这是重复帧, 于是丢弃该帧, 但会重新发送一个ACK2给发送方, 以帮助发送方滑动窗口。
]

== 点到点链路层协议PPP

#remark[
  信道有2种工作方式, 点对点协议与广播协议。点对点协议以PPP协议为例, 广播协议以以太网协议为例。
]

#index[PPP]
PPP协议不是一个单一协议, 而是一个协议族, 其成功之处在于它由三个相互独立、各司其职的组件构成: 
+ *封装 (Encapsulation)* 
  - 功能: 定义如何将网络层的数据包 (如IP数据报) 封装成PPP帧, 以便在链路上传输。这是PPP协议的数据传输基础。
  - 特点: PPP设计得非常灵活, 它支持多种网络层协议 (如IP、IPX等) , 并且既能在异步链路 (如电话拨号, 逐个字符传输) 上运行, 也能在面向比特的同步链路 (如SDH专线) 上运行。
+ *链路控制协议 (LCP)* 
  - 功能: 负责建立、配置、测试和终止数据链路层的连接。它在数据传输开始前工作。
  - 工作内容: 通信双方通过LCP来协商一些链路参数, 如最大接收单元、是否进行身份验证、以及使用何种协议进行压缩等。可以把它想象成通话前的“握手”和“线路测试”。#index[LCP]
+ *网络控制协议 (NCP) *
  - 功能: 在LCP成功建立链路之后, NCP负责配置不同的网络层协议。每个网络层协议都有一个对应的NCP。
  - 常见NCP: 最常用的是IPCP (Internet Protocol Control Protocol) , 它负责为连接分配临时IP地址等参数。只有当NCP协商成功后, 该网络层的数据才能通过这条PPP链路传输。#index[NCP]
#align(center, table(
  align: center,
  columns: (auto, auto, 1fr),
  [*字段*], [*长度*], [*含义与值*],
  [标志 F], [1字节], [固定为0x7E (二进制01111110) , 用于标识一个帧的开始和结束。],
  [地址 A], [1字节], [固定为0xFF。在点对点链路上, 地址意义不大, 保留此字段是为了兼容性。],
  [控制 C], [1字节], [固定为0x03。表示这是无编号帧 (采用无连接传输方式) , 简化了控制。],
  [协议], [2字节], [关键字段！用于说明信息字段中封装的是什么类型的数据。例如: 0x0021表示信息字段是IP数据报；0xC021表示是LCP分组；0x8021表示是NCP分组。],
  [信息], [可变], [承载要传输的数据, 如IP数据包。默认最大长度不超过1500字节 (受MTU限制) 。],
  [FCS], [2字节], [帧检验序列, 使用CRC校验码, 用于检测帧在传输中是否出错。],
  [标志 F], [1字节], [同起始标志, 0x7E, 表示帧结束。]
))

PPP的*透明传输技术*是PPP协议的技术核心。由于标志字段 F是固定的 0x7E (二进制 01111110) , 如果信息字段中恰好出现相同的比特组合, 接收方就会误以为是帧的边界, 导致帧定界错误。PPP采用了两种技术来解决这个问题, 实现透明传输 (即允许信息字段包含任何比特组合) 。

+ *字节填充*
  - 适用场景: 当PPP用于异步传输 (如拨号Modem) 时, 以字符为单位传输。
  - 原理: 定义一个转义字符 0x7D。
  - 规则: 
    - 发送端:
      - 信息字段中的每个 0x7E字节 -> 转换为 0x7D, 0x5E。
      - 信息字段中的每个 0x7D字节 -> 转换为 0x7D, 0x5D。
      - 信息字段中出现的ASCII控制字符 (值小于 0x20)  -> 在前面插入 0x7D, 并改变该字符的编码。
    - 接收端: 进行反向操作, 删除转义字符, 恢复原始数据。
+ *零比特填充*
  - 适用场景: 当PPP用于同步传输 (如SDH光纤链路) 时, 以比特流为单位连续传输。这是更高效、更常用的方法。
  - 发送端在帧的开始和结束标志之间, 只要数据中连续出现5个“1”, 发送方就自动插入一个“0”。
  - 接收端在接收数据时, 连续收到5个“1”后, 就删除其后的一个“0”, 从而恢复原始数据。
  - 零比特填充完美地保证了标志序列 01111110在帧的数据部分永远不会出现。

#remark[
  PPP提供差错检测功能, 但不提供纠错功能。PPP两端的网络层可以运行不同的网络层协议。PPP可用于拨号链接, 支持动态分配IP地址。PPP双方建立LCP链路后, 接着可以进入身份鉴别状态。PPP协议是一种面向连接的点对点数据链路层协议, 但它是不可靠的, 因为它不使用序号和确认机制, 因此PPP协议提供的是有连接的不可靠服务。PPP协议只支持全双工通信。PPP协议没有冲突检测机制 (如CSMA/CD), 而半双工通信需要冲突检测机制来避免数据碰撞。
]
