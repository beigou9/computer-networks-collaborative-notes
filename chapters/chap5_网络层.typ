#import "../note.typ": *


= 网络层

== 网络层服务

网络层实现数据从源端系统到目的端系统的传输，即使它们之间没有直接的物理连接，需要经过多个路由器（即“多跳”）。网络层的功能不仅存在于通信的起点和终点（主机），更关键的是存在于路径上的每一个路由器中。这是实现多跳传输的基础。

工作流程：
+ *发送端主机*​：从传输层接收数据段，为其添加网络层首部信息（如源IP地址、目的IP地址），封装成数据包。
+ *路由器*​：接收数据包，检查其首部信息，根据内部的计算和存储的信息，决定从哪个出口将数据包转发出去。这个过程在每个途经的路由器上重复。
+ *​接收端主机*​：接收数据包，解析（解封装）后，取出其中的传输层数据段，将其上交给自己本地的传输层。

网络层的两个关键功能可以清晰地划分为两个层面：
+ *路由 - 控制面*
  - 职责​：决策。
  - 工作内容​：通过路由算法​（如OSPF, BGP）计算并选择数据包从源到目的地的最佳路径。这些算法会收集整个网络的拓扑信息、链路状态等，生成路由表。这是一个相对“慢”的过程，通常在网络拓扑变化或定期更新时发生。
+ *转发 - 数据面*
  - 职责​：执行。
  - ​工作内容​：当数据包到达路由器的某个输入接口时，路由器查看其首部中的目的地址，并立即查询本地的转发表，快速决定应该从哪个输出接口将数据包发送出去。这是一个非常“快”的、针对单个数据包的操作。

#remark[
  路由是“规划路径”，生成转发表；转发是“按图索骥”，使用转发表。

  转发表是路由过程的本地化、优化结果。路由算法决定了“去往网络X的包，下一跳应该发给路由器Y”，这个信息被精简后填入每个路由器的转发表中。
]

#note[
  在路由器互连的多个局域网的结构中，要求每个局域网*物理层*、*数据链路层*、*网络层*协议可以不同，而*网络层以上的高层协议*必须相同。使用特定的路由器连接IPV4与IP6网络，就是典型的网络层协议不同而实现互连的例子。
]

#remark[
  网络的异构性是指*传输介质*、*数据编码方式*、*链路控制协议*及*不同的数据单元格式和转发机制*，这些特点分别在物理层和数据链路层协议中定义。
]

两个根本性的设计抉择，这决定了网络层的服务模型：
- *问题一：可靠交付由谁负责？*
  - *​​“网络”负责*​：意味着网络层自身需要实现可靠传输（如确认、重传、排序）。这种方式使主机端的传输层设计变得简单，但大大增加了网络核心（路由器）的复杂性和成本。​传统电话网是这种思路。
  - ​*​“端系统”负责*​：网络层只提供“尽力而为”的服务，不保证可靠交付。将复杂性和可靠性保障的责任交给通信两端的主机（由其传输层协议，如TCP，来负责）。这正是当今互联网的设计哲学，使得网络核心简单、高效、易于扩展。
- *问题二：应该提供何种服务模式？​*
  - *面向连接 - 虚电路*​：服务质量易于保障，资源可预留。灵活性差，健壮性差（路径中断则连接中断）。
    - *建立连接时的工作*​：在发送任何数据之前，会有一个“连接建立”的过程（比如从A到B）。这个探路包会使用目的地址来规划整条路径（A->R1->R4->R5->B）。路径上的每个路由器都会为这个连接分配一个本地的“虚电路号”，并记录上下级关系。
    - *数据传输时的工作*​：一旦连接建立，所有后续数据包都像被放进了一条“管道”。它们只需要携带入口的标签，每个路由器就根据事先建立好的映射关系进行“标签交换”，然后转发出去。这时，包内的目的地址就“失效”了，不再被路由器关心。
    #remark[
      虽然虚电路网络中的数据分组确实包含目的地址，但路由器在转发时不依赖它。这看似多余，实则是为了追求极致的转发效率和资源预留。
    ]
  - *无连接 - 数据报*：健壮性强（路径故障可绕行），简单灵活。端系统需要处理乱序、丢失等问题。
#remark[
  互联网的设计者选择了 ​​*“端系统负责可靠性” + “无连接的数据报服务”*​​ 的组合。即网络层（由IP协议实现）提供简单、不可靠、无连接的“尽力而为”数据报交付服务，而将复杂的可靠性、流量控制、拥塞控制等功能放在端系统的传输层​（由TCP协议实现）来完成。这种“边缘复杂、核心简单”的设计是互联网取得成功的关键之一。
]

#note[
  无连接的方法允许分组有选择不同路径的可能性，但这样可能会导致接收数据的失序；
  需要说明的是，为避免增加额外的开销进行数据排序，网络并不会完全随意地发送数据，
  在大多数情况下，仍然是会尽量沿着某一条路径发送。
  
]

#note[
  面向连接的方法也不一定能完全保证数据的可靠传输，链路中的任何一个组成环节仍有可能失效，而这种失效是严重的，可能导致所有数据丢失
]
虚电路工作流程如下：
+ 数据传输前，主机A与主机B先建立连接，主机A发出“呼叫请求”分组，该分组通过中间结点送往主机B，若主机B同意连接，则发送“呼叫应答”分组予以确认。
+ 虚电路建立后，主机A和主机B就可相互传送数据分组。
+ 传送结束后，主机A通过*发送“释放请求”分组*来拆除虚电路，逐段断开整个连接。
#align(center, table(
  columns:3,
  align: center + horizon,
  table.header(
    [*对比内容*], [*虚电路服务*], [*数据报服务*]
  ),
  /* 表格主体数据 */
  [可靠传输的保证], [可靠通信由网络保证], [可靠通信由主机保证],
  [连接的建立], [必须要], [不需要],
  [地址], [每个分组含有一个短的虚电路号], [每个分组需要有源地址和目的地址],
  [状态信息], [建立好的虚电路要占用子网表空间], [子网不存储状态信息],
  [路由选择], [分组必须经过建立好的路由发送], [每个分组独立选择路由],
  [分组顺序], [总是按序到达], [可能乱序],
  [路由器失效], [所有经过失效路由器的虚电路都要终止], [失效结点可能丢失分组],
  [差错处理和流量控制], [网络或用户主机负责], [用户主机负责],
  [拥塞控制], [容易控制], [难控制],
))

#remark[
  网络负责可靠性（虚电路 + 内部可靠机制）​​
​典型例子：X.25 网络​#index[X.25]

*​工作原理*​：在这种古老的包交换网络中，不仅建立了虚电路，而且在每一段链路上（比如从主机到路由器，或从路由器到路由器）都进行了差错检测和重传。网络层协议本身非常复杂，确保数据在网络的每一步都是可靠的。

*​特点*​：网络核心设备（路由器）负担重，设计复杂，但端主机（用户）的通信软件可以很简单。这体现了传统电信网的设计哲学。

虚电路服务不一定是可靠服务。​​ 它是否可靠，取决于在其之上是否实现了额外的可靠机制（如链路层或传输层的确认重传）。
- 在X.25中，虚电路服务是可靠服务。
- 在ATM/帧中继承载IP的情况下，虚电路服务不是可靠服务，它只是*为TCP提供了一个有序的、高性能的传输通道*，真正的可靠性由TCP来最终保障。
]

虚电路服务之所以能较好地应对突发流量，是因为它的“面向连接”特性提供了管理和控制的抓手。在虚电路建立阶段，网络可以检查自身的资源情况（如链路带宽、路由器缓存）。如果资源充足，则建立连接并可能为其预留资源；如果网络已经接近满负荷，​它可以直接拒绝建立新的虚电路。这种“准入控制”机制从源头就防止了网络因连接数过多而被突发流量冲垮。

数据报服务的“无连接”和“无状态”特性使其在面对突发流量时非常被动。每个数据包都是“不速之客”，随机到达路由器。网络无法提前知道会有多少数据包涌来，因此无法进行预防性的资源预留或连接拒绝。所有数据包在每一个路由器端口平等地竞争缓冲区空间和输出链路。当突发流量到来时，路由器的缓存会迅速被填满，导致后续到达的数据包被大量丢弃，从而引发TCP全局同步、重传风暴等问题，使拥塞加剧。

#remark[
  对于出错率高的传输系统，易出现结点故障，采用*数据报方式*更合适。
]

#example[
假设总带宽100Mbps，有2个数据源共享带宽如果每个数据源都是突发流量，且最高可达70Mbps，使用数据报服务/ 虚电路服务，结果如何？

*虚电路服务的结果*:
- 当第一个数据源请求建立一条峰值70Mbps的虚电路时，网络会接受，因为它看到总带宽100Mbps > 70Mbps。
- 当第二个数据源也请求建立一条峰值70Mbps的虚电路时，网络会发现总需求（70+70=140Mbps）远超总带宽（100Mbps）。根据规则，网络会直接拒绝第二个连接建立。

第一个数据源可以正常通信（享有70Mbps甚至更多带宽），而第二个数据源根本无法开始传输。这体现了虚电路“容易控制”的优点，但导致了严重的不公平。

*数据报服务的结果*:
初期会经历短暂的拥塞和丢包，但依靠端到端的TCP拥塞控制，系统能自动、动态地调整到一种公平的稳定状态。虽然性能达不到峰值，但保证了服务的韧性和公平性。
]

== 路由算法
路由算法需要满足的特性：正确性、简单性、魯棒性、稳定性、公平性、有效性。

根据路由算法是否随网络的通信量或拓扑自适应划分分为静态路由选择策略（非自适应路由选择）和动态路由选择策略（自适应路由选择）。

=== 汇集树
汇集树(Sink Tree）：所有的源节点到一个指定目标节点的最优路径的集合构成一棵以目标节点为根的树。#index(initial: "H")[汇集树]

#remark[
  在绝大多数情况下，所有节点到一个指定目标节点的最短路径集合确实能构成一棵树（即汇集树），但这需要满足一个重要的前提条件：网络中不能存在“负权环”。这其实是这个“最短路径”拥有最优子结构的性质使然。
]

#remark[
  汇集树不是唯一的.
]

=== 距离向量路由
距离向量路由算法，每个路由器只维护一个“距离矢量”，即一个表格，记录到每个已知目的网络的最佳距离（如跳数、延迟）和下一跳路由器（即矢量）。路由器定期地与其直接相邻的邻居交换自己的整个路由表（即它的距离矢量）。#index(initial: "J")[距离路由向量]

#note[
  路由表中存储的是 IP 地址（更准确地说是目标网络地址/网段），不存储 MAC 地址。
  - *IP 地址* 是网络层的逻辑地址，用于标识“网络”和“主机”，具有层级结构（网络号+主机号），适合跨网段寻址。
  - *MAC 地址* 是数据链路层（Layer 2） 的物理地址，用于在同一个局域网（广播域） 内部区分设备。
  - 路由器的核心功能是跨网络转发数据，必须依据逻辑地址（IP）来判断数据包应该去往哪个网络。

  虽然路由表里没有 MAC 地址，但路由器转发数据包必须要*有 MAC 地址才能封装数据帧*。MAC 地址存储*在 ARP 表（ARP Table）* 或 邻居表 中。

  路由器转发流程：
  + *查路由表*：根据 目的 IP 找到 下一跳 IP (Next Hop IP) 和 出接口。
  + *查 ARP 表*：根据 下一跳 IP 查找对应的 下一跳 MAC 地址。
  + *封装转发*：将数据包封装成帧（Frame），填入查到的 MAC 地址，发送出去。
]

当路由器从邻居那里收到新的路由信息后，它会使用Bellman-Ford方程来更新自己的路由表。该方程的核心思想是：如果邻居声称它到某个目的地Y有距离D，而我自己到这个邻居的距离是C，那么我通过这个邻居到达Y的距离就是 C + D。我需要比较所有邻居提供的这条路径代价，并选择最小的一个。用公式表达为：
$
  D_x (y)=min{c(x,v)+D_v (y)}
$

其中，$D_x (y)$ 表示节点$x$到节点$y$的最小代价，$c(x, v)$是$x$到其邻居$v$的直接代价，$D_v (y)$是邻居$v$声称的它到$y$的代价.

#remark[
  距离向量算法特点：迭代的、分布式的
  - 每次本地迭代由下列引起: 本地链路费用改变、邻居更新报文
  - 分布式:各节点依次计算，相互依赖

  基于传闻的路由，对故障的反应迟缓且低效。
]

距离向量算法的核心是每个路由器都相信它的邻居告诉它的信息。在网络稳定时，这很有效。但当一条直接连接的链路失效时，问题就出现了。*“计数到无穷”就是指路由器无法快速意识到某个网络已经不可达，而只能通过一次次地与邻居交换路由信息，将到该网络的度量（跳数）缓慢地、一步一步地增加到一个被认为是“无穷大”（表示不可达）的数值（如RIP协议中的16）。*这个过程就像从1慢慢数到无穷大一样，因此得名。

*“好消息传得快，坏消息传得慢”*
- 好消息（比如一个新网络上线或一条链路恢复）：它的传播是“加法”。一旦有一条更优的路径出现，它会通过邻居间的比较和替换快速扩散到整个网络。
- 坏消息（比如链路故障）：它的传播是“减法”，而且触发了我们上面描述的“计数到无穷”机制。路由器无法区分“一个邻居告诉我它有一条到X的新路”和“这个邻居本身需要经过我才能到X”。它缺乏全局视角，导致需要依赖一个可能已经失效的信息来源，从而产生环路并缓慢收敛。

#remark[
为了解决这个问题，人们设计了一些增强机制：
+ *水平分割*：一条基本规则是：不要将从一个邻居学来的路由信息再发回给那个邻居。这从根本上避免了环路的产生。
+ *毒性逆转*：比水平分割更主动。R2会明确地告诉R1：“我到Net1的距离是无穷大（中毒了）”，即使它实际上有一条路由。这相当于一个明确的否定信号。
+ *触发更新*：一旦检测到链路失效，立即发送更新报文，而不等待定期更新的时间点，以加速坏消息的传播。
]

=== 链路状态路由
*链路状态（Link State）路由*可分为五个部分：
1. 发现邻居，了解他们的网络地址；
2. 设置到每个邻居的成本度量；
3. 构造一个分组，分组中包含刚收到的所有信息；
4. 将此分组发送给其他的路由器；
  - 每个LSP分组包含一个序列号，且递增
  - 路由器记录所收到的所有（源路由器、序列号）对
  - 当一个新分组到达时，路由器根据记录判断：如果是新分组，洪泛广播；如果是重复分组，丢弃；如果是过时分组，拒绝。
  
5. 计算到其他路由器的最短路径。
#remark[
  在链路状态路由协议中，“过时分组”指的是一个由于序列号小于接收路由器当前记录的、来自同一源路由器的最新序列号，而被认为是旧数据的链路状态分组（LSP）。路由器依靠这个机制来确保网络中传播的是最新的网络拓扑信息，而不是陈旧的历史数据。
]
#table(

  columns: (0.8fr, 1.2fr, 2fr),
  align: (center, center, left),
  inset: 8pt,
  
  // 表头
  [概念], [英文全称与中文], [角色与功能],
  
  // LSA 行
  [LSA], [
    Link-State Advertisement \
    链路状态通告
  ], [
    描述路由器自身链路状态的基本信息单元。
    好比一张"告示"，说明"我是谁，我和谁相连，成本多少"。
  ],
  
  // LSDB 行  
  [LSDB], [
    Link-State Database \
    链路状态数据库
  ], [
    存储由所有LSA构成的网络完整拓扑信息的数据库。
    所有路由器的LSDB最终完全一致。
  ],
  
  // LSP 行
  [LSP/PDU], [
    Link State Packet/PDU \
    链路状态分组/协议数据单元
  ], [
    在IS-IS协议中，承载和传输LSA的"信封"或"集装箱"。
  ]
)#index[LSA]#index[LSDB]#index[LSP]#index[PD]

年龄字段的具体运作机制可以概括为以下几个核心步骤：

+ *设置初始年龄*:
  当一台路由器生成一个新的LSP时，会为其中的“年龄”字段设置一个初始值。在不同的协议中，这个值有所不同。例如，在OSPF协议中，初始年龄通常设置为 0，表示它刚刚“出生”；而在IS-IS协议中，则设置一个最大值（如120秒），然后开始递减。
+ *年龄的递减与泛洪*:
  生成的LSP会被泛洪到整个网络。关键点在于，LSP在传播过程中，其“年龄”会随着时间推移而逐渐减少（比如每秒减1）。每台收到LSP的路由器都会先暂存它，然后在转发给其他邻居之前，将其“年龄”减小。
+ *达到“保质期”的处理*:
  当LSP的“年龄”减少到 0时，就意味着它的“保质期”已到。此时，持有该LSP的路由器会执行一个关键操作：如果这台路由器就是生成该LSP的源路由器，它必须立即生成一个全新的、序列号更新的LSP来替换这个过期的旧LSP，并重新泛洪。如果不是源路由器，则会将这个年龄为0的LSP从自己的链路状态数据库中清除。

- *防止“僵尸”LSP*:
  想象一下，如果生成某LSP的路由器突然断电下线，它之前发出的LSP就会成为无人更新的“僵尸”信息。如果没有年龄机制，这些陈旧的拓扑信息会一直留在其他路由器的数据库中，导致路由计算错误。年龄机制确保了这些“僵尸”LSP会被自动清理。
- *定期刷新，保持信息新鲜*:
  即使网络稳定，年龄机制也迫使源路由器必须定期（在年龄到期前）生成新的LSP。这种周期性的刷新行为，相当于一种健康检查，向全网宣告：“我还在，我的连接状态没有变化。” 这保证了所有路由器使用的都是一份“新鲜”的网络地图。
- *处理序列号循环*:
  序列号虽然很大，但理论上存在耗尽并重新开始（循环）的可能。年龄机制可以确保非常陈旧的、序列号已经循环过一圈的LSP被及时清理掉，从而避免新的LSP（但序列号值小）被误判为“过时分组”。

#remark[
  序列号用于判断新旧，年龄用于判断有效性。
]

=== 层次路由
层次路由是构建现代大规模网络（尤其是互联网）的核心架构思想，它通过“分而治之”的策略，将复杂的路由问题分解为多个更易管理的层次，完美解决了超大规模网络带来的挑战。

它的核心思想是：不再将整个网络视为一张巨大的、扁平化的拓扑图，而是将其划分为多个称为自治系统（Autonomous System, AS）的区域。每个AS通常由一个独立的机构（如一家运营商、一所大学或一家大公司）管理，内部运行统一的路由策略。基于这种划分，路由任务被清晰地分为两类：#index[AS]
- *AS内部路由*：也称为域内路由。在一个AS内部，所有路由器运行相同的域内路由协议，如RIP或OSPF。它们负责快速、高效地计算AS内部的最佳路径。
- *AS间路由*：也称为域间路由。当数据需要跨越不同的AS时，由运行在AS边界上的网关路由器使用域间路由协议（主要是BGP）来进行路由决策和信息交换。

常见的域内路由协议主要有两种：*距离向量协议*（例如RIP协议），*链路状态协议*（例如（OSPF协议）。

BGP是互联网AS间路由的事实标准协议，它的设计哲学与域内协议有根本不同。#index[BGP]
- *路径向量协议*：BGP通告的是一条到达目标网络所需要经过的AS序列（AS-PATH）。这个序列本身就能有效防止环路（如果看到自己的AS号在路径中，则拒绝该路由）
- *策略驱动*：BGP的路由选择高度由策略决定。其决策过程通常遵循以下准则
  - 本地偏好：管理员手动设置的最高优先级策略。
  - 最短AS-PATH：选择经过AS数量最少的路径。
  - 最近NEXT-HOP：即“热土豆路由” - 尽快将数据包送出本AS，而不管后续AS内部的代价。

=== 广播路由
广播路由是网络层一种一对所有的通信方式，它就像一个扩音喇叭，一次喊话能让整个区域（网络或子网）里的所有设备都听到。

#table(
  columns: (3.5cm, 1fr, 1.2fr),
  align: (center + horizon, left + horizon, left + horizon),
  table.header(
    [*实现方法*],
    [*工作原理*],
    [*优点与缺点*],
  ),
  // 1. 给每个主机单独发送数据包
  
  [给每个主机单独发送数据包 (Independent Unicasts)],
  [
    源主机为网络中每个目标主机生成一个单独的*单播*数据包，并逐一发送。每个数据包都独立路由。
  ],
  [
    *优点*: 实现简单，不需要特殊的路由协议。\
    *缺点*: 效率极低。源主机负担重，网络中传输大量的重复数据包 $N-1$ 个，浪费带宽。
  ],
  
  [多目标路由 (Multidestination Routing)],
  [
    数据包携带一个包含所有目标地址的列表。路由器接收后，根据地址列表复制数据包，并根据路由表将列表拆分，分别转发。
  ],
  [
    *优点*: 减轻了源主机发送 $N-1$ 次数据的负担。\
    *缺点*: 路由器处理逻辑复杂，需要维护和操作地址列表，效率不如真正的广播/组播机制。
  ],
  
  [泛洪 (Flooding)],
  [
    路由器收到广播数据包后，会从*除接收接口外*的所有接口转发出去。
  ],
  [
    *优点*: 简单粗暴，一定能到达所有节点。\
    *缺点*: 资源浪费严重，产生大量的重复数据包，极易引发广播风暴和网络拥塞，可能导致数据包无限循环。
  ],
  [有控制的泛洪 (Controlled Flooding)],
  [
    在基本泛洪的基础上加入控制机制，以防止循环和重复。
     *逆向路径转发 (RPF)*: 路由器只转发从到达*源地址*的最佳路径接口收到的数据包。
     *序列号*: 使用数据包序列号，路由器只转发以前未见过的序列号。#index[RPF]
  ],
  [
    *优点*: 避免了泛洪的循环和重复问题，比原始泛洪效率高得多。\
    *缺点*: RPF 依赖于正确的单播路由表，且数据包仍可能在没有树结构的情况下重复。序列号机制开销较大。
  ],
  [生成树 (Spanning Tree)],
  [
    在网络拓扑中计算出一棵连接所有节点的*无环路*最小子集树结构。广播数据包仅沿着这棵树的边进行转发。
  ],
  [
    *优点*: 资源利用率最高。每个网络链路只传输一次广播数据包，避免了所有循环和重复。\
    *缺点*: 需要预先计算和维护生成树，需要复杂的分布式算法（如 STP）。
  ]
)
== 网络层流量管理

=== 流量管理的必要性
- *拥塞*: 是指到达通信子网中某一部分的*分组数量过多*，使得该部分网络来不及处理，以致*这部分乃至整个网络性能下降*的现象，严重时甚至导致网络通信业务陷入停顿，即出现死锁现象。（*吞吐率下降*）
- *拥塞控制*（congestion control）: 需要确保通信子网能够承载用户提交的通信量，是一个全局性问题，涉及主机、路由器等多种因素。
- 产生拥塞的原因:
  - 主机发送到网络的数据包数量过多，超过了网络的承载能力
  - 突发的流量填满了路由器的缓冲区，造成某些数据包会被丢弃
- 拥塞控制的基本策略
  - *开环控制*：事先对通信流参数进行协商，如果协商失败则拒绝连接，协商后，不管网络是拥塞还是带宽充足，参数不能动态改变。开环控制属于预防性拥塞控制，它竭力使网络总是处于无拥塞状态运行。开环控制的方法包括决定什么时候接受新流量，什么时候丢弃数据包和丢弃哪些数据包。其缺点是没有考虑网络的当前状态。
  - *闭环控制*：根据网络状态进行动态控制，包括两部分：反馈机制和控制机制。闭环控制方法分为两个子类：显式反馈与隐式反馈。TCP协议就是典型的闭环控制。
#remark[
  这里面的*“环（Loop）”*指的是反馈回路。也就是是否会因为结果来调整状态
]
#table(
  columns: (1fr, 1fr, 2fr, 2fr),
  inset: 8pt,
  align: horizon,
  fill: (col, row) => if row == 0 { gray.lighten(80%) } else { none },
  
  [*层级*], [*英文名称*], [*核心行为*], [*适用场景*],

  [1. 提高网络供给], [Network provisioning], [*“修路”*：购买带宽、升级路由器、增加链路。], [长期规划，当现有网络经常性过载时使用。],
  
  [2. 流量感知路由], [Traffic-aware routing], [*“绕路”*：根据负载情况动态调整路由表，将流量引导至空闲链路。], [中期调整，利用网络拓扑中的冗余路径分流。],
  
  [3. 准入控制], [Admission control], [*“限流”*：当负载过高时，拒绝建立新的连接（虚电路）。], [连接建立阶段，防止网络进一步恶化。],
  
  [4. 流量调节], [Traffic throttling], [*“减速”*：发送拥塞信号（如ECN、抑制包），强制发送方降低发送速率。], [拥塞刚发生时，通过反馈机制降低负载。],
  
  [5. 负载丢弃], [Load shedding], [*“扔货”*：当路由器缓存溢出时，直接丢弃数据包。], [拥塞极严重时的最后手段，牺牲数据换取网络可用性。]
)

=== 负载脱落

负载丢弃会进行选择性丢弃，给包标上优先级或类型，让节点在决定丢弃包时可作出有利的选择。

=== 流量整形

*流量整形(traffic shaping)*：其作用是限制流出某一网络的某一连接的流量与突发，使这类报文以比较均匀的速度向外发送。流量整形算法包括*漏桶算法*和*令牌桶算法*：#index(initial: "L")[漏桶算法]#index(initial: "L")[令牌桶算法]
- *漏桶算法（Leaky Bucket Algorithm）*：主要目的是控制数据注入到网络的速率，平滑网络上的突发流量。漏桶算法提供了一种控制机制，通过它，突发流量可以被整形以便为网络提供一个稳定的流量。
- *令牌桶算法（Token Bucket Algorithm）*：用来控制发送到网络上的数据的数目，并允许突发数据的发送。

漏桶算法在逻辑上由以下两个核心组件构成：
- *有限容量的缓冲区（Queue/Buffer）*：这是一个先进先出（FIFO）的队列。设其最大容量为 $B$（通常以数据包数量或字节数为单位）。该缓冲区用于暂存瞬间到达的突发流量。
- *恒定速率的处理器（Constant Rate Server）*：设设定的处理速率（流出速率）为 $R$。无论队列中有多少数据，处理器严格按照固定的时间间隔从队列头部取出数据并发送。

算法主要包含两个并发过程：数据包到达（入列）与数据包发送（出列）。

- *输入处理（Traffic Arrival）*：当一个数据包到达网络接口时，算法执行以下判断逻辑：
  + 检查队列状态：检测当前缓冲区已占用的空间 $Q_("current")$。
  + 接纳与丢弃：如果 $Q_("current") < B$（队列未满）：数据包被放入队列尾部等待处理。否则触发*尾部丢弃（Tail Drop）*策略，该数据包被直接丢弃（或者标记为非优先，视具体QoS策略而定）。
- *输出处理（Traffic Departure）*：处理器以固定的时钟周期工作，其逻辑如下：
  + 恒定调度：系统以速率 $R$ 周期性地检查队列。
  + 发送操作：如果队列不为空：从队列头部取出一个数据包（或固定大小的数据块）发送到网络上。否则处理器处于空闲状态，不发送任何数据（此时输出速率实际上为 0，但一旦有数据，速率立即恢复为 $R$）。

*令牌桶算法（Token Bucket）*是网络流量整形（Traffic Shaping）和速率限制（Rate Limiting）中最常用的算法。与漏桶算法不同，令牌桶允许一定程度的突发流量（Burstiness）。

令牌桶算法主要由以下三个核心要素构成：
- *令牌桶（Token Bucket）*：一个虚拟容器，用来存放“令牌”、容量固定为 $B$（Burst Size），单位通常是字节（Bytes）或比特（Bits）、桶满时，新产生的令牌会被丢弃。
- *令牌生成器（Token Generator）*：以恒定的速率 $R$（Rate）向桶中添加令牌。
- *流量控制器（Traffic Controller）*：负责处理到达的数据包，根据桶内令牌数量决定数据包的行为。

该算法包含两个独立的动作：*令牌投放*与*数据包消耗*。
- *令牌投放（Token Injection）*，这是一个持续进行的后台过程：
  + 系统以固定的时间间隔（$1/R$）向桶内放入令牌。
  + 容量限制检查：如果当前令牌数 $< B$：令牌数加 1（或增加对应的时间量）。否则判断桶已满，新产生的令牌直接丢弃（溢出）。
- *数据包消耗（Packet Consumption）*，当一个大小为 $S$ 字节的数据包到达时：
  + 检查令牌余额：查看桶内当前的令牌数量 $T_("current")$。
  + 处理逻辑：如果 $T_"current" gt S$（令牌足够）则消耗$S$个令牌，并更新桶内令牌数$T_"current"=T_"current"-S$，放行；否则，令牌不足，会直接丢弃(常见于流量监管)或者进入排队队列缓冲区等待，直到桶内积累了足够的令牌（常见于流量整形）

=== 抑制数据包
用于通知发送方减小发送量，路由器选择一个被拥塞的数据包，给该数据包的源主机返回一个抑制包，抑制包中的目的地址取自该拥塞数据包。源主机收到抑制包后，减少发向特定目的地址的流量。

但是这个方法会有：*效率问题*，阻塞包可能误伤（不同源、不同流）；*有效性问题*，阻塞包可能不起作用；*公平性问题*：可能只抑制低速发送者。

=== 逐跳后压（避免式）
*逐跳的抑制包(Hop-by-Hop Choke Packets)*：在高速或长距离网络中，由于源主机响应太慢，抑制包算法对拥塞控制的效果并不好，可采用逐跳抑制方法；其核心思想是抑制包对它经过的每个路由器都起作用，能够迅速缓解发生拥塞处的拥塞，但要求上游路由器有更大的缓冲区。


=== 显式拥塞通知（避免式）
*显式拥塞通告（ECN，Explicit Congestion Notification）*#index[ECN]，在IP包头中记录数据包是否经历了拥塞。在数据包转发过程中，路由器可以在包头中标记为经历拥塞，然后接收方在它的下一个应答数据包里回显该标记作为显式拥塞信号。RFC2474中重新定义TOS域为包含一个6位的区分服务码点(DSCP)和2位未使用位；RFC3168重新定义RFC2474中TOS域未使用的两位为ECN域，包含如下值：
- 00：发送主机不支持ECN
- 01或者10：发送主机支持ECN
- 11：路由器正在经历拥塞

== Internet网际协议

=== IPv4协议
IPv4协议，网际协议版本4，一种无连接的协议，是互联网的核心，也是使用最广泛的网际协议版本，其后继版本为IPv6。#index[IPv4]

Internet协议执行两个基本功能：寻址和分片。

#figure(image("/fig/IPv4数据报格式.png", width: 60%), caption: "IPv4数据报格式")
#table(
  columns: (auto, auto, 1fr),
  inset: 10pt,
  align: (col, row) => (
    if col == 2 { left + horizon }
    else { center + horizon }
  ),
  table.header(
    [*字段名称*], [*长度*], [*功能描述*]
  ),
  [版本], [4 bit], [表示采用的IP协议版本],
  [首部长度], [4 bit], [表示整个IP数据报首部的长度, 单位：4B],
  [区分服务], [8 bit], [该字段一般情况下不使用],
  [总长度], [16 bit], [表示整个IP报文的长度，能表示的最大字节为 $2^16-1=65535$ 字节, 单位：1B],
  [标识], [16 bit], [IP软件通过计数器自动产生，每产生1个数据报计数器加1；在IP分片以后，用来标识同一片分片。属于同一原始IP分组的分片具有相同的标识。],
  [标志], [3 bit], [目前只有两位有意义；MF（置1表示后面还有分片，置0表示这是数据报片的最后1个#index[MF]）；DF（置0时表示允许分片#index[DF]）],
  [片偏移], [13 bit], [表示IP分片后，相应的IP片在总的IP片的相对位置, 单位: 8B],
  [生存时间TTL], [8 bit], [表示数据报在网络中的生命周期，用通过路由器的数量来计量，即跳数（每经过一个路由器会减1）,用于避免无限转发环路],
  [协议], [8 bit], [标识上层协议（TCP/UDP/ICMP...）],
  [首部校验和], [16 bit], [对数据报首部进行校验，不包括数据部分],
  [源地址], [32 bit], [标识IP片的发送源IP地址],
  [目的地址], [32 bit], [标识IP片的目的地IP地址],
  [选项], [可变], [定义了安全性、严格源路由、松散源路由、记录路由、时间戳等选项],
  [填充], [可变], [用全0的填充字段补齐为4字节的整数倍]
)

限制（MTU）：每个网络链路都有最大传输单元（MTU）。对应的解决策略为：#index[MTU]
- *切碎发送*：允许路由器在遇到窄链路时把大数据包切开（允许途中分片）。
- *提前探测*：发送端提前探测整条路上最小的MTU，直接发小包，避免在路上被切（路径MTU发现）。
- *终点组装*：切碎的包在到达目的地前不合并，直到终点才统一还原。这是互联网的标准做法，为了减轻路由器负担。
- *还原依据*：靠首部里的“标识”（属于哪个原包）、“片偏移”（在原包的哪个位置）和“标志”（是否还有后续分片）来拼接。

IPv4分组在传输途中可以多次分片，IPv4分片只在目的IP对应的目的端系统进行重组。

#remark[
  若分组长度超过MTU，则当DF=1时，丢弃该分组，并且要用ICMP（终点不可达报文）向源主机报告差错。
]

=== IP 地址
IP地址，网络上的每一台主机（或路由器）的每一个接口都会分配一个全球唯一的32位的标识符。网络号相同的这块连续IP地址空间称为地址的前缀，或网络前缀。将IP地址划分为固定的类，每一类都由两个字段组成。IP地址的书写采用点分十进制记法，其中每一段取值范围为0到255。#index[IP]

#table(
  columns: (auto, auto, auto, auto, 1fr),
  inset: 8pt,
  align: center + horizon,
  table.header(
    [*类别*], [*二进制前缀*], [*第1字节范围*], [*结构 (Net:Host)*], [*主要用途*]
  ),
  [A 类], [$0dots$], [$1 tilde 126$], [8 : 24], [大型网络 (单播)],
  [B 类], [$10dots$], [$128 tilde 191$], [16 : 16], [中型网络 (单播)],
  [C 类], [$110dots$], [$192 tilde 223$], [24 : 8], [小型网络 (单播)],
  [D 类], [$1110dots$], [$224 tilde 239$], [-], [多播 (组播)],
  [E 类], [$1111dots$], [$240 tilde 255$], [-], [保留 / 实验]
)

#table(
  columns: (auto, 1fr),
  inset: 10pt,
  align: (col, row) => (
    if col == 1 { left + horizon }
    else { center + horizon }
  ),
  table.header(
    [*地址*], [*用途*]
  ),
  [全0网络地址], [只在系统启动时有效，用于启动时临时通信，又叫主机地址],
  [网络127.0.0.0], [指本地节点(一般为127.0.0.1)，也称为本地回路地址，用于测试网卡及TCP/IP软件，这样浪费了1700万个地址],
  [全0主机地址], [用于指定网络本身，称之为网络地址或者网络号],
  [全1主机地址], [用于广播，也称定向广播，需要指定目标网络],
  [0.0.0.0], [指任意地址],
  [255.255.255.255], [用于本地广播，也称有限/受限广播，无须知道本地网络地址]
)

如果不划分，一个 B 类网络有 6 万多个主机号，全在一个局域网里，一旦广播，网络直接瘫痪（广播风暴）。 目的：把一个大网络切成几个小网络，隔离广播域，提高安全性，节约 IP 地址。

假设原本主机位为 $H$ 位，现在借了 $n$ 位去做子网，剩下 $m$ 位做主机 ($H = n + m$)：子网数量 = $2^n$，每个子网的可用主机数 = $2^m - 2$（全 0 地址：代表本子网的网络地址；全 1 地址：代表本子网的广播地址）
+ *定需求*：需要几个子网？或者每个子网需要几台主机？
+ *算位数*：根据 $2^n gt.eq "子网数"$ 或 $2^m - 2 gt.eq "主机数"$ 算出要借几位。
+ *改掩码*：把借走的位在子网掩码里全部置 1。

在跨网段数据传输过程中，IP地址与MAC地址的变化规律：
- *IP地址（始终不变）*：表示“起点”和“终点”。无论经过多少个路由器，源IP ($"IP"_1$) 和目的IP ($"IP"_2$) 保持不变。
- *MAC地址（跳跳改变）*：表示“这一站”到“下一站”。每经过一个链路（网段），源MAC和目的MAC都会发生改变。
#note[默认路由][
用特殊前缀0.0.0.0/0表示默认路由，全0掩码和任何目的地址进行按位与运算，结果必然为全0，即必然和前缀0.0.0.0/0相匹配。只要目的网络是其他网络（不在转发表中），就一律选择默认路由。默认路由通常用于路由器到互联网的路由，互联网包括无数的网络集合，不可能在路由表项中一一列出，因此只能采用默认路由的方式。
]
#remark[
  网络层（IP）提供*端到端（End-to-End）的逻辑通信，而数据链路层（MAC）负责点到点（Hop-by-Hop）*的物理传输。
]

=== 无类域间路由 CIDR（ Classless Inter-Domain Routing ）
#index[CIDR]

#remark[
  在 CIDR 出现之前（1993年以前），IP 地址严格遵循 A、B、C 类的划分（称为 Classful 寻址）。这导致了两个致命问题：
  - *IP地址浪费严重*：如果想要建立一个有 500 台主机的网络。申请 C 类（254个）不够用, 申请 B 类（65534个）太大了，你会浪费 6 万多个地址。
  - *路由表爆炸*：互联网上的路由器需要记录去往每个网络的路径。如果大家都申请一堆细碎的 C 类地址，核心路由器的表项会多到内存溢出，处理速度变慢。
]

将32位的IP地址划分为前后两个部分，并采用斜线记法，即在IP地址后加上“/”，然后再写上网络前缀所占位数。

它彻底废除了 A、B、C 类的概念。不再通过 IP 地址的前几位来判断网络类别，而是完全依赖 子网掩码（前缀长度） 来决定网络范围。CIDR 引入了我们现在最熟悉的写法：IP地址 / 网络前缀长度。 例如：128.14.35.7/20，/20 表示前 20 位是网络号，剩下的 12 位 ($32-20$) 是主机号。它不再受限于 /8、/16、/24 这种整字节的边界，可以是 /13、/27 等任意数字。

路由聚合是 CIDR 最厉害的地方。它允许将多个连续的较小前缀合并成一个较短的前缀。这被称为“超网”（Supernetting）。#index(initial: "C")[超网]

#example[
  假设某ISP拥有 4 个连续的 C 类网络：220.10.0.0/24, 220.10.1.0/24, 220.10.2.0/24, 220.10.3.0/24. 如果不聚合，互联网上的路由器需要存储 4 条 路由记录。 使用 CIDR，我们观察这 4 个地址的二进制,前 22 位是完全一样的（只有最后两位的 00, 01, 10, 11 在变）。 所以，我们可以对外只通告一条路由： 220.10.0.0/22. 路由表从 4 条变成 1 条，大大减轻了互联网核心路由器的压力。
]

因为 CIDR 允许地址块重叠（大包小），路由器在查找路由表时，必须遵循*最长前缀匹配原则(Longest Prefix Match, LPM)*。#index[LPM]

#example[
  路由器收到了一个发往 10.1.1.1 的数据包，路由表里有两条记录都能匹配上：
  - 10.1.0.0/16 (匹配前 16 位，范围广)
  - 10.1.1.0/24 (匹配前 24 位，范围窄，更精准)

  路由器会选择 掩码最长（/24） 的那一项。掩码越长，网络划分越细，代表的目的地越具体。
]

*对于子网划分类型的问题*

+ 先确定题目给出的原始网段和掩码，算出还剩多少位可以操作。

  公式：$"剩余位数" = 32 - "当前掩码前缀"$

+ 看题目要求的是什么，决定怎么切分这块蛋糕。
  - 情况 A：求“子网数量” (切成几块？)算法：假设需要 $X$ 个子网，求 $n$。从剩余的主机位中借走 $n$ 位给子网位。
    $2^n gt.eq X$
  - 情况 B：求“每个子网的主机数量” (每块多大？)假设每个子网需要 $Y$ 台主机，求 $h$。必须保留 $h$ 位给主机，剩下的才能拿去划分子网。
    $2^h - 2 gt.eq Y$
+ 无论怎么划分，必须遵守：主机位 $gt.eq$ 2：新掩码 = 旧掩码 + 借出的位数。确保新掩码 $lt.eq 30$。
=== IP包转发
在 IP 包的转发过程中，直接交付和间接交付是根据“目的主机是否在当前局域网内”来划分的两种传输方式。

*直接交付*，发送站与目的站处于*同一个物理网络（网段）*中。数据包不需要经过路由器中转，直接发送给目标。源主机（或最后一跳路由器）计算发现：*目的IP & 子网掩码 == 源IP & 子网掩码*（即网络号相同）。

+ 发送端封装 IP 数据报。
+ 发送端查询 ARP 缓存或广播 ARP 请求，获取目的主机的 MAC 地址。
+ 将数据帧直接发送给目的主机。

*间接交付*，发送站与目的站处于不同的物理网络。数据包必须先发送给路由器（默认网关），由路由器转发。源主机计算发现：目的IP & 子网掩码 != 源IP & 子网掩码（网络号不同）。

+ 发送端无法直接获取目的主机的 MAC 地址。
+ 发送端查询路由表，找到默认网关（路由器）的 IP。
+ 发送端使用 ARP 获取网关的 MAC 地址（注意：这里目标 IP 是终点，但目标 MAC 是路由器）。
+ 将数据帧发送给路由器。
+ 路由器收到后，剥去帧头，检查 IP 表，进行下一跳转发。

#remark[
  “间接交付”本质上是一系列“直接交付”的组合。 整个互联网通信过程是：源主机 -> [直接交付] -> 路由器 -> [直接交付] -> 路由器 ... -> [直接交付] -> 目的主机。每一次跳跃（Hop）在链路层看都是一次直接交付（发给相邻设备），但在网络层宏观上看，只有最后一步才是真正的直接交付给终点。
]

=== DHCP

#note[
  DHCP是应用层协议，因为它是通过客户/服务器模式工作的，DHCP客户向DHCP服务器请求服务，而其他层次的协议是没有这两种工作方式的。

  DHCP采用的是UDP, 而不是TCP. 因为不知道ip地址的情况下，是无法进行3次握手的。
]
*DHCP (Dynamic Host Configuration Protocol)，即动态主机配置协议。*#index[DHCP]

DHCP 的作用就是自动化完成IP地址、子网掩码、网关、DNS等配置，可以实现网线的即插即用。

DHCP 的交互过程非常经典，被称为 DORA 流程（取四个步骤的首字母）。#index[DORA]

假设电脑（Client）刚连上 Wi-Fi，网络里有一台 DHCP 服务器（Server）：

+ 发现 (Discover)
  - 动作：客户端（Client）广播发送 DHCP Discover 报文, 寻找DHCP服务器，请求IP地址。
  - 地址：源 IP 是 0.0.0.0（因为自己没 IP），目的 IP 是 255.255.255.255（广播）。
+ 提供 (Offer)
  - 动作：DHCP 服务器收到请求，从自己的地址池里选一个未被占用的 IP（比如 192.168.1.100），单播发送 DHCP Offer 报文。
  - 注意：如果有多个 DHCP 服务器，客户端可能会收到多个 Offer，通常“先到先得”。
+ 请求 (Request)
  - 动作：客户端选中其中一个 Offer，*广播*发送 DHCP Request 报文。
  - 广播是为了告诉其他 DHCP 服务器。
+ 确认 (ACKnowledgment)
  - 动作：被选中的 DHCP 服务器单播发送 DHCP ACK 报文。
  - 客户端收到 ACK 后，正式配置 IP，开始上网。

#remark[
  DHCP通常还会分配：IP地址、子网掩码、默认网关、DNS服务器。
]

DHCP 分配的 IP 不是永久的，而是租给你的。


=== ARP
ARP (Address Resolution Protocol)，中文名为地址解析协议。它的核心作用：将 IP 地址（逻辑地址）解析为 MAC 地址（物理地址）。整体过程是：查表，广播请求，单播响应，更新缓存。ARP 只有效于*同一个局域网（网段）*内。ARP 请求包无法穿过路由器。如果必须通过路由器，那么主机 A 会对*默认网关（路由器）*的 IP 发起 ARP 请求，获取路由器的 MAC 地址，把包交给路由器去转发。#index[ARP]

IP地址是网络层及网络层之上使用的地址，它是*分层式的*。硬件地址（MAC地址）是数据链路层使用的地址，它是*平面式的*。IP地址放在IP数据报的首部，而MAC地址放在MAC帧的首部。把IP数据报封装为MAC帧后，数据链路层看不见IP数据报中的IP地址。因为路由器的隔离，IP网络中无法通过广播MAC地址来完成跨网络的寻址，所以在*网络层只使用IP地址来完成寻址*。寻址时，每个路由器依据其路由表（依靠路由协议生成）选择到目标网络（即主机号全为0的网络地址）需要转发到的下一跳（路由器的物理端口号或下一网络地址），而IP数据报通过多次路由转发到达目标网络后，改为*在目标局域网中通过数据链路层的MAC地址以广播方式寻址*。这样可以提高路由选择的效率。IP数据报在每个网络中都被路由器解封装和重新封装，其MAC帧首部中的源地址和目的地址会不断改变。这也决定了*无法使用MAC地址跨网络通信*。

#remark[
  路由器因为互连多个网络，所以它不仅有多个IP地址，而且有多个硬件地址。
]

- 集线器（Hub）：纯物理层设备，收到任何信号都会*泛洪（Flood）*到除入口外的所有端口。连在集线器上的设备（以及交换机的级联口）属于同一个冲突域。
- 交换机（Switch）：数据链路层设备，具有MAC地址学习功能。
  - 收到广播帧（如ARP Request）：泛洪到所有端口（除入口外）。
  - 收到单播帧（如ARP Reply）：如果转发表中有目的MAC记录，则点对点转发；如果目的MAC在同一个端口（例如都在集线器侧），则*过滤（丢弃）*不转发。


ARP工作在网络层，其工作原理如下：主机A欲向本局域网上的某台主机B发送IP数据报时，先在其ARP高速缓存中查看有无主机B的IP地址。若*有*，则可以查出其对应的硬件地址，再将此硬件地址写入MAC帧，然后通过局域网将该MAC帧发往此硬件地址。若*没有*，则通过使用*目的MAC地址为FF-FF-FF-FF-FF-FF*的帧来封装并*广播ARP请求*分组（广播发送），使*同一个局域网*里的所有主机都收到此ARP请求。主机B收到该ARP请求后，向*主机A发出ARP响应分组（单播发送）*，分组中包含主机B的IP地址与MAC地址的映射关系，主机A收到ARP响应分组后就将此映射写入ARP缓存，然后按查询到的硬件地址发送MAC帧。ARP因为“看到了”IP地址，所以它工作在网络层，而*NAT路由器*因为“看到了”*端口*，所以它工作在*传输层*。

=== NAT 网络地址转换
网络地址转换(NAT)用于解决IPv4地址不足的问题，是一种将私有（保留）地址转化为公有IP地址的转换技术。它的作用是：让内网中拥有私有 IP 的设备，能够共用一个（或少量）公网 IP 去访问互联网。
#index[NAT]#index[NAPT]#index[PAT]

私有IP地址：
- A类地址：10.0.0.0--10.255.255.255 
- B类地址：172.16.0.0--172.31.255.555
- C类地址：192.168.0.0--192.168.255.255

NAT 有多种实现方式，最常用的叫 NAPT (Network Address Port Translation)，也叫 PAT。它通过端口号来区分不同的内网主机。

#example[
  假设局域网内有两台主机 A 和 B，公网出口（NAT路由器）的 IP 是 203.0.113.5。主机 A 想访问百度的 Web 服务器（80端口）。

  + 主机发送（内网 $arrow.r$ 路由器）主机 A 发出数据包：
    - 地址：192.168.1.2:1234（内网 IP + 随机源端口）
    - 目的地址：百度IP:80
  + NAT 转换（路由器 $->$ 互联网）路由器收到包，发现要去外网。它会在自己的NAT 转换表里记录一条映射，并修改数据包的源地址：
    - 路由器分配一个未使用的空闲端口，比如 6001。
    - 新源地址：203.0.113.5:6001（公网 IP + 路由器分配的端口）
    - 目的地址：百度IP:80（不变）
  + 服务器响应（互联网 $arrow.r$ 路由器）百度收到请求，处理完后回信。百度只知道发件人是路由器的公网 IP。
    - 源地址：百度IP:80
    - 目的地址：203.0.113.5:6001（发回给路由器）
  + NAT 还原（路由器 $\to$ 内网）路由器收到回信，查看目的端口是 6001。
    - 查表：6001 对应的是内网谁？ $->$ 查到了，是 192.168.1.2:1234。
    - 修改目的地址：把 203.0.113.5:6001 替换回 192.168.1.2:1234。
    - 转发给主机 A。
  多路复用：主机 A 和主机 B 虽然内网 IP 不同，但对外都显示为同一个公网 IP 203.0.113.5，仅靠端口号 6001 和 6002 区分。
]

优点：
- 节省 IPv4 地址：这是最重要的贡献。
- 安全性：隐藏了内网的真实拓扑结构，外网无法主动攻击内网主机（因为不知道内网 IP，且 NAT 表里没有记录时，路由器会丢弃外网主动发来的包）。

缺点：
- 违背了端到端原则：P2P 下载（如 BT）和网络游戏会受影响，因为外网无法主动连接内网（需要“NAT 穿透”技术）。
- 性能开销：路由器需要修改每个包的 IP 头和 TCP/UDP 头（重新计算校验和），增加了延迟。


=== Internet 控制报文协议(ICMP)
ICMP 是 TCP/IP 协议簇的核心协议之一。它工作在网络层，但被封装在 IP 数据报中传输（IP 首部协议字段值为 1）。它的核心职能是：弥补 IP 协议“不可靠、无连接”的缺陷，提供差错报告和网络探寻机制。#index[ICMP]

ICMP 报文格式
#table(
  columns: (3cm, 3cm, 1fr),
  inset: 10pt,
  align: (col, row) => (
    if col == 2 { left + horizon }
    else { center + horizon }
  ),
  table.header(
    [*字段名称*], [*长度*], [*功能描述*]
  ),
  table.cell(rowspan: 3, align: center)[*固定首部* \ (前4字节)],
  [类型 (Type)], [8 bit], [标识 ICMP 报文的大类],
  [代码 (Code)], [8 bit], [对类型的进一步细分（如：在类型 $3$ 下，代码 $1$ 表示主机不可达）。],
  [校验和 (Checksum)], [16 bit], [对整个 ICMP 报文（首部 + 数据）进行校验，计算方法与 IP 首部校验和一致。],
  
  [*类型相关字段* \ (Rest of Header)], [32 bit \ (4字节)], [这 4 个字节的具体内容取决于 *类型*：
  - *Echo 请求/回复 (Ping)*：标识符 (Identifier) + 序列号 (Sequence Number)。
  - *差错报文*：通常填 0 (Unused)。
  - *重定向*：路由器的网关 IP 地址。],
  
  [*数据部分* \ (Data)], [可变], [这部分内容也取决于 *类型*：
  - *Echo 请求/回复 (Ping)*：包含发送的时间戳或任意数据。
  - *差错报文*：必须包含出错数据报的 *IP 首部 + 数据部分的前 8 个字节*（用于让源主机定位是哪个进程的数据包出错了）。]
)

ICMP报文类型及功能
#table(
  columns: (auto, auto, auto, 1fr),
  inset: 10pt,
  align: horizon,
  table.header(
    [*分类*], [*Type*], [*Code*], [*功能描述与应用*]
  ),
  table.cell(rowspan: 4, align: center)[*差错报告*],
  [3], [0\~15], [*终点不可达 (Destination Unreachable)* \
  Code 0: 网络不可达 \
  Code 1: 主机不可达 \
  Code 3: 端口不可达 (常用于 Traceroute 终点检测) \
  Code 4: 需要分片但设置了 DF (用于路径 MTU 发现)],
  [11], [0, 1], [*时间超过 (Time Exceeded)* \
  Code 0: TTL 耗尽 (Traceroute 的核心原理) \
  Code 1: 分片重组超时],
  [5], [0\~3], [*重定向 (Redirect)* \
  路由器通知主机有更好的路由路径，让主机更新路由表。],
  [12], [0], [*参数问题* \
  IP 首部参数错误，导致无法处理。],
  
  table.cell(rowspan: 2, align: center)[*询问报文*],
  [8 / 0], [0], [*回送请求/回答 (Echo Request/Reply)* \
  Type 8: 请求，Type 0: 回答 \
  这是 *Ping* 命令的基础。],
  [13 / 14], [0], [*时间戳请求/回答* \
  用于时钟同步和测量时间延迟。]
)

对于以下几种情况，不应发送ICMP差错报告报文：
+ 对ICMP差错报告报文，不再发送ICMP差错报告报文。
+ 对第一个分片的数据报片的所有后续数据报片，都不发送ICMP差错报告报文。
+ 对具有多播地址的数据报，都不发送ICMP差错报告报文。
+ 对具有特殊地址（如127.0.0.0或0.0.0.0）的数据报，不发送ICMP差错报告报文。

*ICMP询问报文*有4种类型：回送请求和回答报文、时间戳请求和回答报文、地址掩码请求
和回答报文、路由器询问和通告报文，最常用的是前两类。

ICMP的两个常见应用是分组网间探测PING（用来测试两台主机之间的连通性）和Traceroute(UNIX中的名字，在Windows中是Tracert，可以用来跟踪分组经过的路由)。其中PING使用了ICMP回送请求和回答报文，Traceroute（Tracert）使用了ICMP时间超过报文。

#note[
  PING工作在应用层，它直接使用网络层的ICMP；Traceroute/Tracert工作在网络层。
]
== Internet路由协议

=== OPSF-内部网关路由协议
OSPF 是一种基于链路状态 (Link-State) 的内部网关协议 (IGP)，用于在单一自治系统 (AS) 内决策路由。它基于 Dijkstra 的最短路径优先 (SPF) 算法，旨在解决 RIP (距离矢量协议) 收敛慢、易产生环路等问题。#index[OSPF]#index[IGP]

直接封装在 IP 数据包中，协议号为 89。触发式更新，网络拓扑变化时立即发送更新。支持 VLSM/CIDR：支持可变长子网掩码和无类域间路由。

#table(
  columns: (auto, 1fr),
  inset: 10pt,
  align: (col, row) => (if col == 0 { center + horizon } else { left + horizon }),
  fill: (col, row) => if row == 0 { luma(240) } else { white },
  
  [*报文类型*], [*功能与描述*],
  
  [*1. Hello*], 
  [
    - 用于发现、建立和维护邻居关系。
    - 协商参数（Hello Interval, Dead Interval, Area ID 等）。
    - 选举 DR (Designated Router) 和 BDR。
  ],

  [*2. DD* \ (Database Description)], 
  [
    - 描述本地 LSDB 的摘要信息（头部信息），用于两台路由器对比数据库差异。
  ],

  [*3. LSR* \ (Link State Request)], 
  [
    - 向邻居请求特定的 LSA 数据（当发现对方有更新的 LSA 时）。
  ],

  [*4. LSU* \ (Link State Update)], 
  [
    - 发送完整的 LSA 信息（响应 LSR 或主动触发更新）。
    - *这是承载路由信息的实际载体。*
  ],

  [*5. LSAck* \ (Link State Acknowledgment)], 
  [
    - 对接收到的 LSU 进行确认，确保可靠传输。
  ]
)


#table(
  columns: (18%, 1fr, 1fr),
  inset: 10pt,
  align: horizon,
  fill: (col, row) => if row == 0 { luma(240) } else { white },

  [*表名称*], [*描述*], [*作用*],

  [*邻居表* \ (Neighbor Table)],
  [记录所有已建立邻接关系的直连路由器。],
  [确保双向通信 ($2-"Way"$) 或完全邻接 ($"Full"$)。],

  [*链路状态数据库* \ (LSDB)],
  [存储全网（或本区域）所有的 LSA (链路状态通告)。],
  [相当于网络的拓扑地图，同一区域内所有路由器的 LSDB 一致。],

  [*路由表* \ (Routing Table)],
  [通过 SPF 算法基于 LSDB 计算出的最优路径。],
  [指导数据包转发。]
)

#figure(image("/fig/OSPF报文格式.png", width: 70%))

OSPF支持将一组网段组合在一起，称为一个区域。详细的描述拓扑结构的链路状态信息仅在区域内传递，区域间传递的是抽象的路由信息。使用层次结构的区域划分，上层的区域叫做主干区域 (backbone area)，其他区域都必须与主干区域相连。非主干区域之间不允许直接发布区域间路由信息。划分区域可以缩小LSDB规模，减少网络流量。
=== RIP-内部网关路由协议
*RIP (Routing Information Protocol)* 是一种基于距离矢量 (Distance Vector) 的内部网关协议 (IGP)。它是互联网早期广泛使用的协议，虽然在大型网络中已被 OSPF 或 IS-IS 取代，但因其配置简单，在小型网络中仍有应用。#index[RIP]

- 算法基础：Bellman-Ford 算法。
- 协议层级：应用层协议，封装在 UDP 数据包中，端口号 520。
- 度量值 (Metric)：跳数 (Hop Count)。每经过一个路由器，跳数加 1。最大跳数为 15。16 跳表示网络不可达 (Infinity)。
- 更新机制：
  - 周期性更新：每 30 秒 向邻居广播/组播完整的路由表。
  - 触发式更新：当拓扑发生变化时，立即发送更新。

RIP 最大的弱点是*收敛慢*，容易产生路由环路（Count-to-Infinity，计数到无穷大）。当网络故障时，“坏消息传得慢”。
- 水平分割 (Split Horizon)路由器从接口 $A$ 学习到的路由，不会再从接口 $A$ 发送回去。
- 毒性逆转 (Poison Reverse)路由器从接口 $A$ 学习到的路由，会从接口 $A$ 发送回去，但把跳数设为 16 (不可达)。
- 触发更新 (Triggered Updates)一旦检测到网络故障，不用等待 30秒 计时器，立即广播路由更新。
- 抑制计时器 (Hold-down Timer)当一条路由被标记为不可达（16跳）后，在一段定时间内（默认 180秒）不接受关于该网段的任何更新（除非是原下一跳发来的）。防止因网络抖动导致的路由表反复翻转。

RIP 依赖四个主要计时器来工作：
- Update Timer (更新计时器)：30s。每隔 30 秒发送一次 Response 包。
- Invalid Timer (失效计时器)：180s。如果 180 秒没收到某条路由的更新，将其标记为不可达 (16跳)。
- Hold-down Timer (抑制计时器)：180s。路由失效后，进入抑制状态，防止环路。
- Flush Timer (刷新/垃圾收集计时器)：240s。如果 240 秒还没收到更新，彻底从路由表中删除该条目。

#figure(image("/fig/RIP报文格式.png", width: 70%), caption: "RIP报文格式")

#note[
RIP是一种分布式的基于距离向量的路由选择协议，它通过广播UDP报文来交换路由信息。

- RIP 的机制：RIP 需要向同一局域网内的所有邻居路由器发送路由更新。
  - RIPv1 使用广播（255.255.255.255）。
  - RIPv2 使用组播（224.0.0.9）。
- *UDP 的能力*：UDP 是无连接的，支持一对多的通信（广播和组播）。路由器只需要发出的一个 UDP 包，所有订阅了该组播地址的邻居都能收到。
- *TCP 的限制*：TCP 是面向连接的协议，必须进行三次握手建立一对一（Unicast）的连接。TCP 不支持广播或组播。如果用 TCP：路由器必须知道每一个邻居的 IP，并与它们每一个都单独建立一条 TCP 连接。这对于动态发现邻居的 RIP 来说，既复杂又消耗资源。

- 通常我们选 TCP 是为了“可靠传输”（丢包重传）。但 RIP 的设计逻辑让 TCP 的可靠性变得多余。
- 冗余性：RIP 默认每 30秒 发送一次完整的路由表。如果这一轮的路由更新包在网络中丢失了，没关系，30秒后下一个更新包就会发过来。

- RIP 是为小型网络设计的（最大跳数 15），追求轻量级。
- 当网络发生拥塞或不稳定时（这正是需要路由协议快速收敛的时候），TCP 会触发拥塞控制（如慢启动、拥塞避免），自动降低发送速率。RIP 的需求：不管网络堵不堵，路由更新必须尽快发出去告诉邻居“路断了”或“有新路”。UDP 不受拥塞控制限制，能更及时地将路由变动广播出去。
]




=== BGP-外部网关路由协议
BGP会话: 两个BGP路由器通过TCP连接交换BGP报文。通告到不同网络前缀的路径，即路径向量协议。#index[BGP]

核心原则：只通告最优路径 (Best Path Only)这是 BGP 通告的第一铁律。 BGP 路由器虽然可能从多个邻居收到去往同一个目的网络的多条路径，但在它的 BGP 表（BGP Table）中，只有被选为最优 (Best) 的那一条路径，才会被放入路由表并通告给其他邻居。

BGP 的通告规则取决于路由的来源（本地、eBGP、iBGP）以及通告的对象（eBGP 邻居还是 iBGP 邻居）。其中iBGP之间的通告是被禁止的，不允许修改AS_PATH, 这是为了防止环的出现。其他通告都是可以的。

BGP (Border Gateway Protocol) 的报文机制非常特殊，它不像 OSPF 那样直接封装在 IP 里，而是基于 TCP 的。这意味着 BGP 不需要自己处理重传、确认和排序，全部交给 TCP 来保障可靠性。传输协议：TCP。端口号：179。点对点单播（Unicast）。不像 OSPF 那样用组播发 Hello，BGP 必须手工指定邻居 IP 并建立 TCP 连接。

- BGP 传输的是全互联网的路由表（几十万条），数据量巨大，不能像 RIP 那样每 30 秒发一次全量包（网络会崩）。
- BGP 采用触发式增量更新（只发变化的部分）。既然只发一次变化，就绝对不能丢包，所以必须依赖 TCP 的高可靠性来确保对方一定收到了这条更新。

BGP 通用头部 (BGP Header)
#table(
  columns: (auto, auto, 1fr),
  inset: 10pt,
  align: (col, row) => if col == 2 { left + horizon } else { center + horizon },
  stroke: 0.5pt + luma(150),
  fill: (col, row) => if row == 0 { luma(230) } else { white },
  
  // 表头
  [*字段*], [*长度*], [*描述*],

  // 内容
  [Marker (标记)], 
  [16 字节], 
  [通常全置为 1 (FF..FF)，用于兼容性检测和同步。若不全为 1，可能涉及认证信息。],

  [Length (长度)], 
  [2 字节], 
  [BGP 报文总长度（含头部），最小 19 字节，最大 4096 字节。],

  [Type (类型)], 
  [1 字节], 
  [标识报文的功能类型（1-5），具体含义见下表。]
)

五种报文类型详解
#table(
  columns: (auto, auto, 1fr),
  inset: 10pt,
  align: (col, row) => if col == 2 { left + horizon } else { center + horizon },
  stroke: 0.5pt + luma(150),
  fill: (col, row) => if row == 0 { luma(230) } else { white },

  // 表头
  [*类型值*], [*报文名称*], [*功能与特征*],

  // 内容
  [1], 
  [OPEN], 
  [
    - *建立连接*：TCP 连接建立后发送的第一个报文。
    - *协商参数*：携带 AS 号、Hold Time、BGP Identifier (Router ID)。
  ],

  [2], 
  [UPDATE], 
  [
    - *路由更新*：BGP 最核心的报文。
    - *携带信息*：路径属性 (Path Attributes)、网络可达信息 (NLRI)、撤销路由 (Withdrawn Routes)。
  ],

  [3], 
  [NOTIFICATION], 
  [
    - *报错通知*：当检测到错误（如 AS 号不匹配）时发送。
    - *后果*：发送后立即关闭 TCP 连接和 BGP 会话。
  ],

  [4], 
  [KEEPALIVE], 
  [
    - *保活机制*：周期性发送（默认 60s），维持邻居关系。
    - *结构*：仅包含 19 字节的通用头部，无数据体。
  ],

  [5], 
  [ROUTE-REFRESH], 
  [
    - *路由刷新*：用于支持“软重置”能力。
    - *作用*：请求对等体重新发送完整的 UPDATE 报文，以便应用新的路由策略。
  ]
)

== 路由器工作原理
=== 路由器概述
路由器是互联网最主要的网络设备，包含2个核心功能：
- 控制层：运行各种路由协议：BGP、OSPF、RIP，学习去往不同目的的转发路径：路由表
- 数据层：根据上述路由表，将收到的IP分组转发到正确的下一跳链路



=== 路由器控制层
路由器可同时运行多个路由协议，也可不运行任何路由协议，只使用静态路由和直连路由。路由管理根据路由优先级，选择最佳路由，形成核心路由表，控制层将核心路由表下发到数据层，形成转发表（FIB）。优先级数值越小，优先级越高。

直连路由：0；静态路由：1；eBGP路由：20；OSPF路由：110；RIP路由：120；iBGP路由：200


#algorithm[路由器分组转发算法][
+ 从收到的IP分组的首部提取目的主机的IP地址D（即目的地址）。
+ 若查找到特定主机路由（目的地址为D），则按照这条路由的下一跳转发分组；否则从转发表中的下一条（即按前缀长度的顺序）开始检查，执行步骤3。
+ 将这一行的子网掩码与目的地址D逐位“与”（AND操作）。若运算结果与本行的前缀匹配，则查找结束，按照“下一跳”指出的进行处理（或者直接交付本网络上的目的主机，或通过指定接口发送到下一跳路由器）。否则，若转发表还有下一行，则对下一行进行检查，重新执行步骤3。否则，执行步骤4。
+ 若转发表中有一个默认路由，则把分组传送给默认路由：否则，报告转发分组出错。
]#index(initial: "L")[路由器分组转发算法]

#remark[
  *特定主机路由* (Host-Specific Route) 是指路由表中*专门为某一台具体的计算机（主机）*设定的路由规则。
  
  路由器并不强制要求必须包含“特定主机路由”才能正常工作。 绝大多数情况下，路由器依靠“网络路由（网段路由）”和“默认路由”就足以完成互联互通的任务。
]#index(initial: "T")[特定主机路由]

== IPv6技术
IPv6（Internet Protocol version 6）是互联网工程任务组（IETF）设计的用于替代IPv4的下一代协议, 应付“32-bit地址空间耗尽”问题（CIDR和NAT都无法从根本上解决地址短缺问题），增加地址空间。地址长度为128bit，是IPv4地址长度的4倍，地址空间数量为$3 times 10^38$，表示法采用冒分16进制。每个x前面的0可省略，可把连续的值为0的x表示为“::”, 且“::”只能出现1次。
#index[IPv6]
=== IPv6头部结构
- *版本*：4bit，协议版本号，值为6
- *流量类型*：8bit，区分数据包的服务类别或优先级
- *流标签*：20bit，标识同一个数据流
- *有效载荷长度*：16bit ，IPv6报头之后载荷的字节数（含扩展头），最大值64K
- *下一个首部*：8bit ，IPv6报头后的协议类型，可能是TCP/UDP/ICMP等，也可能是扩展头
- *跳数限制*：8bit ，类似IPv4的TTL，每次转发跳数减1，值为0时包将会被丢弃
- *源地址*： 128bit ，标识该报文的源地址
- *目的地址*： 128bit ，标识该报文的目的地址

IPv6头部长度固定40字节，所有“选项”字段都在IPv6扩展头部分。IPv6分组不能在传输途中分片，只在源端进行分片，IPv6设计了专门的分片扩展头，分片字段不存在基本IPv6头部中，IPv6支持Path MTU发现机制。IPv6报文可承载多个扩展头，每个扩展头都包含“下一个首部”字段（IPv6首部固定字段也有），如有多个扩展头，需按规定顺序出现。

#table(
  columns: (auto, auto, 1fr),
  inset: 10pt,
  align: (col, row) => if row == 0 { center + horizon } else { left + horizon },
  stroke: 0.5pt,
  fill: (col, row) => if row == 0 { luma(240) } else { white },

  [地址类型], [前缀/范围], [描述],

  [未指定地址], 
  [$::\/128$], 
  [不能分配给任何节点。],

  [回环地址], 
  [$::1\/128$], 
  [表示节点自己，不分配，类似 IPv4 中的 127.0.0.1。],

  [组播地址], 
  [$"FF00"::\/8$], 
  [用于组播通信。],

  [链路本地地址], 
  [$"FE80"::\/10$], 
  [也称为 Link-local 地址，仅在本地链路上使用，网络设备根据接口 MAC 地址自动生成。],

  [全局单播地址], 
  [其他地址], 
  [相当于 IPv4 的公网地址。]
)

#table(
  columns: (25%, 1fr),
  inset: 10pt,
  align: (col, row) => if row == 0 { center + horizon } else { left + horizon },
  stroke: 0.5pt,
  fill: (col, row) => if row == 0 { luma(240) } else { white },

  [配置方式], [详细说明],

  [手动配置], 
  [管理员手动输入 IP 地址。],

  [DHCPv6], 
  [IPv6 动态主机配置协议。],

  [无状态地址自动配置\ (SLAAC)], 
  [基于 ND (Neighbor Discovery) 协议的 RS 报文获取 IPv6 前缀信息，结合自己的链路层地址 (MAC) 生成 IPv6 地址。]
)

#remark[
  IPv6不允许将数据报分片。若一个路由器收到的IPv6数据报因太大而不能转发到链路上，那么数据报会被丢弃。
]

IPv4向IPv6过渡主要依赖三种核心技术：*双协议栈 (Dual Stack)*，即设备和节点同时支持并运行IPv4和IPv6两套协议栈，实现共存；*隧道技术 (Tunneling)*，通过将IPv6报文封装在IPv4报文的数据部分中（如6to4、ISATAP），使其能够穿越旧有的IPv4网络骨干；*网络地址翻译 (Translation/NAT64)*，利用网关设备在网络边界将IPv4和IPv6的报头及地址进行相互转换，实现纯IPv4与纯IPv6网络之间的直接通信。
== VPN技术
VPN (Virtual Private Network，虚拟专用网络) 技术的核心在于通过公共网络（如 Internet）构建一条逻辑上的专用通信隧道。它的本质并不是建立物理连接，而是通过隧道技术 (Tunneling)、加密 (Encryption) 和 身份认证 (Authentication) 将数据封装起来传输。

VPN的设计原则：安全性、隧道与加密、数据验证、用户验证、防火墙与攻击检测

VPN 客户端通过操作系统内核的虚拟网络设备截获网络流量。#index[VPN]

#table(
  columns: (auto, auto, auto, 1fr),
  inset: 10pt,
  align: (col, row) => if row == 0 { center + horizon } else { left + horizon },
  stroke: 0.5pt,
  fill: (col, row) => if row == 0 { luma(240) } else { white },

  [接口名称], [OSI 层级], [处理对象], [典型应用场景],
  
  [TUN (Tunnel)], 
  [Layer 3 (网络层)], 
  [IP 数据包], 
  [路由模式 VPN (Routing)。\ Android/iOS VPN，WireGuard，OpenVPN (tun模式)。],

  [TAP (Tap)], 
  [Layer 2 (数据链路层)], 
  [以太网帧], 
  [桥接模式 VPN (Bridging)。\ 需要传输非 IP 协议 (如 NetBIOS) 或进行二层组网时使用。]
)

#table(
  columns: (auto, auto, auto, 1fr),
  inset: 10pt,
  align: (col, row) => if row == 0 { center + horizon } else { left + horizon },
  stroke: 0.5pt,
  fill: (col, row) => if row == 0 { luma(240) } else { white },

  [协议], [工作层级], [实现空间], [核心特点],

  [IPsec], 
  [Layer 3 (IP)], 
  [内核态 (Kernel)], 
  [工业标准，支持广泛。包含 AH (认证) 和 ESP (加密) 协议。配置较复杂，适合站点到站点 (Site-to-Site)。],

  [SSL/TLS \ (如 OpenVPN)], 
  [Layer 4/7], 
  [用户态 (User)], 
  [基于 TLS (HTTPS) 握手。防火墙穿透性极强（看起来像网页流量）。部署灵活，性能略低于内核级协议。],

  [WireGuard], 
  [Layer 3], 
  [内核态], 
  [新一代协议。代码极简 (4000行)，使用现代加密原语，连接速度极快，无状态 (Stateless) 设计。]
)

VPN 的本质是“报文的二次封装”。以下展示了原始数据包如何被封装在公网数据包中。

#figure(
  table(
    columns: (1fr, 3fr),
    inset: 10pt,
    align: center + horizon,
    stroke: 0.5pt,
    
    // 原始报文
    table.cell(rowspan: 2, align: center + horizon)[*原始报文*\ (内网传输)],
    table.cell()[
      *IP 头部 (Inner Header)* \
      Src: 192.168.1.10 (内网) \
      Dst: 10.0.0.5 (公司服务器)
    ],
    table.cell()[
      *负载 (Payload)* \
      TCP / HTTP Request
    ],

    // 转换箭头
    table.cell(colspan: 2, stroke: none, align: center)[
      $arrow.b$ *经过 VPN 虚拟网卡加密封装* $arrow.b$
    ],

    // 封装后报文
    table.cell(rowspan: 2, align: center + horizon)[*封装后报文*\ (公网传输)],
    table.cell()[
      *新 IP 头部 (Outer Header)* \
      Src: 202.1.1.1 (家) \
      Dst: 58.1.1.1 (VPN 网关)
    ],
    table.cell()[
      *加密载荷 (ESP/TLS Payload)* \
      $ "Encrypt" ( "Inner IP Header" + "Original Payload" ) $
    ]
  )
)
