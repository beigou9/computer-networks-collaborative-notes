#import "../note.typ": *


= 介质访问子层

数据链路层分为两个子层: 
+ MAC子层: 介质访问
+ LLC子层: 承上启下 (弱层) 

#remark()[
  - *信道划分介质访问控制*的核心思想是: 通过分时、分频、分码等方法, 将广播信道变为若干点对点信道。
  - *随机访问介质访问控制*: 胜利者通过争用获得信道, 从而获得信息的发送权。
  - *集中式介质访问控制*: 由一个主站控制所有从站的发送顺序, 从站只能在主站允许时才能发送数据。
  - *轮询访问介质访问控制*: 通过轮询方式依次询问每个站点是否有数据要发送。
]
#index[LLC]
#index[MAC]
== 信道分配问题
- 信道: 信号的通道。
  - 比如: 双绞线、铜缆、光纤、卫星、空气等
- 点到点信道: 信道直接连接两个端点
  - 比如: 家中计算机通过modem连接到电信公司端局 (调制解调器 (Modem) 是一个用于网络连接的关键设备, 它的核心作用可以概括为信号转换。) 
- 多点访问信道: 多用户共享一根信道
  - 比如: 当使用集线器或交换机工作在半双工模式的时候, 它的逻辑拓扑是总线式的, 信道是共享的。

常见的局域网拓扑有: 总线拓扑、星型拓扑、环型拓扑。他们都共享一根信道 (别称: 广播信道、多路访问信道、随机访问信道) 

#remark[*令牌传递机制*

环形拓扑形成了一个物理上的闭环。数据 (包括令牌) 只能沿着一个方向 (顺时针或逆时针) 依次从一个站点传递到下一个站点。

*工作原理: *令牌在环上单向循环。站点只有拿到令牌才能发送数据。数据帧会遍历环上的所有站点, 目标站点复制数据, 最后由发送站将其移出环并释放新令牌。
]
广播信道面临的问题: 可能两个 (或更多) 站点同时请求占用信道。用介质的多路访问控制进行解决: 在多路访问信道上确定下一个使用者 (信道分配) 而介质访问控制 (分配信道) 分为静态分配与动态分配两种。

== 多路访问协议
=== 随机访问协议
纯ALOHA协议基本原理: *无协调的异步传输*。在纯ALOHA协议中, 任何一个网络节点在生成一个数据帧后, 会立即通过共享信道进行传输。此过程无需任何预先的协调机制, 例如不检测信道是否空闲 (载波侦听) , 也不依赖于一个统一的时序划分 (时隙) 。这种“生成即发送”的模式被称为异步传输。#index[ALOHA]

由于传输是异步且无协调的, 多个节点可能在任何时间点同时发送数据帧。当两个或多个数据帧在时间上发生重叠时, 即产生冲突。
- *信号叠加与帧破坏*: 在广播信道中, 冲突导致电磁信号在信道上相互叠加, 使得接收方无法正确解析任何一方的数据。因此, 所有发生冲突的帧均会完全损坏。发生冲突的数据帧完全失效, 必须重传。
- *冲突窗口*: 一个已发送的数据帧容易遭受冲突的时间范围为其整个发送过程再向前延伸一帧的传输时间。具体而言, 如果一个帧在时间t开始发送, 那么任何在$[t - T, t + T]$区间内 (其中T为一帧的传输时间) 开始发送的其他帧都会与之冲突。

// 
// TODO: 
// #note[
//   如果这里用码分复用呢？
// ]
// 
节点在发送帧后, 会启动一个定时器等待来自接收方的确认。如果在超时前未收到确认, 节点即判定传输失败 (极有可能是因冲突导致) 。随后, 该节点将在一段随机退避时间后重新发送受损的帧。引入随机延迟的目的是为了降低同一组节点在重传时再次发生冲突的概率。

分隙ALOHA是对纯ALOHA协议的一种根本性改进, 其核心思想是通过引入离散的时间片来约束节点的发送行为, 从而显著降低冲突概率。

*基本思想: 从异步到同步*
+ *时间分隙*: 系统将连续的时间轴划分为等长的、固定的时间间隔, 称为时隙。
+ *时隙同步*: 所有网络节点需要保持一个基本的时钟同步 (例如, 由一个主节点发送同步信号) , 使得大家对时隙的起点和终点有共同的认识。
+ *发送规则*: 任何节点在有数据帧需要发送时, 必须等待下一个时隙的起点才能开始传输。它不能在时隙中间随意开始发送。
+ *帧长约束*: 每个时隙的长度被设置为恰好可以传输一个完整的数据帧。这意味着一个帧的传输时间$T$正好等于一个时隙的长度。

理论上是一个无限短的时刻, 但因为它会导致整个时隙的帧损毁, 所以可以认为冲突风险期等于一个时隙长度 T

CSMA的基本思想是“先听再发”, 旨在降低冲突概率。适用于总线型网络, 是有线网络, 不是无线网络。本质上是半双工通信。#index[CSMA]

+ *非持续式CSMA (Non-persistent CSMA)*
  - 特点: 
    - 侦听信道, 如果空闲, 则立即发送。
    - 如果信道忙, 则不持续侦听, 而是等待*一个随机长度的时间*后, 再重新执行步骤1。
  - 优点: 通过等待随机时间, 降低了多个等待站点在信道空闲时同时发送数据的概率, 从而减少了冲突的可能。
  - 缺点: 在等待的随机时间内, 即使信道很快变为空闲, 站点也不会立即发送, 导致信道利用率降低, 产生空闲时间浪费。
+ *1-持续式CSMA (1-persistent CSMA)*
  - 特点: 
    - 侦听信道, 如果空闲, 则立即发送 (发送概率为1, 故名“1-持续”) 。
    - 如果信道忙, 则*持续侦听, 一旦发现空闲, 立即发送*。
    - 如果发生冲突, 则*等待一个随机时间*后重试。
  - 优点: 相比非持续式, 因为持续侦听, 信道空闲时能立即被使用, 减少了延迟。
  - 主要问题: 如果有两个或以上站点都在等待发送, 一旦信道空闲, 它们会同时开始发送, 导致必然的冲突。
+ *p-持续式CSMA (p-persistent CSMA)*
  - 这是对1-持续式的优化, 适用于时分槽信道.
  - 特点: 
    - 侦听信道, 如果空闲, 则以 p的概率发送, 以 1-p的概率延迟一个时间单元 (如一个时隙) 再发送。
    - 如果信道忙, 则持续侦听, 一旦空闲重复步骤①。
    - 如果已经推迟了一个时间单元, 则重复步骤①。
  - 1-持续式是p-持续式在 p=1时的特例。通过引入概率 p, 即使多个站点同时侦听到信道空闲, 也能在一定程度上错开发送时间, 降低了冲突概率。

#figure(image("../fig/CSMA_CD碰撞检测时间点及事件.png", width: 50%), caption: "CSMA/CD碰撞检测时间点及事件")
- $t=0$时, $A$发送数据, $B$检测到信道空闲。
- $t= tau - delta$时, $A$还未到达$B$, 由于$B$检测到信道空闲, 因此$B$发送数据。
- $t=tau -delta/2$时, $A$与$B$发送碰撞, 此时$A$与$B$未检测到碰撞, 信号延迟。
- $t=tau$时, 失真信号到达$B$, $B$检测到碰撞, 停止发送数据。
- $t=2tau-delta$时, 失真信号到达$A$, $A$也检测到碰撞, 也停止发送数据。

#figure(image("../fig/CSMA_CD碰撞二级结论.png", width: 80%), caption: "左图甲乙同时发送数据,历时最短;右图甲的第一个比特到达乙时, 乙刚好发送数据,历时最长")
- 当$delta=tau$时, 所经历时间最短, 即甲乙同时发送的数据, 最短时间为$T_"min"=tau$.
- 当$delta -> 0$时, 所经历时间最长, 即在甲第一个比特到达乙时, 乙刚好发送数据, 最长经历时间为$T_"max"=2tau$.

#remark[
  若$B$在$t=tau$时再未发送数据, 之后就会检测到信道忙碌, 就不会再发送数据了。那么在$t=2tau$时, 双方若未检测到碰撞, 说明 $B$没有发送数据, 则肯定这次未发生碰撞。

  *于是我们把$t=2tau$的时间称之为一个争用期*。注: 由争用期的概念可引出能使用CSMA/CD协议的最短帧长, 要求*数据帧发送时间$gt.eq.slant$争用期*。原因是, 若数据帧发送时间小于争用期, 发送方$A$在数据到达$B$之前就发完了数据。而检测碰撞必须是边发送边检测的, 数据发完就停止检测碰撞, 于是碰撞就检测不出来了。
]

#example[
  假设1km长的CSMA/CD协议网络的数据率是1Gbit/s, 设信号的传播速率为$2times 10^5$km/s, 求能够使用此协议的最短帧长。

  传播时延为$tau=5 mu s$, 争用期$2tau =10 mu s$, 所以最短帧长$L_"min"=c 2tau=10000 "bit"$.
]

#note[
  特别注意, 最短帧长是要考虑到确认帧回传的, 确认帧也需要满足最短帧的限制。
]
CSMA的根本缺陷: *传播延迟 (Propagation Delay)* 。

- 冲突原因: 
  + *同时传送*: 两个站点可能在同一时刻侦听都认为信道空闲, 然后同时发送。
  + *传播延迟* (关键原因) : 这是最核心的问题。假设站点A开始发送, 信号在物理链路上传播到站点B需要一段时间。在信号到达B之前, B侦听信道时依然会认为是空闲的, 此时B也可能开始发送, 从而导致在中间某处发生冲突。
- 冲突窗口 (Collision Window) : 
  - 定义: 一个站点从开始发送帧到能够检测到冲突的最长时间。
  - 数值上, 它等于网络中最远两个站点之间传播延迟 (D) 的两倍, 即 2D。
  - 这也就是数据往返一次的时间 (RTT) 。只有在“冲突窗口”2D时间内没有检测到冲突, 才能基本肯定本次发送占据了信道。
  - 重要推论: 发送的数据帧不能太短, 必须长于“冲突窗口”时间2D, 否则站点在发完整个帧之前都无法检测到是否发生了冲突。

为了克服CSMA的缺陷, 引入了边发边听的机制, 这就是CSMA/CD, 它是经典以太网 (10BASE5, 10BASE2) 的基础。#index[CSMA/CD]
+ *先听*: 发送前侦听信道。若空闲, 则发送；若忙, 则持续侦听。
+ *边发边听* : 在发送过程中持续侦听信道, 比较发送的信号和接收到的信号。
+ *冲突处理*: 
  - 一旦检测到冲突, 立即停止发送当前帧, 并发送一个短暂的Jam (强化冲突) 信号, 以确保所有站点都知道发生了冲突。
  - 然后, 站点等待一个随机长度的时间 (退避) , 再重新尝试发送。

#remark[
  发送端 (Tx) 和接收端 (Rx) 协同工作: 站点一边发送信号 (Tx) , 一边通过接收器 (Rx) 从信道上接收信号。如果接收到的信号与发送的信号不一致 (例如, 电压水平异常) , 就表明有另一个站点的信号叠加了上来, 即发生了冲突。
]

CSMA/CD网络的工作时间可以划分为三个周期: 
- 传输周期: 一个站点成功占用信道并传输数据的时间。
- 竞争周期: 传输结束后或冲突发生后, 所有站点通过CSMA/CD规则竞争信道使用权的时间。
- 空闲周期: 没有任何站点需要发送数据的时间。

对于CSMA/CD
- 吞吐量: 比ALOHA高, 比P-持续式CSMA低
- 冲突: 比ALOHA少, 比P-持续式高
- P-持续式付出了高延迟的代价

#example[
  在Hub再生比特流过程中, 会产生$1.535 mu s$的延迟, Switch和Hub 均为100Base-T设备, 信号传播速率为$200 "m/"mu s$, 不考虑以太网帧的前导码, 则两个中间只有一个Hub的主机之间, 理论上可以相距最远距离是多少?

  最短帧发送时间为$64B div 100"Mb/s"=5.12 mu s$, 单程总时延是$2.56 mu s$, 传播时延是$2.56-1.535=1.025 mu s$, 从而理论距离最大值是$200 times 1.025=205m$.
]

== 以太网
以太网是目前最流行的有线局域网技术, 以太网逻辑上采用*总线形拓扑结构*, 所有计算机共享一条总线, 信息以*广播方式*发送, 主机运行CSMA/CD协议, 常用的以太网MAC帧格式有两种标准 : DIX Ethernet V2 标准 (最常用的) 和IEEE 的 802.3 标准。

#index(initial: "Y")[以太网]
考虑到局域网信道质量好, 以太网采取两项措施让通信更简单。*采取无连接的工作方式*; 不对发送的数据帧进行编号, *不要求对方发回确认, 提供的是不可靠的服务, 差错的纠正由高层完成*。发送的数据都使用*曼彻斯特编码*的信号。

计算机与外界局域网的连接是通过主板上嵌入的一块网络适配器 (Adapter) [又称网络接口卡(Network Interface Card, NIC)]实现的。适配器上装有*处理器和存储器*, 工作在数据链路层。适配器和局域网的通信是通过*电缆或双绞线以串行方式进行*的, 而适配器和计算机的通信则是通过计算机的I/O总线以并行方式进行的。因此, 适配器的重要功能就是进行数据的串并转换。适配器不仅能实现与局域网传输介质之间的物理连接和电信号匹配, 还涉及帧的发送与接收、帧的封装与拆封、介质访问控制、数据的编码与解码及数据缓存等功能。当适配器收到正确的帧时, 就使用*中断*来通知该计算机, 并交付协议栈中的网络层。当计算机要发送IP数据报时, 就由协议栈把IP数据报向下交给适配器, 组帧后发送到局域网。#index[NIC]


IEEE802标准为局域网规定了一种48位的全球地址, 是指*局域网上的每台计算机中固化在适配器的ROM中的地址, 称为物理地址或MAC地址* (因为这种地址用在MAC帧中), 这个地址用于控制主机在网络上的数据通信。全世界所有的局域网适配器都具有不同的地址, *一台计算机只要没有更换适配器, 不管其地理位置如何变化, 其MAC地址都不会变化。*

硬件地址又称为*物理地址*, 或* MAC 地址*。MAC地址长6字节, 一般用由连字符 (或冒号) 分隔的12个十六进制数表示, 如02-60-8c-e4-b1-21。高24位为厂商代码, 低24位为厂商自行分配的适配器序列号。当*路由器*通过*适配器*连接到局域网时, 适配器上的MAC地址就用来*标志路由器的某个接口*。路由器若同时连接到两个网络上, 则它需要两个适配器和两个MAC地址。

适配器从网络上每收到一个MAC帧, 首先都要用硬件检查MAC帧中的目的地址, 若是发往本站的则收下, 否则丢弃掉。这里有3种可能的帧
+ *单播帧(一对一)*, 即收到的帧的目的地址与本站的MAC地址相同。
+ *广播帧(一对全体)*, 即发送给本局域网上所有站点的(全1地址)。
+ *多播帧(一对多)*, 即发送给本局域网上一部分站点的帧。

以太网MAC帧有2种标准, DIX Ethernet V2标准和IEEE 802.3标准:

#index[DIX Ethernet V2]
#align(center, table(
  columns: 3,
  align: center,
  inset: 5pt,
  stroke: (bottom: 1pt + black), // 仅底部边框

  // 表头
  [*Frame Field*], [*DIX Ethernet V2*], [*IEEE 802.3*],
  // 表体内容
  [前导码], [8 bytes], [8 bytes],
  [目的地址], [6 bytes], [6 bytes],
  [源地址], [6 bytes], [6 bytes],
  [类型 / Length], [类型 (2 bytes)], [Length (2 bytes)],
  [数据], [46-1500 bytes], [46-1500 bytes],
  [检验和], [4 bytes], [4 bytes],
))

IP层将IP数据报交给MAC层的数据区, MAC层再把从目的地址开始的MAC帧交给物理层, 物理层会加入前导定界符。

源地址后面的两个字节, Ethernet V2将其视为上一层的协议类型, IEEE802.3将其视为数据长度。 

在帧前面插入的8字节前导码分为两个字段: 第一个字段是7字节的前同步码, 用来实现MAC帧的比特同步; 第二个字段是1字节的帧开始定界符, 表示后面的信息就是MAC帧。

#remark[
  *以太网帧不需要帧结束定界符, 因为当以太网传送帧时, 各帧之间必须有一定的间隙。*因此, 接收方只要找到帧开始定界符, 其后面连续到达的比特流就都属于同一个帧。实际上, 以太网采用了*违规编码法*的思想, 因为以太网使用曼彻斯特编码, 所以每个码元中间都有一次电压的跳变。发送方发完一个帧后, 发送方网络接口上的电压不再变化, 这样接收方就能很容易地找到帧的结束位置, 这个位置往前数4字节就是FCS字段, 于是就能确定数据字段的结束位置。
]

#note[
  以太网*不采用“字符填充”或“位填充”*, 它*靠“先定界、后计数、再校验”*的机制 (感觉主要是计数机制) 保证一一数据区里*无论出现多少个0xD5都不会被误识别为帧起始*, 也*无需任何转义处理*。
]
数据字段不足46字节, 需要填充整数字节 (Padding) 至46字节, 以保证以太网MAC帧不小于64字节。
#remark[
  为了保证发送方能够检测到冲突, 它发送一个帧的时间 (即帧的发送时延) 必须不小于冲突窗口的时间(2T)。因此, 最小帧长 = 2T × 数据传输速率。考虑到中继器等因素, 冲突窗口 (2T) 被设定为 51.2 微秒。因此计算得最小帧长为512bit, 即64字节。

  如果帧长小于64字节, 发送方可能在冲突窗口结束之前就已经把整个帧发送完毕, 然后停止了信道监听。如果此时在网络的远端发生了冲突, 冲突信号将在之后才传回发送方。但发送方已经“以为”发送成功, 停止了监听, 因此无法检测到这个冲突。这个帧传输失败, 但发送方不知道, 不会进行重传。而上层协议要等到超时后才能发现, 效率极低。

  MAC帧不能小于64字节, 是为了强制帧的发送时间足够长, 长到能覆盖整个网络的“冲突窗口”。这样, 发送站就能在发送过程结束之前, 确保自己发送的数据没有与网络中任何其他站点的数据发生冲突。这是CSMA/CD协议能够正常工作的根本保障。

]

- 目的地址: 6字节, 帧在局域网上的目的适配器的MAC地址。
- 源地址: 6字节, 传输帧到局域网上的源适配器的MAC地址。
- 类型: 2字节, 指出数据字段中的数据应交给哪个上层协议处理, 如网络层的IP协议。

对于检查出的无效 MAC 帧就简单地丢弃。以太网不负责重传丢弃的帧。 
- 数据字段的长度与长度字段的值不一致。
- 帧的长度不是整数个字节
- 用收到的帧检验序列 FCS 查出有差错。
- 数据字段的长度不在 46 $~$ 1500 字节之间。

#remark[
  在以太网中, 若网卡发现某个帧的目的MAC地址不是自己的, 则会丢弃这个帧, 不向网络层报告错误。
]
*二进制指数退避*: 这是决定等待时间的智能算法。
#index(initial: "E")[二进制指数退避]

- 基本时间单位: 时间槽 (= 512比特时间) , 等于网络最远距离的往返传播时延。
- 退避过程: 当第 k次重传时 (k≤10) , 从 [0, 1, ..., 2ᵏ -1]这个集合中随机选择一个数字 r。等待的时间就是 r倍的时间槽。
- 意义: 冲突次数越多, 可能的等待时间范围越大, 这有助于在重负载下分散各站点的重传时间, 降低再次冲突的概率。
- 冲突越多, $k$越大, 再次冲突的概率越低。
- 控制最大等待时间, 当$k$超过10之后, 退避时间已经相当长, 目的是在高负载下让网络有机会恢复平稳。

#example[
  假设网络中有三台设备 (A、B、C) 几乎同时想要发送数据, 导致了冲突。这是它们第一次发生冲突 (冲突次数=1) 。
  
  - 第一次冲突后, $k=1$, 随机数 r的取值范围是 ${0, 1, 2^1 - 1}$, 也就是 ${0, 1}$。这意味着设备可能的退避时间只有两种选择: 0微秒 (立即重试) 或 51.2微秒,假设A随机到0, B随机到1, C也随机到0。A和C会立即重试, 导致第二次冲突。
  - 第二次冲突后, $k=2$, 机数 r的取值范围扩大到 ${0, 1, 2, 3}(2^2 - 1 = 3)$。现在可能的退避时间有4种: 0、51.2微秒、102.4微秒、153.6微秒. 选择范围变大了, A和C随机到相同数字的概率降低了。假设A随机到0, C随机到3。A立即重试成功, C等待153.6微秒后再重试。
]


#align(center, table(
  columns: 5,
  align: center,
  stroke: black,
  table.header(
    [*标准名称*], [*10BASE5*], [*10BASE2*], [*10BASE-T*], [*10BASE-F*]
  ),
  [传输介质], [同轴电缆 (粗缆) ], [同轴电缆 (细缆) ], [非屏蔽双绞线], [光纤对 (850nm) ],
  [编码], [曼彻斯特编码], [曼彻斯特编码], [曼彻斯特编码], [曼彻斯特编码],
  [拓扑结构], [总线形], [总线形], [星形], [点对点],
  [最大段长], [500m], [185m], [100m], [2000m],
  [最多结点数目], [100], [30], [2], [2]
))#index[10BASE5]#index[10BASE2]#index[10BASE-T]#index[10BASE-F]

经典以太网的信道效率为: 
$
  1/(1+(2B L e) /(c F))
$
$F$: 帧长, $B$: 带宽, $L$: 电缆长度, $c$: 光速, $e$: 常数。

帧长越长 (F↑) , 效率越高 (因为有用数据发送时间占比大) 。网络距离越长 (L↑) 或带宽越高 (B↑) , 效率越低 (因为冲突窗口时间占比变大) 。根本矛盾: CSMA/CD协议限制了网络规模和速度不能同时提高。这就是为什么早期10Mbps以太网有距离限制, 而想要提高速度 (如到100Mbps) , 就必须缩短网络距离。

发展到交换式以太网

Hub的本质: 是一个物理层设备, 仅仅是对信号进行放大和广播。所有端口共享同一根总线。所有连接到Hub的设备处于同一个冲突域。同一时间只能有一台设备成功发送数据。设备越多, 冲突概率越高, 效率急剧下降。#index[Hub]

#note[
  集线器不能隔离冲突域。交换机和路由器可以隔离冲突域。广播域由路由器进行划分。
]

改进为交换机, 而交换机的本质是数据链路层设备, 智能地基于MAC地址表进行转发。
- 交换机内部有一个MAC地址表, 记录每个MAC地址对应哪个端口。
- 收到数据帧后, 只将其转发到目标MAC地址所在的端口, 而不是广播到所有端口。

#remark[
  - *中继器*: 工作在*物理层*, 放大信号, 用于信号的远距离传输。
  - *集线器*: 工作在*物理层*, 本质为一个多口中继器, 他会对于发送给他的信号进行广播, 所以他不能划分冲突域和广播域。
  - *网桥*: 工作在*数据链路层*, 是进阶版集线器, 接收其他集线器的广播, 基于 ​​“存储转发”​​ 和 “MAC地址表” 的工作机制，从广播变成了有目的的转发。(相当于用MAC地址进行过滤, 不满足发送要求的不允许跨越网桥), 因此网桥隔离冲突域, 不隔离广播域。
  - *交换机*: 工作在*数据链路层*, *升级版的网桥, 现在已经基本全面取代了网桥*。对于同一个网络内可以实现点对点通信, 可以支持多对用会同时通信。可以分割冲突域, 但不分割广播域。
  - *路由器*: 工作在*网络层*, 实现不同网络之间的通信, 可以分割冲突域和广播域。

  #index(initial: "Z")[中继器]
  #index(initial: "J")[集线器]
  #index(initial: "W")[网桥]
  #index(initial: "J")[交换机]
  #index(initial: "L")[路由器]
]
#table(
  columns: 4,
  align: center,
  stroke: black,
  [标准名称], [100Base-T以太网], [吉比特以太网], [10吉比特以太网],
  [传输速率], [100Mb/s], [1Gb/s], [10Gb/s],
  [传输介质], [双绞线], [双绞线或光纤], [双绞线或光纤],
  [通信方式], [支持半双工和全双工方式], [支持半双工和全双工方式], [只有全双工方式],
  [介质访问控制协议], [半双工方式下使用CSMA/CD协议], [半双工方式下使用CSMA/CD协议], [无]
)

#remark[
  - Hub (共享式) : 所有设备在一个冲突域, 半双工, 必须用CSMA/CD。
  - Switch (交换式) : 每个端口是一个独立的冲突域。支持全双工通信 (可以同时收发) , 因此完全不需要CSMA/CD协议。

  在解决了冲突域这个根本问题后, 以太网的发展重点转向了提高速度。交换机的全双工模式消除了CSMA/CD的限制, 使得高速化成为可能。
]
#align(center, table(
  columns: 4,
  align: (center, left, left, left),
  inset: 5pt,
  
  [以太网类型], [标准], [主要特点], [新技术/挑战],
  
  // 快速以太网
  [快速以太网 (100Mbps)], [IEEE 802.3u], [
    - 速度提升10倍
    - 完全兼容10M以太网帧格式和接口
    - 保护投资
  ], [
    - *代价*: 最大电缆长度缩短为1/10
    - *新功能*: 引入自动协商 (速度和双工模式) 
  ],
  
  [千兆以太网 (1Gbps)], [IEEE 802.3ab/z], [
    - *重要转折点*: 默认全双工模式
    - 通常不再使用CSMA/CD
  ], [
    - *技术挑战*: 载波扩充保证最小帧长
    - *新特性*: 支持流量控制和可选巨型帧
  ],
  
  [万兆以太网 (10Gbps)], [IEEE 802.3ae], [
    - *根本性变化*: 只支持全双工
    - 彻底抛弃CSMA/CD协议
  ], [
    - *重点*: 技术挑战集中在物理层实现
    - 如光纤、高级编码方式
  ]
))

== 无线局域网
无线局域网（Wireless Local Area Network，WLAN)：指以无线信道作为传输介质的计算机局域网。两个重要组织：IEEE 802.11工作组、Wi-Fi联盟（Wi-Fi Alliance，WFA）。设计目标：#index[WLAN]
- 针对小的覆盖范围（受限的发射功率）
- 使用无需授权的频谱（ISM频段)
- 面向高速率应用
- 能够支持实时和非实时应用

基础架构模式：分布式系统(DS), 访问点(AP), 站点(STA), 基本服务集(BSS), 扩展服务集(ESS), 站点之间通信通过AP转发。
#index[DS]#index[AP]#index[STA]#index[BSS]#index[ESS]

自组织模式(Ad hoc): 站点，独立基本服务集(BSS), 站点之间直接通信，共享同一无线信道。

无线局域网的体系结构遵循IEEE 802.11标准，可以看作是经典的OSI模型或TCP/IP模型在无线环境下的具体实现，主要关注物理层和数据链路层。

#table(

  columns: (0.67fr, 0.66fr, 2.6fr),
  align: left,
  inset: 8pt,
  
  // 表头
  [协议栈层次], [对应子层], [核心功能与解读],
  
  // 数据链路层 - LLC层
  [数据链路层], [
    LLC层 \
    （逻辑链路控制层）
  ], [
    功能：为上层网络协议提供统一的接口，实现与具体物理介质无关的数据链路控制。\
    详细解读：\
    • 统一接口：为上层（网络层）提供独立于具体MAC技术的标准服务接口。\
    • 复用分用：通过SAP（服务访问点）标识符，允许多个上层协议共享同一数据链路。\
    • 流量控制与差错控制：提供可选的可靠性服务，确保帧的有序传输。\
    重要性：LLC层使得网络层协议（如IP）能够透明地运行在不同的物理网络（以太网、Wi-Fi等）之上。
  ],
  
  // 数据链路层 - MAC层
  [数据链路层], [
    MAC层 \
    （介质访问控制层）
  ], [
    - 可靠数据传输：在有干扰的无线环境中，通过确认（ACK）和重传机制保证帧的可靠交付。\
    - 介质访问控制：协调多个设备共享无线信道，核心机制是CSMA/CA（载波侦听多路访问/冲突避免），相当于"先听听有没有人在说话，等别人说完了再抢着说，避免同时开口"。\
    - 安全机制：提供身份认证和数据加密（如WPA2, WPA3）。
  ],
  
  // 物理层 - PLCP层
  [物理层], [
    PLCP层 \
    （物理层汇聚协议子层）
  ], [
    作为MAC层和底层PMD层的适配器。\
    - 统一接口：为MAC层提供一个统一的接口，使其无需关心底层具体的传输技术（是使用2.4GHz还是5GHz频段？采用何种调制技术？）。\
    - 帧适配：负责将MAC层下来的帧（MPDU）转换成适合在物理介质上传输的格式（PPDU），并添加前导码和帧头，用于接收方的同步和解调。
  ],
  
  // 物理层 - PMD层
  [物理层], [  
    PMD层 \
    （物理介质相关子层）
  ], [
    这是真正与"物理世界"打交道的部分。\
    • 调制解调：将PLCP层下来的数字信号（0和1）调制成可以在自由空间传输的无线电波；反之，将接收到的无线电波解调为数字信号。\
    • 编码/解码：对数据进行编码（如加入纠错码）以提高无线传输的抗干扰能力。
  ]
)

不能直接将CSMA/CD 用于无线局域网：
- 冲突检测困难
  - 在接收端，发送功率和接收功率相差太大
  - 站点在发送时关闭接收功能，无法在发送时同时检测冲突
- 在同一BSS中，不是所有站点都能互相感知到对方发送的信号
  - 载波侦听失败，但在接收站点处发生冲突
  - 被称为隐藏终端问题

暴露终端问题，降低网络的吞吐量，信号衰落随时间发生变化，使问题变得更加复杂。

#remark[
  在无线通信中，发送功率和接收功率之所以存在巨大差距，主要源于信号在空间传播过程中的路径损耗。设备在发送时，自身的强大发射信号会完全“淹没”从远方传来的微弱信号，使得在同一频段上同时进行发射和接收变得异常困难。所以目前的无线通信设备大多采用半双工模式。虽然存在一种称为“双工器”的器件可以在理论上允许在同一频段上同时收发，但它会显著增加设备的复杂性和成本。
]

- 传输范围(TX-Range)：成功接收帧的通信范围，取决于发送功率和无线电波传输特性。
- 物理层侦听范围（PCS-Range ）：检测到该传输的范围，取决于接收器的灵敏度和无线电波传输特性
- 干扰范围（IF-Range ）：在此范围内的节点如果发送不相关的帧，将干扰接收端的接收并导致丢帧

隐藏终端问题：由于距离太远（或障碍物）导致站点无法检测到竞争对手的存在，隐藏站点不能侦听到发送端但能干扰接收端。核心矛盾是不该同时发送时，却同时发送，导致冲突；结点关系是发送节点之间无法相互感知；对网络的影响是增加数据碰撞，降低吞吐量，造成时隙资源无序争用。

=== CSMA/CA （Carrier Sense Multiple Access with Collision Avoid）
#index[CSMA/CA]
为了更好地解决前面提到的两个难题，冲突检测困难和隐藏终端问题，才提出了CSMA/CA协议。

CSMA/CA通过“先监听，后预约，再发送”的原则来尽量避免冲突，其核心流程可以概括为以下几个关键步骤和机制。
- *载波侦听*：站点在发送数据前，会持续监听信道，通过能量检测（ED）、载波检测（CS）或混合检测三种方式来判断信道忙闲。
  - 能量检测: 通过测量接收信号的能量强度来判断信道忙闲。	实现简单，无需知道信号的先验信息，通用性强，但无法区分信号类型（是Wi-Fi数据还是噪声），在信噪比较低时容易误判。
  - 载波检测: 通过识别特定的信号特征（如导频或前导码）来判断是否有有效载波存在，能够准确识别出同类型的合法信号，抗干扰能力强。只能检测已知格式的信号，如果信号格式不匹配或强度很弱，即使信道已被占用也可能检测不到。
  - 混合检测: 结合了能量检测和载波检测两种方式，综合进行判断，	提高了检测的准确性和可靠性，在不同环境下表现更稳健。	实现起来相对复杂，对硬件要求较高。
- *帧间隔（IFS）*：即使侦听到信道空闲，站点也不会立即发送，而是等待一段特定的时间。协议定义了不同长度的帧间隔（如DIFS、SIFS），为不同类型的数据帧分配优先级。例如，确认帧（ACK）使用最短的SIFS，从而拥有最高的发送优先级。
  - DIFS(分布式帧间间隔): 异步数据服务。任何一个站点想要发送新数据前必须经历的“准备竞赛”阶段。如果信道从忙变为空闲，站点不会立即发送，而是必须等待一个完整的DIFS时长。这个等待的目的，就是给那些使用SIFS的高优先级帧（如ACK、CTS）一个“插队”的机会。如果在DIFS期间信道一直空闲，说明没有更高优先级的通信需要立即进行。
  - PIFS（点协调帧间间隔）：轮询服务。时长居中，专为接入点（AP）设计。在点协调功能（PCF）模式下，AP通过等待PIFS而非更长的DIFS，总能比普通站点更早地访问信道，从而优先发起无竞争轮询，为需要保证服务质量的通信提供支持。
  - SIFS(短帧间间隔): 轮询服务。确保一个帧交换序列能够不被中断地连续进行。
#table(
  columns: 4,
  align: center + horizon,
  inset: 5pt,
  stroke: 0.5pt,
  // 表头
  [*特性*], [*SIFS**(短帧间间隔)*], [*PIFS**(点协调帧间间隔)*], [*DIFS**(分布式帧间间隔)*],
  // 表格内容
  [核心功能], [用于高优先级、需立即响应的帧交换], [用于点协调功能(PCF)模式下，接入点(AP)优先获得媒体访问权，以发起无竞争期(CFP)], [用于异步数据帧和管理帧的竞争传输起始阶段],
  [时长与优先级], [最短的间隔*(如802.11b为10μs)*,拥有最高优先级], [中等长度间隔*(如802.11b为30μs)*优先级介于SIFS与DIFS之间], [最长的基本间隔*(如802.11b为50μs)*拥有最低优先级],
  [主要应用场景], [ACK确认帧、CTS清除发送帧、数据分片后续帧], [PCF模式下AP发送的信标帧、轮询帧等；信道切换公告帧], [站点准备发送新的数据帧或管理帧时的初始等待],
  [计算关系], [基础值 (aSIFSTime)], [PIFS = SIFS + Slot Time], [DIFS = SIFS + 2 × Slot Time]
)
#index[SIFS]#index[PIFS]#index[DIFS]

- *随机回退（二进制指数退避）*：在DIFS等待结束后，站点还会选择一个随机时隙进行回退等待。这个随机值减少了多个站点同时等待信道空闲后又同时发送数据的可能性。
- *确认（ACK）*：接收方在成功接收数据帧后，会等待一个SIFS间隔，随即向发送方回复一个确认帧。如果发送方在规定时间内没有收到ACK，就会判定数据发送失败（可能因冲突导致），并准备重传。

为了解决“隐藏终端”问题，提出了RTS/CTS(请求发送/清楚发送)四步握手流程来解决这个问题: 
+ *请求发送（RTS）*：当发送方（如设备A）有数据要发送时，它首先向接收方（如路由器B）发送一个很短的RTS控制帧。这个帧中包含了一个信息，指明这次通信（包括后续的CTS、数据和ACK确认）总共需要占用信道多长时间。#index[RTS]
+ *清除发送（CTS）*：接收方B在收到RTS帧后，会回复一个同样很短的CTS控制帧。这个CTS帧有两个关键作用：一是明确允许A发送数据；二是作为一个广播信号，通知B周围所有能“听到”它的设备（包括那个A看不到的C）：“信道即将被占用，请大家暂时安静。”#index[CTS]
+ *数据传输*：A在收到CTS帧后，便知道信道已经预约成功，可以开始发送完整的数据帧了。
+ *确认（ACK）*：B在成功接收数据后，会向A回复一个ACK确认帧，表示传输成功完成。

在这个过程中，尽管设备C听不到A发送的RTS帧，但它能接收到B广播的CTS帧。C在听到CTS后，会启动自己的网络分配向量（NAV）——一个内部倒计时器。在NAV超时前，C会认为信道是“忙”的，从而抑制自己的发送行为，这样就避免了与A的数据发生碰撞。#index[NAV]

无线传输由于信道干扰，信号衰减与衰落和节点的移动性，误码率远高于稳定、封闭的有线信道。采用较小的帧（即帧分段）来应对高误码率，是一种“化整为零、各个击破”的策略。其有效性体现在两个层面：减少重传成本和提升信道利用率。所谓“*对用户透明*”，是指这一切分段和重组操作都在网络底层（数据链路层）完成，上层的应用程序和用户完全感知不到这个过程，却享受到了其带来的可靠性和效率提升。

#remark[
  在长肥链路中，停等机制存在较大的性能问题，但是无线局域网中的RTT较小，使用停等机制对性能影响不明显。停等机制实现简单，流水线机制复杂。
]

#figure(image("../fig/无线网络中基于RTSCTS的信道访问与NAV设置时序图.png", width: 70%), caption: "CSMA/CA 与 RTS/CTS 握手机制时序图")

=== 802.11帧格式一般结构
#index[802.11]
#figure(image("../fig/802.11帧格式一般结构.png", width: 70%), caption: "802.11帧格式一般结构")

#table(
  columns: 2,
  align: left,
  inset: 5pt,
  stroke: 0.5pt,
  // 表头
  [*字段名称*], [*含义与作用*],
  // 帧控制字段及其子字段
  [*帧控制*], [包含多个子字段，用于控制帧的基本行为：\
  • 协议版本：通常为0\
  • 类型和子类型：标识帧类型（管理/控制/数据），子类型最后一位为1表示QoS数据帧\
  • 更多段：1表示长帧分段的非最后一段\
  • 重传：1表示当前帧是重传帧\
  • 电源管理：1表示发送方进入节能模式\
  • 更多数据：1表示有更多缓存数据待发送\
  • 安全保护：1表示使用802.11标准安全机制\
  • 顺序：1表示接收方需严格按序处理],
  [*持续时间*], [表示下一个帧可能持续的传输时间（用于NAV），或作为关联ID（AID）],
  [*地址1-地址4*], [MAC地址字段，具体含义由"去往DS"和"来自DS"标志位决定，可能包含：目的地址、源地址、接收地址、发送地址],
  [*顺序控制*], [用于过滤重复帧，以及在分片传输时进行帧的组合与排序],
  [*QoS控制*#index[QoS]], [存放数据流的服务质量信息（802.11e中扩展），支持优先级和流量类别],
  [*数据*], [有效载荷部分，包含实际传输的数据，长度可变（0-2312字节）],
  [*CRC校验*], [循环冗余校验码（4字节），用于检测传输过程中的数据错误]
)

#table(
  columns: 3,
  align: left,
  inset: 5pt,
  stroke: 0.5pt,
  // 表头
  [*特性*], [*BSSID (基本服务集标识符)*], [*IBSSID (独立基本服务集标识符)*],
  // 表格内容
  [*网络模式*], [基础架构模式 (Infrastructure Mode)], [自组织模式 (Ad-Hoc Mode)],
  [*核心设备*], [必须有接入点 (Access Point, AP)], [无中心AP，设备间直接通信],
  [*实际是什么*], [就是AP的无线MAC地址], [一个随机生成的、本地管理的MAC地址],
)

#index[BSSID]
#index[IBSSID]
#index[AP]
#table(
  columns: 7,
  align: center + horizon,
  inset: 5pt,
  stroke: 0.5pt,
  // 表头
  [*说明*], [*去往DS*], [*来自DS*], [*地址1* #linebreak() *(物理接收者)*], [*地址2* #linebreak() *(物理发送者)*], [*地址3* #linebreak() *(逻辑发送者)*], [*地址4* #linebreak() *(逻辑接收者)*],
  // 表格内容
  [自组织模式], [0], [0], [DA], [SA], [IBSSID], [—],
  [接收自AP], [0], [1], [DA], [BSSID], [SA], [—],
  [发送至AP], [1], [0], [BSSID], [SA], [DA], [—],
  [AP到AP], [1], [1], [接收AP], [发送AP], [SA], [DA]
)

#remark[为什么自组织模式下地址3是IBSSID而不是SA][
  自组织网络的本质：它是一个没有中心协调者的、临时的对等网络。设备间直接通信：地址1（物理接收者）已经是DA，地址2（物理发送者）已经是SA。通信的端点信息已经完全具备。需要一个新的网络标识符：那么，当设备收到一个数据帧时，它如何判断这个帧是否是发给它所在的这个“临时群组”的呢？它需要一个网络名称来过滤信息。这就是IBSSID的作用！它是一个随机生成的、代表这个临时网络的MAC地址。当设备收到一个帧时，它会检查帧中的地址3字段：
  - 如果与自己所处的临时网络的IBSSID匹配，就接受并处理。
  - 如果不匹配，就忽略它（因为可能是邻近的另一个自组织网络发的帧）。
]

#table(
  columns: (2.5cm, 3cm, 12.5cm),
  align: (left, left, left),
  [场景], [空缺字段], [解释],
  [自组织模式 (IBSS)], [地址4 (逻辑接收者)], [在自组织网络中，设备间直接通信，无需中转。地址1 (DA)已经指明了物理接收者，也就是逻辑接收者；地址2 (SA)已经指明了物理发送者，也就是逻辑发送者。端到端的通信双方已经由地址1和地址2完全标识清楚，因此不再需要地址4来重复指明逻辑接收者。而地址3被用于存放IBSSID（这个临时网络的标识符），以确保设备能识别出这个数据帧是属于当前这个“群组”的。],
  [接收自AP], [地址4 (逻辑接收者)], [在这个场景下，数据是从分布式系统（如互联网）通过AP发往无线设备（如你的手机）。地址1 (DA)是目标设备（你的手机），也就是逻辑接收者。地址3 (SA)是原始数据来源（如互联网上的服务器），也就是逻辑发送者。同样，逻辑上的发送和接收方已经被地址1和地址3明确。地址2 (BSSID)用于指明是哪个AP在发送。此时，逻辑接收者（DA）的信息已经由地址1承载，因此地址4字段为空缺，避免信息重复。],
  [发送至AP], [地址4 (逻辑发送者)], [特别注意，这里地址3表示逻辑接受者。AP 需要知道这个数据帧最终要转发给谁（例如，有线网络上的路由器、服务器、或互联网上的某个主机）。AP 会使用 Address 3 来决定将该帧从 DS 发送到哪里。因为地址 2 已经包含逻辑发送者，所以逻辑发生者被省略。],
  [AP到AP], [无], [这是最复杂的场景，通常用于无线分布式系统（WDS）或Mesh网络，数据需要在多个AP之间进行接力传输。地址1和地址2表示当前这一跳的物理接收AP和物理发送AP。为了在整个多跳传输过程中不丢失数据的最终来源和最终目标，必须同时保留最原始的“逻辑发送者(SA)”和“逻辑接收者(DA)”。因此，地址3被用来存放SA（最初发送数据的客户端地址）。地址4被用来存放DA（最终要接收数据的客户端地址）。所有四个地址字段都被充分利用，所以没有空缺。],
)
== 数据链路层交换
数据链路层的核心任务是在直接相连的网络节点间提供可靠的数据传输。其“交换”的本质, 是设备 (主要是二层交换机) 根据数据帧中的MAC (媒体访问控制) 地址, 做出转发决策的过程。
=== 数据链路层交换原理
交换机内部维护一张关键的 MAC地址表, 它记录了MAC地址与交换机端口的对应关系。其工作流程可以概括为三个核心动作: 
+ 学习: 当交换机从一个端口收到数据帧时, 它会查看该帧的源MAC地址, 并将这个地址与接收端口关联起来, 记录到MAC地址表中。这个过程是自动进行的, 因此交换机被称为“即插即用”设备。
+ 转发/过滤: 接着, 交换机会查看数据帧的目的MAC地址, 并在MAC地址表中进行查找。如果找到明确的对应端口, 并且这个端口不是接收端口本身, 交换机就会将数据帧仅转发到那个特定端口。这样, 数据只会送达目标设备, 不会影响其他端口, 这就是“过滤”带来的效率提升。
+ 泛洪: 如果目的MAC地址在地址表中找不到, 或者数据帧是一个广播帧 (目的地址为FF:FF:FF:FF:FF:FF) , 交换机就会将这个帧从除接收端口之外的所有其他端口发送出去。这种广播行为确保了即使交换机不知道目标在哪里, 数据也有机会到达目的地。

#remark[
  网桥或交换机都可以隔离冲突域, 但是集线器hub不可以。
]
理想的网桥是透明的。即插即用, 无需任何配置。网络中的站点无需感知网桥的存在与否。


MAC地址表中的条目通常有一个老化时间 (默认一般为300秒) , 如果一段时间内没有再次收到某个MAC地址发出的帧, 对应的表项会被自动删除。这样可以适应网络拓扑的变化, 比如设备更换了端口。
=== 链路层交换机
链路层交换机 (通常直接称为交换机) 是数据链路层交换原理的物理载体。与工作在物理层、只是简单广播信号的集线器 (Hub) 相比, 交换机带来了革命性的变化。



交换机的核心价值在于: 
- 隔离冲突域: 交换机的每个端口都是一个独立的冲突域。这意味着连接在不同端口上的设备可以同时进行通信, 而不会产生数据碰撞, 极大地提升了网络总体带宽和效率。
- 支持全双工通信: 由于冲突被消除, 交换机端口可以同时进行数据的发送和接收, 进一步提高了吞吐量。
- 多种帧转发方式: 为了在延迟和可靠性之间取得平衡, 交换机可能采用不同的转发策略: 
  - 存储转发: 最常用的方式。交换机先接收并缓存整个数据帧, 进行差错校验 (如检查帧尾的FCS) , 确认无误后再转发。可靠性高, 但延迟稍大。
  - 直通转发: 交换机只读取到目标MAC地址后, 就立即开始转发。延迟极低, 但可能转发错误的或有残缺的帧。
=== 生成树协议

#index(initial: "S")[生成树协议]
在网络中引入冗余链路是提高可靠性的基本手段, 但这会在数据链路层形成环路。环路会导致可怕的“广播风暴” (广播帧在网络中无限循环) 、MAC地址表震荡等问题, 使网络迅速瘫痪。生成树协议 (STP, IEEE 802.1D) 就是为了解决这个矛盾而生的。它的核心思想是: 在存在物理环路的网络中, 通过软件计算, 逻辑上阻塞一些端口, 从而构建一个无环的树形拓扑。当活动链路发生故障时, STP会重新计算, 激活之前阻塞的备用链路, 实现冗余备份

STP的运作包括以下几个关键步骤: 
+ 选举根桥: 网络中所有运行STP的交换机通过交换BPDU (桥协议数据单元) , 选举出一台唯一的根交换机。选举依据是桥ID (由桥优先级和MAC地址组成) , 桥ID最小的成为根桥。
+ 选举根端口: 在每台非根交换机上, 选择一个到根桥路径开销最小的端口作为根端口。
+ 选举指定端口: 在每个物理网段上, 选择一台负责向根桥方向转发数据的交换机端口, 即指定端口。最终, 根桥的所有端口都是指定端口, 而每个网段只有一个指定端口, 阻塞其他端口以消除环路。

STP的端口需要经历一系列状态变迁 (阻塞、监听、学习、转发) , 这个过程收敛较慢 (传统STP可能需要30-50秒) 。后续的快速生成树协议 (RSTP, IEEE 802.1w)  对此做了大幅改进, 实现了秒级的快速收敛
=== 虚拟局域网

#index[VLAN]
VLAN 并不是一种新的局域网技术, 而是在现有以太网基础上, 通过交换机软件提供的一种“虚拟化”服务,  用于将一个物理上的局域网划分成多个逻辑上独立的广播域。一个以太网是一个广播域, 当一个以太网中包含的计算机太多时, 往往会导致: 
- 以太网中出现大量的广播帧, 特别是经常使用ARP, DHCP协议(网络层的协议)
- 一个单位不同部门共享一个局域网, 对信息保密安全不利。

VLAN技术允许网络管理员将一个物理的局域网逻辑地划分成多个广播域。每个VLAN就像一个独立的虚拟交换机, 其广播流量只会在本VLAN内传播, 不会扩散到其他VLAN。VLAN建立在交换技术的基础上, 以软件方式实现逻辑分组与管理。

VLAN的划分方式有多种: 
- *基于端口*: 最常用、最简单的方式。直接将交换机的某些端口指定属于某个VLAN。
- *基于MAC地址*: 根据终端设备的MAC地址来划分VLAN。设备移动位置时VLAN成员身份不变, 但配置管理复杂。
- *基于网络层协议*: 根据数据帧承载的网络层协议类型 (如IPv4、IPv6、IPX等) 来划分VLAN。。灵活性高, 但处理效率较低。
- *基于IP子网的VLAN*: 根据设备的IP地址所属的子网来划分VLAN。交换机检查数据帧中的IP包头, 根据其源IP地址判断它属于哪个子网, 从而将其归入对应的VLAN。

#align(center, table(
  columns: 3,
  align: center + horizon,
  stroke: black,
  table.header(
    [*项目*], [*广域网*], [*局域网*]
  ),
  [覆盖范围], [很广, 通常跨区域], [较小, 通常在一个区域内],
  [连接方式], [通常采用点对点连接], [普遍使用广播信道],
  [OSI参考模型层次], [三层: 物理层, 数据链路层, 网络层], [两层: 物理层, 数据链路层],
  [着重点], [强调资源共享], [强调数据传输]
))
1. 广域网和局域网都是互联网的重要构件, 从互联网的角度看, 二者平等 (不是包含关系) \
2. 当连接到一个广域网或一个局域网上的主机在该网内进行通信时, 只需要使用其网络的物理地址

为了实现跨交换机的VLAN通信, 需要一种方法来标识每个数据帧属于哪个VLAN。这就是 IEEE 802.3ac (定义扩展) / 802.1Q (具体协议) 标准的作用。

#figure(image("../fig/VLAN帧.png", width: 80%), caption: "插入VLAN标签后变成了802.1Q帧")

*VLAN标签 (VLAN Tag)*: 在标准的以太网帧的源MAC地址字段和类型字段之间, 插入一个4字节的标识符, 这个带有标签的帧就叫做 802.1Q帧。因为增加了4字节的标签, 以太网帧的最大长度从标准的1518字节变为1522字节。
  - *TPID (标签协议标识符)*: 前2字节, 固定为 0x8100, 用来告诉接收设备“这是一个802.1Q帧”。
  - *TCI (标签控制信息) *: 后2字节, 其中最重要的12位是 VID (VLAN标识符) 。VID唯一地标识了该帧属于哪个VLAN (例如, VID=10代表VLAN-10) 。12位的VID最多可以支持4094个VLAN (0和4095保留) 。

#remark[
  - 带VLAN标记的帧称为标记帧 (Tagged Frame) 
  - 不携带VLAN标记的普通以太网帧称为无标记帧 (Untagged Frame) 
]
为了实现VLAN间的通信, 必须借助第三层 (网络层) 的路由功能。这可以通过外接路由器或使用三层交换机来实现。

- 接入链路: 连接计算机和交换机端口的链路。在接入链路上传输的是标准的以太网帧, 计算机本身并不知道VLAN的存在。
- 汇聚链路/干线链路: 连接两个支持VLAN的交换机的链路。在汇聚链路上传输的必须是802.1Q帧, 这样对端交换机才能识别帧属于哪个VLAN。
- 交换机的角色: 交换机是VLAN的核心。它知道每个端口属于哪个VLAN (由管理员配置) 。当需要跨交换机转发帧时, 交换机会负责“打标签”和“去标签”。
  - 同一VLAN, 同一交换机内通信
    - 交换机收到标准以太网帧, 查询目的MAC地址, 发现目标主机在同一台交换机且属于同一个VLAN, 则直接像普通交换机一样进行转发。*全程不涉及VLAN标签。*
  - 同一VLAN, 跨交换机通信
    - 打标签: 交换机1发现目标主机在另一台交换机上, 在将帧发送到汇聚链路前, 会根据目标VLAN插入对应的VLAN标签, 形成802.1Q帧。
    - 跨链路传输: 802.1Q帧通过干线链路到达交换机2。
    - 去标签: 交换机2读取VID, 确认目标VLAN后, 在将帧通过接入链路发送给E站之前, 移除VLAN标签, 还原成标准以太网帧。因此, *计算机始终接收和发送标准帧, 感知不到VLAN标签的存在。*
  - 不同VLAN之间通信
    - 即使它们连接在同一台交换机上, 但因为属于不同的VLAN (即不同的逻辑广播域) , 二层交换机默认无法直接转发。
    - 必须通过第三层 (网络层) 设备, 通常是路由器或具有路由功能的三层交换机, 来实现VLAN间的通信。


VLAN的优势非常明显: 
- *控制广播风暴*: 将大型局域网划分为小的VLAN, 有效限制了广播范围, 节约了带宽。
- *增强安全性*: 不同VLAN间的设备在二层是隔离的, 如需通信必须经过第三层设备, 可以在此实施访问控制策略。
- *简化网络管理*: 可以依据部门或功能而非地理位置来组建网络, 当设备物理位置变动时, 只需调整其连接的端口所属的VLAN即可, 管理更灵活。
