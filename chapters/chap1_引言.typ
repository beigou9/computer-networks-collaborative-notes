#import "../note.typ": *
= 引言
== 网络的概念
计算机网络——“自主计算机的互连集合”。网络(network)由若干节点(node)和连接这些节点的链路(link)组成。 网络把许多计算机连接在一起, 向用户提供最重要的功能: *连通性(connectivity)和资源共享(resource sharing) *。

计算机网络是一些互相连接的、自治（不是主从关系）的计算机的集合。因特网(Internet)是“网络的网络”。连接在因特网上的计算机都称为*主机(host)*。

#remark[
    - *互联网 (Internet) *特指遵循 TCP/IP 标准、利用路由器将各种计算机网络互连起来而形成的、覆盖全球的、特定的互连网, 是一个专用名词。
    - *互连网 (internet) *泛指由多个不同类型计算机网络互连而成的网络, 除 TCP/IP 外, 还可以使用其他协议, 是一个通用名词。

    #unim[自己写 FPGA 之间的通信就算 internet 而非 Internet。]
]

== 网络分类
=== 按*拓扑结构*分
星型、总线型、环型、树型、网状 (任一结点至少与其它两个结点直接相连)
#figure(
    image("/assets/image.png", width: 50%),
    caption: [思考题:分析拓扑结构的优劣问题，和广播数据时的传输效率。拿环形网络为例子，为了让广播数据的效率更高，考虑一台机器A，它把数据依次顺着环形网络传输到其后继，最终由A本身回收。],
)

=== 按*地域规模*

- 个域网PAN ( Personal Area Network ) : 能在便携式消费电器与通信设备之间进行短距离通信的网络, 覆盖范围一般在10米半径以内, 如蓝牙耳机等。
- 局域网LAN (Local Area Network) : 局部地区形成的区域网络, 如企业网络, 分布地区范围有限, 可大可小, 大到一栋建筑、小到办公室内的组网。电脑WLAN接入，打印机共享等等
- 城域网MAN (Metropolitan Area Network ) 范围覆盖一个城市的网络
- 广域网WAN (Wide Area Network) 覆盖很大地理区域, 乃至覆盖地区和国家
#figure(image("/assets/image-4.png"))
=== 按*用途*
+ 传输网
    - 骨干网络（backbone），承载两个端点间传输流量的角色（有偿使用）
    - 通常内容和应用托管在数据中心网络，用户通过接入网访问这些内容，传输网实现从数据中心到接入网的连接
+ 企业网
    - 实现机构内计算机之间的资源共享（resource sharing）
    - 异地采用VPN（Virtual Private Network）连接成一个逻辑网络
    - 为员工间提供功能强大的通信媒介（communication medium），实现电子邮件（E-mail）、IP电话/语音、桌面共享（desktop sharing）等服务


=== 按*网络技术*
==== 非交换网络 (一跳通信)
- 点-点信道型网络: 每个信道连接一对节点。非相邻节点之间的通信需通过其它节点转发。星型、环型、树型、网格型。
- 共享信道型 (多点信道型) 网络: 所有节点共享一条信道, 可以看成都相邻。每个节点所发送的信息可被所有节点收到。总线型、卫星广播网。
==== 交换网络 (多跳通信) :
1. *电路交换(Circuit Switching)*: 数据传输前需要建立一条端到端的通路。呼叫——建立连接——传输——挂断。
#figure(image("/assets/image-1.png"), caption: [
    - 空分电路交换:每个用户一条传输信道;
    - 时分电路交换:多个终端共接于一条公用通信线路，划分时间片进行线路连接
])
- 建立连接的时间长；
- 一旦建立连接就独占线路, 线路利用率低；
- 无纠错机制；
- 建立连接后, 传输延迟小。
2. *报文交换(Message Switching)*:将用户要发送的完整数据块 (即“报文”) 作为一个整体, 通过*“存储-转发”*的方式, 从源节点传输到目的节点。#index(initial: "B")[报文交换]
#figure(image("/assets/image-2.png"))
- *整体发送*: 要传输的数据 (比如一个文本文件、一张图片) 被当作一个完整的、独立的逻辑单元——“报文”。报文的开头包含目的地址等控制信息。
- *存储*: 网络中的每个交换节点 (如路由器或专门的报文交换局) 收到一个报文后, 并不会立刻发送出去, 而是先将整个报文完整地存储在自己的缓冲区 (如大容量磁盘) 中。
- *转发*: 交换节点会根据报文中的目的地址, 为它选择一条通往下一个节点的最佳路径。当这条路径空闲时, 才将报文发送出去。
- *报文大小灵活*: 报文可以很长, 理论上允许传输整个文件, 无需在发送端进行分割。
- *线路利用率高*: 采用存储-转发机制, 多个报文可以共享信道。不同报文可以分时在同一链路上传输, 避免了电路交换中通道独占的浪费。
- *支持速率和格式转换*: 不同的交换节点之间可以有不同的传输速率和数据格式, 因为每个节点都有存储和处理能力。
- *缓冲区管理复杂*: 因为报文“大小不一”, 交换节点需要准备大小灵活的缓冲区来存放各种长度的报文, 这给内存管理带来了复杂性。
- *传输时延长*: 这是报文交换最显著的缺点。因为每个节点都要等整个报文全部接收完毕后才开始转发, 这个“存储”时间加上排队等待“转发”的时间, 会导致端到端的时延很大, 且不确定 (时延抖动大)
- *出错重传效率低*: 在传输过程中, 如果报文有一位出错, 接收方会要求发送方整个报文全部重发。对于一个大报文来说, 这无疑是低效的。
3. *分组交换(Packet Switching)*:在发送数据之前, 先将一个完整的“报文”分割成多个大小固定、较小的数据包, 每个数据包称为一个“分组”。然后以组为单位转发形成*流水线*各自通过网络被传送到目的地, 到达后再重新组装成原始报文。
    #figure(
        image("/assets/image-3.png", width: 40%),
        caption: [一个关键结构: 分组 = 首部 + 数据#index(initial: "F")[分组交换]],
    )
    - *首部 (Header) *:  包含了至关重要的控制信息, 如*目的地址、源地址、序号*等。没有首部, 网络中的交换机就不知道这个分组该发往何处。
    - *数据 (Data)* : 是实际要传输的用户数据的一部分。
    #note[
        特别注意, 这里是用户数据的一部分, 而不是全部的用户数据, 这就是分组交换的核心。

        这里本质上是一个“把大数据切成多个小包”.并且注意小包的个数是向上取整的.
    ]
    - 工作原理
        + 分组化: 在发送端, 将原始报文分割成多个分组。
        + 独立传输: 每个分组带上自己的“地址” (首部信息) , 独立进入网络。
        + 存储-转发: 网络中的每个结点交换机 (如路由器) 收到一个分组后, 先存储下来, 然后根据其首部中的地址, 选择最佳路径转发给下一个结点。
        + 重组: 所有分组最终到达目的地后, 接收端根据分组中的序号等信息, 将它们重新组装成原始报文。
        - *缓冲要求小, 速度快*: 分组大小固定且较小, 交换机可以用高速的内存 (而非硬盘) 来缓存, 极大提高了处理速度。
        - *转发时延小*: 交换机不必像报文交换那样等待整个大报文接收完再转发。它收到一个分组的头部, 知道了路径, 就可以立即开始转发这个分组, 这大大降低了网络时延, 适合电话、视频等交互式通信。
        #note[
            特别注意, 分组交换在计算中, 单独计算第1组的传播, 其他组就不用再计算传播时延了。
        ]
        #example[
            一个大小为 $L$ 的消息被分成 $N$ 个数据包，每个大小为 $P$(所以 $L= N times P$)。它通过一个有 $k$ 条链路的路径发送，每条链路的传输速率为 $R$, 传播延迟为 $d_"prop"$。忽略处理延迟, 计算分组交换的大致端到端延迟?

            第一个分组到达目的地时间:
            $
                T_"first"=k dot P/R+k dot d_"prop"
            $
            总时间为：
            $
                T_"total" & =T_"first"+(N-1)dot P/R \
                          & =(k+N-1)dot P/R+k dot d_"drop"
            $
            特别注意答案不是
            $
                N dot k dot (P/R+d_"drop")
            $
        ]
        - *重发效率高*: 传输过程中若某个分组出错或丢失, 只需重传那一个小的分组即可, 而不必重传整个报文, 效率极高。
    #example[
        如下图所示,主机 A 和 B 都通过 10Mb/s 的链路连接到交换机 S。每条链路上的传播时延都是 20μs。S 是一个存储转发设备, 它在接收完一个分组 35μs 后开始转发收到的分组。将 10000 比特作为两个 5000 比特的分组一个紧接着另一个发送, 计算从 A 发送到 B 所需的总时间。

        #figure(image("../fig/甘特图解法.png", width: 80%), caption: "甘特图解法")
        所以答案是$1575 mu s$.
    ]
==== 实现分组交换的两种方法
- *数据报 (Datagram)* : #index(initial: "S")[数据报]#index[Datagram]：每个分组“各走各的”。
    - *无需建立连接*: 通信前不需要先建立一条专门的通路
    - *独立寻路*: 每个分组都自带完整的目的地址, 独立地在网络中为自已寻找路径。因此, 分组可能不会按序到达目的地。
    - *健壮性强*: 如果某条链路出现故障, 后续分组可以自动选择其他路径, 对网络故障适应力强。
    - 典型代表: 互联网 (Internet) 本身就是基于数据报方式的。
- *虚电路 (Virtual Circuit)* #index(initial: "X")[虚电路]：先确定一条路，之后的分组“都走这条路”。
    - *必须建立连接*: 在发送数据前, 需要先发送一个“呼叫请求”分组, 建立一条逻辑上的连接 (即“虚电路”) 。这条路是预先计划好的。
    - *按序到达*: 所有分组都沿着这条预先建立的虚电路传输, 因此能保证按顺序到达目的地。
    - *时延较小*: 因为路径是预先确定的, 中间节点无需为每个分组进行复杂的路由选择, 处理速度更快, 时延更稳定，保证了通信质量。
    - 典型代表: 早期的X.25网络、帧中继 (Frame Relay) 等。

==== 分组交换带来的问题
- *时延*: 分组在结点交换机处需要排队等待被转发。如果网络繁忙, 排队时间就会变长, 导致时延不确定 (时延抖动) 。这对于实时性要求高的应用 (如网络电话、在线游戏) 是个挑战。
- *开销*: 每个分组都必须携带一个首部, 这些首部信息是额外的开销。虽然一个分组的首部不大, 但海量的分组累加起来, 这些控制信息会占用大量的网络带宽, 降低了传输用户数据的效率。
#figure(image("../fig/3种交换的比较.png", width: 80%), caption: "3种交换的比较")
#remark[
    也有混合交换 (同时采用电路交换和分组交换)
]

#example[
    *这里我们通过一道题来学习一下两种解法。*但是这只适用于数据传输速度一致的情况下, 否则请优先考虑第2种解法。

    一个分组交换网采用虚电路方式转发分组, 分组的首部和数据部分分别为$h$位和$p$位。现有$L$($L >> p$且$L$为$p$的倍数)位的报文通过该网络传送, 源点和终点之间的线路数为$k$, 每条线路上的传播时延为$d$秒, 数据传输速率为$b "b/s"$, 虚电路建立连接的时间为$s$秒, 每个中间结点有$m$秒的平均处理时延。求源点开始发送数据直至终点收到全部数据所需的时间。

    *思路一: 流水线/分组逐个分析*, 计算​第一辆车从启动到通过所有收费站的时间 + 后续车辆以流水线方式陆续通过的时间。

    - 源点需要花费时间将第一个分组完全推上第一条链路, 这个时间是: $(h+p)/b s$。
    - 当第一个分组的最后一个比特还在链路上向第一个中间结点传播时, 源点已经可以开始发送第二个分组了, 此时就开始了并行。
    - 当第一个分组到达第一个中间结点后, 该结点需要:
        - 接收整个分组（存储）。这个接收时间等于该分组的发送时延 $(h+p)/b$。请注意: 这个接收过程与结点处理上一个分组、以及源点发送后续分组是并行的。​
        - 处理分组 (检错、查虚电路表) , 耗时 $m$秒。
        - ​转发分组（发送）到下一段链路。这个“发送”过程, 就是中间结点的发送时延，它同样等于 $(h+p)/b$秒。这个时间必须计算, 因为它代表了分组“占用”下一段链路资源的时间。
    所以总时间是
    #align(center)[*虚电路建立时间​ + ​第一个分组从源点到达终点的时间​ + ​剩余所有分组被目的地接收完毕时间*]
    + 虚电路建立时间​: $s$秒。
    + 第一个分组的端到端时延​: $k dot (h+p)/b + k d+ (k-1)m$
    + 剩余分组的到达时间: $(L/p -1)dot (h+p)/b$
    + 总时间为: $s+(k + L/p -1)dot (h+p)/b + k d + (k-1)m$

    *思路二：总体积/总工作量分析*, 计算​将整个车队看作一个整体。计算点是车队的“车尾”从起点启动，到车队的“车尾”通过最后一个收费站所需的时间。

    整个传输过程的总时延是
    #align(center)[
        *建立连接时延+源点发送时延+中间节点发送时延+中间结点的处理时延+传播时延*
    ]
    分别是: $s$, $(h+p)L /(p b)$, $(h+p)(k-1)/b$, $m(k-1)$, $k d$.

    注意到, 这里计算的中间节点发送时延, 处理时延, 传播时延只计算了1个分组。
]

== 网络实例
=== Internet互联网(构成)
TODO:客户端、对等连接
1. 网络边缘：
端系统：位于互联网边缘与互联网相连的计算机和其他设备。由各类主机(host)构成：桌面计算机、移动计算机、服务器、其他智能终端设备
2. 网络核心：由各类交换机 (路由器) 和链路, 构成的网状网络, 目标是将海量的端系统互联起来。
网络核心的两大功能
- *路由*
    - 性质: 全局操作
    - 定义: *确定*数据分组从源地址到目标地址所采用的端到端*完整路径*。
    - 实现方式: 通过网络中的路由协议和路由算法进行计算, 最终生成路由表。
    - 核心任务: 解决“哪条路是去往目的地的最佳路径？”的问题。
- *转发*
    - 性质: 本地操作
    - 定义: 指*单个网络设备* (如路由器或交换机) 将从一个输入接口接收到的数据分组, 通过某个输出接口发送出去*的动作*。
    - 实现方式: 设备查看接收到分组的分组头中的目的地址, 然后查询本地路由表, 从而确定数据分组的输出接口。
    - 核心任务: 解决“这个到达的数据包, 下一步该从我的哪个端口发出去？”的问题。
== 协议与分层结构
=== 协议设计目的
为进行网络中的数据交换而建立的规则、标准 或约定, 即网络协议(network protocol), 通信双方需要共同遵守, 互相理解。

语法: 规定传输数据的格式 (如何讲) 、语义: 规定所要完成的功能 (讲什么) 、时序: 规定各种操作的顺序 (双方讲话的顺序) 。

目的: *可靠性、资源分配、拥塞问题、自适应性、安全问题*。

=== 基本概念
- 层次栈: 网络协议被组织成一堆层次, 每一层都建立在下层之上。
- 对等实体: 不同计算机上位于同一层的功能单元 (如两台电脑的“网络层”) , 它们之间的通信遵循共同的规则。
- 接口: 相邻层次之间交互的约定, 定义了下层为上层提供哪些服务。
#remark[服务与协议的关系][
    - *服务是垂直的*: 定义了层与层之间如何交互 (上层使用下层的服务) 。
    - *协议是水平的*: 定义了不同机器上对等实体之间通信应遵循的规则。协议是实现服务的工具。
]

=== 通信过程
#figure(image("../fig/分层模型通信过程.png", width: 80%), caption: "分层模型通信过程")
通信过程: 封装与解封装

- *发送端 (封装)* : 数据从应用层 (第5层) 开始, 自上而下传递。每一层都在收到的数据前加上本层的控制信息 (称为“头部”, 如H5, H4) , 这就是封装。
- *接收端 (解封装)* : 数据自下而上传递。然后将剩余数据传递给上层, 这就是解封装。
- *协议数据单元*: 在每一层, 数据包都有特定的名称 (如报文、分组、帧) , *统称为PDU*。

#align(center, table(

    columns: (auto, auto, auto),
    align: (center, center, center),
    table.header([*所属层*], [*协议数据单元( PDU) 名称*], [*核心功能与特点*]),
    [*应用层*],
    [报文],
    [在应用层, PDU通常被称为报文或消息, 如HTTP请求报文、DNS查询报文、SMTP电子邮件报文。这是用户真正关心的数据。],

    [*传输层*],
    [报文段],
    [传输层( 如TCP) 将应用层报文分割成更小的块, 每个块加上TCP头部( 含端口号、序列号等) , 形成报文段。如果是UDP, 则称为数据报。],

    [*网络层*],
    [分组 / 数据报],
    [网络层( 如IP) 将传输层的报文段加上IP头部( 含源/目的IP地址) , 形成分组或数据报。这是分组交换网络的核心概念。],

    [*数据链路层*],
    [帧],
    [数据链路层( 如以太网、Wi-Fi) 将网络层的分组加上帧头部( 含MAC地址) 和帧尾( 用于差错检测的校验和) , 形成帧。帧是在物理链路上传输的基本单位。],

    [*物理层*], [比特流], [物理层不区分数据内容, 它将帧转换为可通过物理介质( 如网线、无线电波) 传输的0和1比特流。],
))


=== 服务类型
- *面向连接服务*
    - 类比: 打电话。通信前必须先建立连接 (拨号、振铃、接听) , 通信过程有问有答 (确认机制) , 结束后释放连接 (挂断) 。
    - 特点: *可靠、有序*。适用于要求高可靠性的通信, 如文件传输、网页浏览。
- *无连接服务*
    - 类比: 寄快递/明信片。每个数据包都携带完整的目标地址, 独立发送。不需要预先建立连接, 也不需要对方确认。
    - 特点: *灵活、高效*, 但可能丢失或乱序。适用于实时性要求高、可容忍少量丢失的场景, 如视频通话、网络广播。
服务由一组可用于用户进程以访问服务的原语 (操作) 形式指定, 原语告诉服务执行某些操作或报告对等实体所采取的操作。

面向连接的服务包含以下原语: *连接请求、接受相应、请求数据、应答、请求断开、断开连接*。

=== 数据单元
- *服务数据单元(SDU)*: 上层交给本层传输的数据, 是本层的“有效载荷”。
- *协议控制信息(PCI)*: 本层协议附加的控制信息 (即“头部”) , 如地址、序号等。
- *协议数据单元(PDU)*: PDU = SDU + PCI。这是对等实体之间实际交换的数据单元。
- *接口数据单元(IDU)*: 相邻两层通过接口传递的数据单元, 它包含了PDU和一些接口控制信息。
#index[SDU]#index[PCI]#index[PDU]#index[IDU]
+ 第n层的SDU (想传的数据) 加上第n层的PCI (本层的控制头) 就构成了第n层的PDU。
+ 第n层的PDU, 作为整体交给下一层 (第n-1层) , 就变成了第n-1层的SDU。
+ 第n-1层再给它加上自己的PCI, 形成第n-1层的PDU, 如此往复。这个过程完美对应了封装的概念。

== 参考模型
=== OSI参考模型
开放系统互连参考模型 (Open System Interconnection Reference Model, OSI/RM) 由国际标准化组织 (ISO) 于1984年提出, 旨在为不同厂商的设备互连提供一个通用的标准框架。它采用分层结构, 将复杂的通信过程分解为七个独立的层次, 每一层为上一层提供服务, 并使用下一层提供的服务。#index[OSI]

#table(
    columns: 2,
    align: center + horizon,
    [*层级与名称*], [*主要功能与协议举例*],
    [第七层: 应用层 (Application Layer) ],
    [
        为应用程序提供网络服务接口, 是用户与网络交互的界面。
        协议示例: #list[
            HTTP, HTTPS, FTP, SMTP, DNS, TELNET
        ]
    ],

    [第六层: 表示层 (Presentation Layer) ],
    [
        负责数据格式的转换、加密解密、压缩解压缩, 确保应用层数据能被对端系统理解。
    ],

    [第五层: 会话层 (Session Layer) ],
    [
        负责建立、管理和终止两台主机上应用进程之间的会话 (Session) , 并提供对话控制 (全双工/半双工) 。
    ],

    [第四层: 传输层 (Transport Layer) ],
    [
        提供端到端 (End-to-End) 的可靠或不可靠的数据传输服务。
        核心协议: #list[
            TCP (传输控制协议, 面向连接、可靠) ,
            UDP (用户数据报协议, 无连接、高效)
        ]
    ],

    [第三层: 网络层 (Network Layer) ],
    [
        负责在不同网络之间进行逻辑寻址 (IP地址) 和路由选择 (路径选择) , 实现数据包的转发。
        核心协议: #list[IP, ICMP, IGMP]
    ],

    [第二层: 数据链路层 (Data Link Layer) ],
    [
        在物理链路上提供可靠的数据传输, 负责帧的封装、差错控制 (CRC) 、流量控制和介质访问控制 (MAC) 。
        将数据封装成帧, 通过MAC地址在局域网内寻址。
    ],

    [第一层: 物理层 (Physical Layer) ],
    [
        在物理媒介上透明地传输原始比特流, 定义电气、机械、过程和功能规格 (如电压、线缆、接口) 。
    ],
)

=== TCP/IP参考模型
TCP/IP参考模型 (又称DoD模型) 源于美国国防部高级研究计划局 (ARPA) 的ARPANET项目, 是互联网事实上的标准。它采用更简洁的四层结构, 将OSI模型的上三层合并为应用层, 将数据链路层和物理层合并为网络接口层。

#table(
    columns: 2,
    align: center + horizon,
    [*层级与名称*], [*主要功能与核心协议*],
    [第四层: 应用层 (Application Layer) ],
    [
        对应OSI的应用层、表示层、会话层, 提供面向用户的各种网络服务。
        核心协议: #list[
            HTTP, FTP, SMTP, DNS, Telnet, SNMP
        ]
    ],

    [第三层: 传输层 (Transport Layer) ],
    [
        功能与OSI传输层类似, 提供端到端通信。
        核心协议: TCP (可靠、面向连接) 、UDP (高效、无连接)
    ],

    [第二层: 网络互联层 (Internet Layer) ],
    [
        对应OSI的网络层, 核心职责是使用IP协议将数据包从源主机路由到目的主机。
        核心协议: IP (网际协议) , 以及ICMP, IGMP, ARP, RARP等
    ],

    [第一层: 网络接口层 (Network Interface Layer) ],
    [
        对应OSI的数据链路层和物理层, 负责与底层网络硬件 (如以太网、Wi-Fi) 的接口。
        此层TCP/IP未严格定义, 可兼容多种底层网络技术
    ],
)

=== OSI模型与TCP/IP模型对比
OSI模型和TCP/IP模型是理解网络的两个关键框架, 它们在设计理念、层次结构和实际应用上存在显著差异。

#table(
    columns: 3,
    align: center + horizon,
    [*对比维度*], [*OSI参考模型*], [*TCP/IP参考模型*],
    [*设计出发点*],
    [
        由国际标准化组织 (ISO) 主导, 作为理论标准设计, 追求通用性和严谨性。
    ],
    [
        源于ARPANET实践, 为解决异构网络互联而开发, 后经实践完善为理论模型。
    ],

    [*层次结构*],
    [
        严格的七层结构, 概念清晰, 功能划分明确。
    ],
    [
        简洁的四层结构, 更注重实用性, 层次划分更符合实际协议簇。
    ],

    [*核心特性*],
    [
        - 清晰区分服务、接口、协议概念。
        - 先有模型, 后试图推动协议发展。
        - 理论模型完整, 但实现复杂。
    ],
    [
        - 协议是核心, 模型是对已有协议的描述。
        - 设计简单, 效率高, 适应性强。
        - 网络接口层定义模糊, 开放性高。
    ],

    [*影响与应用*],
    [
        理论指导意义重大, 是教学和理解网络概念的黄金标准。但因复杂性和实现难度, 未能被广泛采用。
    ],
    [
        互联网的实际标准, 所有网络设备和应用都基于此模型通信, 取得了巨大的成功。
    ],
)
== 计算机网络度量单位

#note[
    特别注意, 1 byte=8bits, 在计算中容易遗忘。文件大小中的MB等概念, 指的都是byte, 而且文件大小的计算中应该是1024为比例, 而不是1000, 与网络带宽的Mbps是不同的。

    但如果是Mbit的message, 还是按照1000比例来处理。
]

- *比特率(bit rate)*
    - 主机在数字信道上传送数据的速率, 也称*数据率*
    - 比特率的单位是b/s(比特每秒), 也可以写为bps, (bit per second), 或 kbit/s、Mbit/s、 Gbit/s等
    #remark[
        $
            "波特率"="比特率"/"每符号含的比特数"
        $
    ]
- *带宽*
    - 网络中某通道传送数据的能力, 即单位时间内网络中的某信道所能通过的“最高数据率”
    - 单位是 bit/s, 即 “比特每秒”
- *包转发率(PPS)*
    - 全称是Packet Per Second(包/秒), 表示交换机或路由器等网络设备以包为单位的转发速率
    - 线速转发: 交换机端口在满负载的情况下, 对帧进行转发时能够达到该端口线路的最高速度
- *时延(Delay)*
    - *时延 (delay 或 latency)* 是指数据 (一个报文或分组) 从网络 (或链路) 的一端传送到另一端所需的时间, 也称为延迟
    - *传输时延(transmission delay)*: 数据从结点进入到传输媒体所需要的时间, 传输时延又称为*发送时延*
    - *传播时延(propagation delay)*: 电磁波在信道中需要传播一定距离而花费的时间
    - *处理时延(processing delay)*: 主机或路由器在收到分组时, 为处理分组 (例如分析首部、提取数据、差错检验或查找路由) 所花费的时间
    - *排队时延(queueing delay)*: 分组在路由器输入输出队列中排队等待处理所经历的时延
    - *往返时延RTT(Round-Trip Time)*: 从发送方发送数据开始, 到发送方收到来自接收方的确认, 经历的总时间,可用于判断网络的通断性、测试网络时延、计算数据包丢失率等。#index[RTT]
$
    d_("trans")="Length(bit)" / R("bit/s")\
    d_("prop")="Distance(m)" / c("m/s")\
    d_"total"=d_"proc"+d_"queue"+d_"trans"+d_"prop"
$
#note[
    - *传播时延*和*传输时延*都乘*链路(link)*的数量
    - *处理时延*乘*路由器(router)*的数量。
]
#example[
    一个 2 MB 的文件通过消息交换方式在由 3 条链路(2 个中间路由器) 组成的网络路径上传输。每条链路的带宽为 2 Mbps, 传播延迟为 5 毫秒。忽略处理和排队延迟，计算总端到端延迟。

    - 文件大小转换: $2 "MB"=2 times 2^20 times 8 =16777216 "bits"$
    - 带宽转换: $2 "Mbps"=2 times 10^6 "bps"$
    所以$T_("total")=25165824+0.015 approx 25.18s$
]
- *时延带宽积*
    - 时延带宽积 = 传播时延 $times$ 带宽, 即按比特计数的链路长度
    - 若发送端连续发送数据, 则在发送的第一个bit即将达到终点时, 发送端就已经发送了时延带宽积个bit , 而这些bit都在链路上向前移动。
- *吞吐量 (throughput)*
    - 单位时间内通过某个网络(或信道、接口)的数据量, 单位是 b/s
- *有效吞吐量(goodput)*
    - 单位时间内, 目的地正确接收到的有用信息的数目 (以 bit 为单位)
- *利用率*
    - 信道利用率指出某信道有百分之几的时间是被利用的
    - 网络利用率则是全网络的信道利用率的加权平均值
- *丢包率*
    - 所丢失数据包的数量占所发送数据包的比率
- *时延抖动*
    - 变化的时延称为抖动 (Jitter)
    - 时延抖动起源于网络中的队列或缓冲, 抖动难以精确预测
    - 在语音、视频多媒体业务中, 抖动往往会严重影响用户的体验
- *延迟丢包*
    - 在多媒体应用中, 由于数据包延迟到达, 在接收端需要丢弃失去使用价值的包
