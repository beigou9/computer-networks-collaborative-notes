#import "../note.typ": *



= 物理层

== 物理层基本概念
物理层是网络体系结构中的最低层。物理层是一套规则、一个功能层。它规定了如何使用这些物理设备和媒体来传输信号。比如, 它规定用多大的电压表示“1”和“0”, 一个比特持续多长时间, 连接器有多少个引脚以及每个引脚的作用等。

#note[
    物理层的作用就是: 尽可能地屏蔽掉不同传输媒体和通信手段的差异, 为数据链路层提供一个统一的数据传输服务。

    - *差异*: 世界上有各种各样的通信方式: 有的用网线 (双绞线) , 有的用光纤, 有的用Wi-Fi (无线电波) 。它们的电气特性、信号编码方式完全不同。
    - *屏蔽*: 物理层通过为每种媒体定义好相应的接口和编码规则, 将所有这些复杂的技术细节都“隐藏”在自己这一层。
]

物理层的核心任务是: 在连接各计算机的传输媒体上传输数据比特流。
+ *接收比特流*: 物理层从它的上层——数据链路层——接收需要发送的原始比特流 (就是一长串的0和1) 。
+ *编码*: 物理层不会直接把0和1送出去。它需要根据所使用的传输媒体的特性, 将这些比特流“翻译”成能够在媒体上传播的物理信号。
    - 例如, 在网线上, 它可能用高电压表示“1”, 低电压表示“0”。
    - 在光纤中, 它用有光脉冲表示“1”, 无光脉冲表示“0”。
    - *这个过程就是“编码”。*
+ *传输*: 将编码后的物理信号, 通过传输媒体 (网线、光纤、空气等) 发送出去, 最终被下一个网络节点 (比如另一台电脑的路由器) 的物理层接收。

*数据终端设备(DTE)*, 是一种具有一定的数据处理和转发能力的设备, 可以是数据的源点或终点。

*数据电路终结设备(DCE)*, 在DTE和传输线路之间提供信号变换和编码的功能, 负责建立、保持和释放数据链路。
#index[DTE]#index[DCE]
#remark[
    - *电气特性*: 规定各条线上的电压范围, 以及电缆长度的限制。
    - *功能特性*: 指明某条线上出现某一电平的电压表示何种意义, 以及每条线的功能 (数据线、控制线、时钟线)。
    - *过程特性*: 描述完成每种功能的事件发生顺序。
    - *机械特性*: 定义了网络设备之间物理连接时的物理参数, 例如​规定连接器（如水晶头、插座）的具体外形和几何尺寸等。
]
== 传输介质
=== 传输介质分类
传输介质是指发送器与接收器之间的物理通路可分两大类:
- *导引型传输介质*: 指电磁波被导向沿着某一媒体传播, 包括双绞线、同轴电缆、电力线和光纤等。
- *非导引型传输介质*: 指电磁波在大气层、外层空间或海洋中进行的无线传播, 包括短波传输、地面微波、卫星微波、光波传输等。

=== 导引性介质
+ *双绞线*
    - 具有绝缘保护层的两根铜导线按一定密度绞缠在一起形成的线对。通常多根双绞线再绞合成电缆状。
    - 影响双绞线的特性阻抗、衰减和近端串扰的因素: *绞合密度、扭绞方向和绝缘材料*等。
    - 适用于模拟传输或数据传输, 通信距离一般为几到几十公里。
    #remark[
        绞合可以减少两根导线相互的电磁干扰。
    ]
+ *同轴电缆*
    - 同轴电缆由硬的铜质芯线和外包一层绝缘材料, 在绝缘材料外面是一层网状密织的外导体, 以及塑料保护外套组成。
    - 同轴电缆具有寿命长、容量大、传输稳定、外界干扰小、维护方便等优点
    #remark[
        同轴电缆的带宽更高得益于他的屏蔽性好。
    ]
+ *光纤*
    - 光纤是一种新型的光波导, 结构一般是双层或多层同心园柱体, 由纤芯、包层和护套组成。
    - 由于纤芯折射率大于包层的折射率, 使得折射角大于入射角。当入射角足够大时, 就会引起全反射, 光线重新折回纤芯, 从而不断向前传播。
    - 三种实用光纤:
        - *多模突变光纤* (又称阶跃光纤) : 指光纤的纤芯和包层的折射率沿光纤的径向分布是均匀的, 而在两者的交界面上发生突变。多模突变光纤的带宽较窄, 适用于小容量短距离通信。
        - *多模渐变光纤*: 指纤芯的折射率是半径的函数, 沿着径向随半径的增加逐渐减小, 直到达到包层的折射率值为止, 而包层内的折射率又是均匀的。多模渐变光纤带宽较宽, 适用于中容量中距离通信。
        #remark[
            多模光纤的光源是发光二极管, 光脉冲在多模光纤中传输时逐渐展宽, 造成失真, 因此多模光纤只适合近距离传输。
        ]
        - *单模光纤*: 指纤芯中仅传输一种最低模式的光波, 由于纤芯直径很小 (通常为$8 tilde.op 12 mu m$) , 制作工艺难度大, 其折射率分布属于突变型。单模光纤的带宽极宽, 适用于大容量远距离通信。
    - *光纤通信系统的收发端器件*
        - 发送端的光源:
            - 发光二极管: 价格较低, 工作温度也较宽, 使用寿命长。
            - 注入激光二极管: 发光效率高, 可支持较高的传输速率。
        - 接收端
            - 光敏二极管: 遇到光照射时, 光敏二极管会产生一个电脉冲。
            - 光敏二极管的典型响应时间为$1n s$, 因而限制传输速率在$1"Gb/s"$左右。
+ *电力载波*
    - 电力载波是电力系统特有的通信方式。利用现有电力线, 通过载波方式将模拟或数字信号进行高速传输。
    - 电力线是指输送电能的电力线路。

== 数据通信基础
=== 数据通信基础理论

#remark[
    码元是代表不同离散数值的基本信号波形, 是数字信号的计量单位。码元传输速率 (波特率)​​：单位时间内传输的码元个数, 单位是波特(Baud)。比特是信息的单位, 而码元是信号的单位。一个码元能携带多少比特的信息, 完全取决于它的进制数。例如, 一个二进制码元只能携带1比特信息, 而一个十六进制码元则可以携带4比特信息。
    $
        "比特率"="波特率" times "每码元携带的比特数"
    $
]
奈魁斯特(H. Nyquist)推导出无噪声有限带宽信道的最大数据传输速率公式。#index(initial: "N")[奈魁斯特定理]
#theorem[奈魁斯特定理][
    $
        "最大数据传输速率"=2H log_2V ("bps")
    $
    任意信号通过一个带宽为$H$的低通滤波器, 则每秒采样$2H$次就能完整地重现该信号, 信号电平分为$V$级。

    所以有：
    $
        "最大波特率"=2H
    $

]

#theorem[香农定理][
    带宽为$H$赫兹, 信噪比为$S/N$的任意信道的最大数据传输速率为:
    $
        H log_2 (1+S/N) ("bps")
    $
    由信息论得出的, 具有普遍意义, 与信号电平级数、采样速度无关。
]#index(initial: "X")[香农定理]
#remark[
    奈奎斯特定理, 描述的是在理想无噪声的低通信道中, 码元传输速率的极限；核心是: *信道带宽直接限制了无失真传输的符号速度。*一个码元能携带的信息量是$log_2 V$比特；无论采用多少进制的调制, 奈奎斯特定理计算出的最终数据传输速率 C, 其单位始终是“二进制比特每秒” (bps)
]

#remark[
    香农定理描述的是在有随机噪声 (如热噪声) 的带宽受限信道中, 可靠信息传输速率的上限 (即信道容量) 。这个定理的核心是: *信道的最终容量由带宽和信噪比共同决定, 噪声的存在给传输速率设定了一个不可逾越的绝对上限。*
]
#note[
    信噪比是信号功率与噪声功率的比值, 衡量信号质量。通常用分贝(dB)表示。
    $
        "信噪比"("dB")=10 times log_(10) (S/N)
    $
    如果用电压来计算则系数从10改为20, 这是因为功率与电压的平方成正比。
]
#note[
    易错！！！*当需要计算一个实际信道的最大数据速率时, 需要分别用两个定理计算, 然后取较小值作为实际可能达到的速率上限。*
]

#remark[
    - *采样*是指将连续时间上的模拟信号转换为离散时间上的样本序列的过程。
    - *带宽*就是一个数字信号他的频率的取值范围的大小。例如频率范围是3.5$~$3.9MHz, 那么带宽就是0.4MHz.
]

通信系统中的电信号有模拟信号和数字信号, 而数据又有模拟数据和数字数据。无论是模拟数据还是数字数据都能编码成模拟信号或数字信号。选择何种编码方式取决于需满足的要求和可能提供的传输媒体和通信设施。

#table(
    columns: 5,
    align: center + horizon,
    inset: 5pt,
    stroke: 0.5pt,

    // 表头
    table.header(
        [*数据* (要传送的信息)], [*信号* (在信道中跑的波形)], [*转换过程名称*], [*常见技术与标准*], [*典型应用场景*]
    ),

    // 数据行
    [*数字数据* (计算机中的0和1)],
    [*数字信号* (电压脉冲序列)],
    [*编码*],
    [曼彻斯特编码、4B/5B编码],
    [以太网 (局域网) 、USB、硬盘接口],

    [*数字数据*], [*模拟信号* (连续电磁波)], [*调制*], [ASK, FSK, PSK, QAM,], [Wi-Fi、蓝牙、ADSL、传统电话调制解调器],

    [*模拟数据* (声音、图像)], [*数字信号*], [*数字化编码*], [脉冲编码调制], [固定电话、音乐CD、数字电话],

    [*模拟数据*], [*模拟信号*], [*模拟调制*], [AM (调幅) 、FM (调频) 、PM (调相) ], [传统广播、电视],
)

=== 传输方式
为适应不同的需要, 通信线路采用不同的连接方式。有点到点传输、点到多点传输等。按照数据信号在信道上的传送方向与时间的关系, 传输方式可分为三类:
- *单工*: 指两个站之间只能沿一个指定的方向传送数据信号。
- *半双工*: 指两个站之间可以在两个方向上传送数据信号, 但不能同时进行, 又称“双向交替”模式, 发/收之间的转向时间为20～50ms。
- *全双工*: 指两个站之间可以在两个方向上同时传送数据信号。
#index(initial: "D")[单工]
#index(initial: "B")[半双工]
#index(initial: "Q")[全双工]
#remark[
    想象一下, A要发送一串二进制比特流 01001101给B。

    B面临两个难题:
    + *位同步*: 每个比特持续多长时间？B应该在什么时候判断当前是0还是1？判断早了或晚了都可能出错。
    + *帧同步*: 这一长串比特流从哪里开始算一个完整的数据单元 (比如一个字符或一个数据块) ？又在哪里结束？

    异步传输和同步传输就是为解决这两个问题而生的两种不同策略。
]

按照发/收两端实现同步的方法, 传输方式可分为两类:
- *异步传输*:
    这种方式适用于低速、零星的数据传输, 比如早期的键盘、鼠标与电脑通信。被传送的每一个字符一般都附加有1个起始位和1个停止位, 起始位与停止位的极性不同, 为了保证正确接收, 利用一个频率为传输比特率的$n(=16)$倍的时钟, 在每一个比特周期的中心采样。
    #remark[
        发送方和接收方的内部时钟是独立的, 不需要保持一致。接收方会用一个比传输速率快很多倍 (比如16倍) 的高速时钟来监测线路。一旦检测到起始位 (从高电平跳到低电平) 的下降沿, 接收方就开始用它的高速时钟进行计数。它会数到第8个周期 (即在比特周期的中心点) 时, 对线路电平进行一次采样, 判断是0还是1。因为这个点是最稳定的, 受信号边沿抖动的影响最小。它对每个比特都重复这个“中心采样”的过程, 直到收完停止位。
    ]
- *同步传输*:
    这种方式适用于高速、大量的数据传输, 比如网络通信、磁盘读写。每一个字符使用起止位按位进行传送, 数据块以帧作为整体传输, 并做到:
    - 发/收之间的位同步.
    - 每一帧建立同步标志, 建立帧同步.
    #remark[
        数据不是以字符为单位, 而是以帧为单位传输。一帧可能包含几百到几千个字节的数据。帧头与帧尾都有特殊的同步标识 (如比特序列 01111110) 。

        发送方和接收方的时钟必须是同步的。实现方法有两种:
        - 单独时钟线: 用一根单独的线路传送时钟信号。
        - 编码技术: 采用曼彻斯特编码等技术, 将时钟信号“嵌入”到数据信号中, 接收方可以从数据流中直接提取出时钟信号。
    ]

异步传输实现简单, 成本低。因为每个字符都是独立的包裹, 即使中间有间隔也没关系。但是效率低。每个字符都要额外附加2-3个比特的“包装” (起始位、停止位, 可能还有校验位) , 有效数据占比 (效率) 不高。

同步传输效率极高。因为只有整个数据块才需要加一个帧头帧尾, 额外开销占比很小。但是实现复杂, 成本高。*要求收发双方有高度精确的时钟同步机制*。

#align(center, table(
    align: center,
    columns: (auto, auto, 1fr),
    [*特性*], [*异步传输*], [*同步传输*],
    [*传输单位*], [字符], [数据块 (帧) ],
    [*时钟关系*], [不同步], [同步],
    [*同步机制*], [靠起始位/停止位为每个字符同步一次], [靠同步标识建立帧同步, 靠编码技术维持位同步],
    [*效率*], [低 (额外开销大) ], [高 (额外开销小) ],
    [*适用场景*], [低速设备 (键盘, 鼠标) ], [高速设备(以太网, 光纤通信)],
))

几种数据编码方式, 都是为了解决: 如何将数字比特流 (0和1) 转换成物理信号 (如电平) 在线上传输, 并且接收方能够准确无误地解读。
- *不归零制码*:
    - *原理*: 直接用两种固定的电平表示二进制。高电平代表1, 低电平代表0；
    - *波形特征*: 在一个比特的持续时间内, 电平保持不变, 不会“归零”。
    - 缺点:
        - *难以同步*: 如果连续发送多个“1”或多个“0”, 信号会长时间保持为一条直线。接收方无法知道过去了几个比特, 因为它看不到电平变化 (跳变) 来作为计时参考。
        - *需要额外时钟线*: 为了解决同步问题, 发送方和接收方必须用一根单独的线路来传输时钟信号, 这增加了成本。
        - *直流分量*: 长串相同的比特会导致信号产生直流分量, 不利于某些信道传输。
- *曼彻斯特编码*#index(initial: "M")[曼彻斯特编码]
    - *原理*: 在每一个比特的正中间, 必须发生一次电平跳变。从低电平跳变到高电平代表“0”, 从高电平跳变到低电平代表“1”。
    - *波形特征*: 每次跳变既作为时钟信号 (告诉接收方“现在是比特的中间点, 可以开始采样判断了”) , 又作为数据信号 (跳变的方向决定了比特的值) 。
    - 优点:
        - *自同步*: 因为每个比特中间都有一次强制跳变, 接收方可以非常容易地从数据流中提取出时钟信号, 实现同步, 无需额外的时钟线。
        - *无直流分量*: 由于信号频繁跳变, 平均电平为零, 消除了直流分量问题。
- *差分曼彻斯特编码*#index(initial: "C")[差分曼彻斯特编码]
    - *原理*: 在每一个比特的正中间, 同样必须有一次电平跳变。 但这次跳变仅用作时钟信号, 不表示数据。数据的值 (0或1) 由每个比特开始时刻是否有跳变来决定。在比特开始时发生跳变代表“0”, 在比特开始时无跳变代表“1”。
    - *波形特征*: 每个比特周期内至少有一次跳变 (中间) , 也可能有两次 (开始和中间) 。
    - 优点:
        - *抗干扰性更强*: 因为信息的判断依赖于“相对变化” (是否有跳变) , 而不是“绝对电平”。即使信号在传输过程中整体电平发生了偏移 (例如, 所有高电平都变低了一点) , 只要跳变关系正确, 数据就能被正确解读。这比曼彻斯特编码依赖绝对的跳变方向更可靠。

#remark[
    “直流分量”可以通俗地理解为信号中的“直流偏置”, 即信号电压长时间偏向一侧 (正或负) , 其平均值不为零。这会带来几个主要问题:
    - 阻碍变压器耦合: 很多网络设备 (如早期的以太网) 为了电气隔离和安全, 会使用变压器进行信号传输。变压器无法通过直流分量, 会导致信号严重失真甚至完全无法传递。
    - 造成能量浪费和信号畸变: 直流分量本身不携带任何信息, 却会白白消耗传输线的能量, 导致信号功率效率降低。同时, 它可能影响接收端判决电路的正常工作点, 增加误码风险。
    - 影响基线漂移: 如果信号中存在强烈的直流分量, 接收端可能难以确定判断0和1的准确电压阈值 (基线) , 尤其是在长距离传输后, 信号质量下降, 误码率会升高。
]

#figure(
    image("../fig/数据编码技术示意图.png", width: 80%),
    caption: "数据编码技术示意图",
)
频带传输, 数字数据的模拟传输。它把我们电脑产生的数字信号 (0和1) , 转换成特定频率的模拟信号, 以便在电话线、无线信道等模拟线路上传输。普通电话线或无线信道有特定的频率通过范围 (频带) , 无法直接传输基带数字信号。频带传输就是将信号“搬”到适合信道传输的频段上去。关键依靠调制解调器。它负责调制 (在发送端将数字信号转换为模拟信号) 和解调 (在接收端将模拟信号还原为数字信号) 。

三种最基本的调制技术:
+ *ASK - 幅移键控 (调幅) *
    - 原理: 固定载波的频率和相位, 只改变它的幅度来表示0和1。
    - 例子: 用高振幅表示“1”, 用低振幅 (或零振幅) 表示“0”。
    - 特点: 实现简单, 但抗干扰能力差 (振幅容易受信号强弱影响) 。#index[ASK]
+ *FSK - 频移键控 (调频) *
    - 原理: 固定载波的振幅和相位, 只改变它的频率来表示0和1。
    - 例子: 用高频率表示“1”, 用低频率表示“0”。 (就像两种不同音高的声音)
    - 特点: 抗干扰能力比ASK强, 因为频率不#index[FSK]易受振幅衰减的影响。
+ *PSK - 相移键控 (调相) *
    - 原理: 固定载波的振幅和频率, 只改变它的相位来表示0和1。
    - 最简单的例子 (BPSK) : 用相位变化0°表示“0”, 变化180°表示“1”。
    - 特点: 抗干扰能力最好, 是现代通信中最常用的技术。#index[PSK]

为了让一个码元 (一个波形符号) 携带多个比特的信息, 从而在有限的带宽内传输更高的数据速率。

+ *BPSK (二进制相移键控) *
    - 原理: 使用两种相位 (如0°和180°) 。
    - 效率: 一个码元携带 1 比特 信息。#index[BPSK]
+ *QPSK (四相相移键控) / 4QAM*
    - 原理: 使用四种相位 (如0°,  90°,  180°,  270°) 。
    - 效率: 一个码元可以表示 00, 01, 10, 11四种状态, 即携带 2 比特 信息。效率是BPSK的两倍。#index[QPSK]
+ *16QAM (十六进制正交幅度调制) *
    - 原理: 这是PSK和ASK的结合, 它同时改变载波的相位和幅度, 在坐标图上形成16个不同的点。
    - 效率: 一个码元可以表示16种状态, 携带 4 比特 信息。效率极高。
    - 代价: 点越密集, 对信道的信噪比要求越高, 因为一点微小的干扰就可能使接收方判错点, 导致误码。#index[16QAM]

== 多路复用技术
信道资源是有限的, 实际网络中, 多对用户往往需要利用相同的信道资源传输信息, 不同的信号同时在同一信道中传输会产生严重的相互干扰, 导致传输失败。复用 (multiplexing) 技术的目的是: *允许用户使用一个共享信道进行通信*, 避免相互干扰, 降低成本, 提高利用率。

#remark[
    FDM更适合传输模拟信号, TDM更适合传输数字信号。TDM的应用更为广泛。

    FDM的核心是模拟滤波。它使用模拟滤波器 (如带通滤波器) 来分离或合并不同频率的信号。这个过程本身就是对连续波形进行操作，非常适合处理连续的模拟信号(如语音、广播信号)。信号只需经过简单的调制 (如调幅AM、调频FM) 就能被搬移到指定的频带上，接收端再用相应的模拟滤波器即可提取出原始信号。

    TDM的核心是采样和时隙分配。数字信号本身就是离散的(用0和1表示), 非常适合被分配到离散的时间槽中传输。
]
=== 频分复用(FDM)
#index[FDM]
- *核心思想*: 将信道的总频率带宽划分成多个不同且互不重叠的频段 (子信道) , 每个用户固定占用其中一个频段来传输自己的信号。
- *工作方式*: 在发送端, 通过调制技术将各路信号的频谱搬移到分配给它的频带上, 然后合并传输。在接收端, 则用带通滤波器将不同频段的信号分离开来。
- *特点与应用*: 各路信号在频率上是分开的, 但在时间上是重叠的, 可以同时传输。它的优点是技术相对成熟, 实现简单。缺点是各子信道之间需要设置“保护带”以防干扰, 这降低了频谱效率。传统的有线电视和FM广播是FDM的典型应用。
=== 时分复用 (TDM) 与统计时分复用 (STDM)
- *核心思想*: 将时间划分成等长的、周期性重复的时隙, 每个用户在一个周期内固定分配到一个时隙, 在其占用的时隙内独占整个信道的全部带宽。
- *工作方式*: 每个时隙按照固定顺序分配给不同的用户。*即使用户在属于自己的时隙没有数据要传, 该时隙也会空着, 其他用户无法使用, 这会造成资源浪费*。这种固定分配的TDM也称为同步时分复用。

#index[TDM]#index[STDM]
#example[
    有1路数字信号的数据率是8kb/s, 另外7路数字信号的数据率均为7.2kb/s, 采用同步TDM方式复用到一条通信线路上, 复用线路需要的最小通信能力是多少?

    这里是数据传输到线路上的过程, 如果速度越快, 那么加载的数据就越多, 那么就会有1路拉满了通信线路给他分配的窗口, 而其他7路信号都会有所空缺, 但是空缺的时刻复用线路也是可以传输信号的, 因此最小通信能力是$8 times 8=64"kb/s"$
]

- *统计时分复用*: 为了解决同步TDM可能产生的资源浪费, 统计时分复用(或称异步时分复用) 应运而生。它不固定分配时隙, 而是按需动态分配: *只给那些真正有数据要发送的用户分配时隙*。这大大提高了信道利用率, 但*实现起来更复杂, 需要在数据中加入地址信息以便接收方能正确分路*。

#example[
    - *E1系统*
        - 帧长: 125 μs。这源于奈奎斯特采样定理: 为了无失真地传输频率为4kHz的电话语音, 采样频率需要至少8kHz (即每秒采样8000次) , 所以采样间隔为 1/8000 = 125 μs。
        - 帧结构: 每125μs的一帧被划分为 32 个时隙 (通道) 。
        - 速率计算: (32 时隙/帧) × (8 比特/时隙) × (8000 帧/秒) = 2,048,000 bps = 2.048 Mbps。
    - *T1 系统*
        - 帧结构: 每帧包含 24 个 话路时隙 (每个时隙8bit, 7bit为语音编码, 1bit用于信令) , 然后在24路之后再加上1bit 用于帧同步。所以每帧总共有 24×8 + 1 = 193 bit。
        - 速率计算: 采样频率同样是8kHz, 所以每秒有8000帧。(193 比特/帧) × (8000 帧/秒) = 1,544,000 bps = 1.544 Mbps。
    - *SONET (同步光纤网)*
        - 这是由美国制定的标准。它定义了一个以 51.84 Mbps 为最基本单位的速率等级体系。
        - 这个基本单位被称为 STS-1 (电信号) 或 OC-1 (光信号) 。
]

#note[
    实现TDM, 必须满足:
    - *介质的位速率* (每秒传输的二进制位数) 大于单个信号的位速率。
    - *介质的带宽* (所能传输信号的最高频率与最低频率之差) 大于结合信号的带宽 (所有信号经过调制后形成的复合信号的带宽)。
]
=== 波分复用(WDM)
波分复用本质上是*频分复用技术在光纤这个特定媒介上的应用*。由于光波的频率极高, 人们*习惯用波长而非频率*来指称, 故得此名。
#index[WDM]
- *核心思想*: 在一根光纤中同时传输多个不同波长的光载波信号, 每个波长承载一路独立的信息
- *分类与应用*: 根据波长间隔的疏密, 可分为粗波分复用 (CWDM, 波长间隔大, 成本较低) 和密集波分复用 (DWDM, 波长间隔小, 容量巨大) 。WDM是提升光纤通信系统传输容量的核心技术, 堪称现代互联网骨干网的基石。
=== 码分复用(CDM)
#index[CDM]
这是一种更为“智能”的复用方式, 它允许所有用户在相同时间、相同频段上进行通信。
- *核心思想*: 不再从频率或时间上进行划分, 而是给每个用户分配一个唯一的、相互正交的地址码 (伪随机码序列) 。在发送端, 用这个码序列对原始信号进行调制 (扩频) ；在接收端, 用完全相同的码序列进行相关解调 (解扩) , 从而从混合的信号中恢复出属于自己的那一路信号。
- *特点与应用*: 由于各用户的码序列具有正交性, 即使信号在时域和频域上完全重叠, 也能被有效区分。CDM抗干扰能力强, 保密性好, 是3G移动通信的基础。

- 比特 vs 码片: 你要发送的数据比特 (0或1) 是真正有用的信息。但CDMA不会直接发送这个比特。它会*为每个比特分配一个更细粒度的“身份标识”*, 这就是*码片序列*。
    - 例如数据速率是1 bps (每秒1个比特) 。但系统会将其扩展: 每个比特时间被划分成 m个更小的时间片 (比如m=8) 。这8个小时间片里传输的编码, 就是码片序列。
        - 发送比特 1 -> 直接发送站点的码片序列(例如 [-1, -1, -1, +1, +1, -1, +1, +1])。
        - 发送比特 0 -> 发送站点码片序列的反码 (例如 [+1, +1, +1, -1, -1, +1, -1, -1]) 。
    - 正交性: 这是CDMA的魔法所在。系统给每个用户分配的码片序列是相互正交的。
        - 把每个用户的码片当作向量, 这个向量是正交的。
        - 任意一个码片序列和自己的规格化内积是1。
        - 任何一个码片序列和自己反码的规格化内积是-1。
    - 在CDMA中, 所有用户在相同时间、相同频率上发送信号。信道中实际传输的信号, 是所有这些用户信号线性叠加后的结果。
#example[
    已知四个站的码片序列 (A, B, C, D) 和收到的混合序列 M = (-1 +1 -3 +1 -1 -3 +1 +1)。A的码片序列: A = (-1 -1 -1 +1 +1 -1 +1 +1), B的码片序列: B = (-1 -1 +1 -1 +1 +1 +1 -1), C的码片序列: C = (-1 +1 -1 +1 +1 +1 -1 -1), D的码片序列: D = (-1 +1 -1 -1 -1 -1 +1 -1), 问: 谁发了数据？发了什么？

    逐一计算ABCD的码片序列与M的规格化内积得到, A:1, B:-1, C:0, D:1。所以可用知道, A, D发送了比特1, B发送了比特0, C没有发送数据。
]
#figure(image("../fig/CDMA编码和解码过程示例.png", width: 80%), caption: "CDMA编码和解码过程示例")

=== 正交频分复用(OFDM)
正交频分复用可以看作是FDM的一种高级形式, 是当前4G/5G和Wi-Fi等高速通信系统的关键技术。
- 核心思想: 将高速的数据流分解成多个低速的子数据流, 然后用这些子数据流分别去调制一系列相互正交的子载波。不用带通滤波器来分隔子载波, 而是通过快速傅立叶变换 (FFT)
#index[OFDM]
- “正交”的优势: 传统FDM需要保护带防止干扰, 而OFDM通过精确设计, 使得各子载波的频谱相互正交, 频谱可以重叠而无严重串扰, 从而极大地提高了频谱利用率.它能有效对抗多径效应引起的信号干扰, 特别适合高速无线传输。
    - 离散信号: 每个子信道的信号表示为一个向量, 各向量的内积之和为0
    - 连续信号: 内积之和表示为乘积的积分, $integral A dot B d x=0$

=== 空分复用(SDM)
#index[SDM]
这是一种从物理空间上寻求突破的复用技术。
- *核心思想*: 利用空间分离的天线阵列, 在相同的频率和时间资源上, 同时服务多个位于不同空间位置的用户。
- *实现方式*: 典型技术是大规模MIMO。基站部署大量天线, 通过对信号进行精确的相位和幅度控制, 形成指向特定用户的狭窄波束。这样, 即使多个用户使用相同的时频资源, 也能通过空间上的隔离进行区分。
- *应用*: 这是5G技术实现超高连接数和网络容量大幅提升的关键手段之一。

#example[
    假设现在要发送一个简单的数字序列: [-1, -1, 1, -1, 0, 0] (这已经是数字信号) 。在传统的单载波系统中, 我们会一个接一个地发送这些比特。

    首先我们将长度为6的串行序列分配给3个子载波。分配的原则是“轮流分发”,
    - 子载波 1 获得第1个、第2个数据: [-1, -1]
    - 子载波 2 获得第3个、第4个数据: [1, -1]
    - 子载波 3 获得第5个、第6个数据: [0, 0]

    现在我们定义2个OFDM符号周期 (Symbol Time):
    - 符号周期 1 ($T_1$)​​：用于传输每个子载波的第一个数据
    - 符号周期 2 ($T_2$)​​：用于传输每个子载波的第二个数据

    这里$T$的选取, 只需要与后续子载波表达式满足一个合适的关系即可。子载波我们往往利用傅里叶变换中提到的$cos 2 pi n x$这样的形式, 为了使得后续积分为0, 只需$n T in ZZ$ 即可。不妨后续取$T=1s$.

    我们选择频率是1Hz, 2Hz, 3Hz的余弦波:
    $
        "SC"_1 (t) & =cos(2 pi t) \
        "SC"_2 (t) & =cos(2 pi dot 2t) \
        "SC"_3 (t) & =cos(2 pi dot 3t) \
    $

    *发送方*:

    在每一个符号周期内, 我们用3个数据分别调制3个子载波, 然后将调制后的信号叠加成一个复合信号发送出去。
    - 子载波1用数据-1调制: $-1 dot cos(2 pi t)$
    - 子载波2用数据1调制: $1 dot cos(2 pi dot 2t)$
    - 子载波3用数据0调制:
    $S_1 (t)=-cos(2pi t)+cos(4pi t)$

    同理有$S_2 (t)=-cos(2 pi t)-cos(4 pi t)$

    最终, 在信道中连续传输的波形就是$S_1$ (0-1s) 和$S_2$ (1-2s) 的拼接。

    *接收方*:
    - 解调子载波1的数据, $integral_0^1 S_1 (t)dot cos(2pi t) d t=-0.5$, 积分结果为负, 判定子载波1发送数据为-1
    - 解调子载波2的数据, $integral_0^1 S_2 (t)dot cos(4pi t) d t=0.5$, 积分结果为正, 判定子载波2发送数据为1

    同理即可全部解调成功。

    而至于是行优先还是列优先, 即接收方如何按照发送方拆解之后的顺序来读取, 这个其实是双方事先的约定, 常见为示例中的表现。
]

