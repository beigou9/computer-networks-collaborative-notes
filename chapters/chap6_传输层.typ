#import "../note.typ": *


= 传输层

== 传输服务
1. *体系结构定位：承上启下的桥梁*
  - *层级位置*：传输层位于应用层与网络层之间，是网络体系结构中的关键层次。它利用网络层提供的基础分组交换服务，向分布式应用程序提供通用的通信平台。
  - *屏蔽底层细节*：传输层向高层屏蔽了底层网络核心的复杂性（如路由选择、网络拓扑、异构链路等），使应用开发人员只需关注应用逻辑，而无需为网络设备编写特定的通信代码。
2. *核心设计原则：端到端 (End-to-End)*
  - *仅在终端运行*：遵循因特网的端到端设计原则，传输层协议仅运行在端系统（Host，如计算机、服务器、移动设备）的操作系统中。
  - *网络设备透明*：网络核心中的中间设备（如路由器、交换机）仅处理网络层及以下的协议，它们不拆解传输层报文段，也不维护传输层的连接状态。
3. *通信抽象：逻辑通信与进程寻址*
  - *进程间通信*：与网络层提供“主机到主机” (Host-to-Host) 的通信不同，传输层提供的是“进程到进程”的通信。它负责将数据精准地交付给运行在主机上的特定应用进程。
  - *逻辑链路*：传输层为应用进程提供了一种逻辑通信信道。在应用程序看来，两个进程仿佛是直接通过一条物理线路相连的，尽管实际数据可能穿越了无数个路由器和物理链路。
4. *关键功能：复用与分用*
  - *复用*：源主机的传输层从不同的应用进程收集数据块，封装上头部信息（包含端口号），传递给网络层。
  - *分用*：目的主机的传输层根据报文段头部的端口号，将接收到的数据正确地分发给对应的应用进程。
5. *服务模型：依赖与增强*
  - *服务依赖*：传输层无法突破网络层带宽和延迟的物理限制（即无法在低带宽网络上实现高吞吐，也无法在拥塞网络中消除延迟）。
  - *服务增强*：传输层可以在不可靠的网络层（如IP协议提供的“尽力而为”服务）之上，通过差错检测、重传机制、流量控制和拥塞控制，构建出可靠的数据传输服务（如TCP）；也可以提供仅增加了复用/分用功能的轻量级不可靠服务（如UDP）。
6. *标准接口：套接字 (Socket)*
  - *定义*：套接字是应用层与传输层之间的软件接口，也是操作系统内核向用户空间暴露的网络编程 API。
  - *作用*：它充当了应用程序与网络协议栈之间的门户。通过套接字，应用程序可以请求网络服务（发送/接收数据），而具体的协议实现细节则由操作系统内核在传输层完成。

== 传输协议的要素
=== 寻址
传输层的寻址是为了*解决进程标识*问题。
- 网络层 (IP) 的局限：IP地址只能定位到具体的主机 (Host)，但无法区分主机上运行的不同应用进程 (Process)。
- *传输层的功能*：通过端口 (Port) 来标识应用进程，从而实现多路复用 (Multiplexing) 和多路分解 (Demultiplexing)。
  - 复用：多个应用进程通过不同的端口，将其数据封装后通过同一个网络层接口（IP）发送出去。
  - 分用：接收方传输层根据报文段中的目的端口号，将数据交付给对应的应用进程。

端口是应用层与传输层的服务接口，是传输服务访问点 (TSAP) 的具体实现形式。
- 物理特性：端口是用 16位二进制整数 进行标识的，取值范围为 0 \~ 65535。
- 本地意义 (Local Significance)：端口号只具有本地意义。它仅用于在本地计算机内部区分不同的进程。
#remark[
  注意：互联网上不同计算机的相同端口号之间没有物理联系，它们之间的关联是通过协议标准（如HTTP默认为80）建立的逻辑约定。
]
- 套接字 (Socket)：端口号通常与IP地址组合使用，构成套接字地址 "(IP地址 : 端口号)"，它是TCP/IP网络中进程通信的唯一标识。#index[Socket]

互联网赋号管理局 (IANA) 根据用途将端口号分为三大类：
#table(
  columns: (auto, auto, 1fr, 1.5fr),
  inset: (x: 8pt, y: 10pt),
  align: (col, row) => if col <= 1 { center + horizon } else { left + horizon },
  fill: (col, row) => if row == 0 { luma(230) } else { white },

  // 表头
  [*类型*], [*范围*], [*主要用途*], [*说明 / 机制*],

  // 第一行：熟知端口
  [熟知端口号 \ (Well-Known)],
  [0 \~ 1023],
  [指派给互联网上最核心、最常用的系统级服务。],
  [
    - 全球通用的“标准频道”。
    - 通常只有系统管理员 (root/admin) 才能启动监听此类端口。
  ],

  // 第二行：登记端口
  [登记端口号 \ (Registered)],
  [1024 \~ 49151],
  [供没有熟知端口号的普通应用程序使用 (如数据库、游戏服务器等)。],
  [
    - 建议在 IANA 登记以避免冲突。
    - *示例*: MySQL (3306), Redis (6379), Microsoft RDP (3389)。
  ],

  // 第三行：短暂端口
  [客户端口号 \ (Dynamic)],
  [49152 \~ 65535],
  [留给客户进程 (Client) 暂时使用。],
  [
    1. *分配*: 客户发起通信时，OS 自动分配空闲端口。
    2. *响应*: 服务器通过该端口回传数据。
    3. *回收*: 通信结束后端口释放，供其他进程复用。
  ]
)
#figure(
  table(
    columns: (auto, auto, auto, 1fr),
    fill: (col, row) => if row == 0 { luma(220) } else if calc.even(row) { luma(250) } else { white },
    inset: 8pt,
    align: (x, y) => if x == 0 { center } else { left },
    [*服务*], [*端口*], [*协议*], [*说明*],
    "FTP", "20/21", "TCP", "21 控制连接，20 数据传输",
    "SSH", "22", "TCP", "安全远程登录",
    "Telnet", "23", "TCP", "早期的远程登录 (明文)",
    "DNS", "53", "UDP/TCP", "域名解析 (主要用 UDP)",
    "DHCP", "67/68", "UDP", "67 服务端，68 客户端",
    "HTTP", "80", "TCP", "超文本传输协议",
    "RPC", "111", "TCP/UDP", "Portmapper (过程调用映射)",
    "HTTPS", "443", "TCP", "基于 SSL/TLS 的 HTTP",
  ),
  caption: [常见熟知端口号对照表]
)

在 OSI 七层模型与 TCP/IP 模型中，寻址概念有着严格的层级对应关系：
  - *NSAP (Network Service Access Point)*：网络服务访问点,在 TCP/IP 中，对应 IP 地址。用于标识网络上的主机节点。#index[NSAP]
  - *TSAP (Transport Service Access Point)*：传输服务访问点，在 TCP/IP 中，对应 端口号 (Port)。用于标识主机上的特定应用进程。#index[TSAP]

远程的客户端 (Client) 如何知道服务器 (Server) 的特定服务在哪个端口上监听？主要有两种方法：
- *预先约定* (Well-Known / Static)
  - 原理：某些服务极为重要，其端口号是全球统一、广为人知的标准。
  - 流程：客户端无需询问，直接向约定的端口发起连接。
  - 示例：Web浏览器访问网页，默认直接连接目标IP的 80 端口。Telnet客户端默认连接目标IP的 23 端口。
- *动态查询 (Name Server / Directory Server)*
  - 此方法常用于 RPC (远程过程调用) 环境，因为自定义服务众多，无法全部使用熟知端口。
  - 核心组件：*名字服务器 (Name Server)* 或 *端口映射器 (Port Mapper)*。这是一个特殊的进程，它自己必须使用一个熟知端口（如 RPC Portmapper 使用 111）。#index(initial: "M")[名字服务器]
  - 流程：
    1. 服务注册：服务器端的应用进程启动时，向本地的“名字服务器”注册自己的服务名和随机获取的端口号。
    2. 查询端口：客户端先连接服务器端的“名字服务器”（使用熟知端口 111）。
    3. 获取映射：客户端发送服务名称，名字服务器返回该服务当前对应的 TSAP（端口号）。
    4. 建立连接：客户端释放与名字服务器的连接，根据拿到的端口号，向目标服务进程发起真正的连接。
=== 连接建立: 三次握手 (Three-Way Handshake)
在分组交换网络中，由于网络层提供的服务是不可靠的（存在丢包、延迟、乱序），传输层必须独立处理连接状态的同步问题。
- 核心目标：不是为了单纯传输数据，而是为了同步双方的初始序列号 (Initial Sequence Number, ISN) 并确认双方的接收与发送能力。
- 根本解决的问题：网络中滞留的过时重复分组 (Delayed Duplicates) 导致的错误连接建立。

#example[
  假设主机 A 为发起方（Client），主机 B 为响应方（Server）。
  - 第一步：连接请求 (CR)
    - 动作：A 向 B 发送 CR TPDU。
    - 内容：SEQ = x。
    - 状态变更：A 选择一个随机的初始序列号 $x$，进入 等待确认 状态。
    - 语义：“请求建立连接，我方发送数据的起始序号为 $x$。”#index[CR]
  - 第二步：连接确认 (CC)
    - 动作：B 收到 CR 后，向 A 发送 CC TPDU。
    - 内容：SEQ = y, ACK = x (或 $x+1$, 视协议具体定义)。
    - 状态变更：B 为该连接分配资源，选择自己的初始序列号 $y$，进入 半连接 (Half-Open) 状态。
    - 语义：“收到你的请求 $x$，我同意连接。我方发送数据的起始序号为 $y$。”#index[CC]
  - 第三步：确认的确认 (ACK)
    - 动作：A 收到 CC 后，检查 ACK 是否与自己的 $x$ 匹配。若匹配，向 B 发送 DATA/ACK TPDU。
    - 内容：SEQ = x+1, ACK = y。
    - 状态变更：A 进入 已连接 (ESTABLISHED) 状态。B 收到该帧后，也进入 已连接 状态。
    - 语义：“收到你的确认 $y$，连接正式建立。”
]

#remark[
  TPDU 是 Transport Protocol Data Unit 的缩写，中文翻译为 传输协议数据单元。它是计算机网络体系结构中传输层 (Transport Layer) 进行对等通信时交换的数据单位。在 TCP/IP 协议栈中，根据使用的具体协议不同，TPDU 有特定的称呼：
  - TCP 协议：称为 报文段 (Segment)。
  - UDP 协议：称为 用户数据报 (User Datagram)。#index[TPDU]
]

#note[
  该协议最精华的部分，主要依靠序列号比对和超时重传机制。
]

*场景一：处理延迟的重复请求 (Delayed Duplicates)*

- 现象：A 并没有发起新连接，但网络中一个陈旧的 CR (SEQ=old_x) 到达了 B。
- B 的反应：B 无法区分这是新包还是旧包，因此 B 会正常响应，发送 CC (SEQ=y, ACK=old_x)。
- A 的防御：A 收到 CC 后，检查其 ACK 字段。发现 ACK=old_x 与当前状态（无连接或正在进行的其他连接）不符。A 发送 RST / REJECT 报文。B 收到后终止连接并释放资源。
- 结论：*通过第三次握手，A 拥有了“否决权”，从而防止了 B 被旧报文欺骗。*

*场景二：第三次握手丢失*
- 现象：A 发送了最终的 ACK 后进入已连接状态，但该报文在网络中丢失，B 未收到。
- 死锁风险：A 认为连接已好，B 认为连接未完成。
- 解决机制：B 端的超时重传。
  - B 在发送第二步的 CC 时启动定时器。
  - 若超时未收到 A 的回复，B 会判定自己的 CC 可能丢失，于是重传 CC。
- A 的响应：A 收到重复的 CC 后，明白自己的确认丢了，于是重发 ACK。

*场景三：第二次握手丢失*

此时A会重传CC，B会重传CC。在实际网络中，A 和 B 谁先重传，取决于谁的定时器设置得更短（通常是指数退避算法决定的）。只要网络恢复通畅，依靠双方（或任意一方）的重传机制，连接最终都能成功建立。
=== 连接释放

连接释放的复杂性在于确保双方的数据都已传输完毕。
- 非对称释放 (Asymmetric Release)
  - 定义：任何一方发送释放请求 (Disconnect Request, DR) 后，连接立即完全断开。
  - 缺陷：数据丢失风险。如果 A 发送 DR 时，B 还有数据在发送给 A 的途中，A 此时已关闭接收端口，这些在途数据将全部丢失。
  - 适用场景：仅适用于确定没有双向数据传输的简单场景。
- 对称释放 (Symmetric Release)
  - 定义：将一个全双工连接视为两个独立的单工连接。每个方向必须单独释放。
    - A 发送 DR，仅表示 A 没数据发了，但 A 仍能接收 B 的数据。
    - 只有当 B 也发送了 DR，连接才算彻底关闭。
  - 挑战：经典的两军问题。如果最后一句“再见”丢了，B 就不知道 A 是否知道自己已经讲完了。
为了解决对称释放中的确认丢失问题，实际协议（如 TCP）采用了一种带定时器的三次握手机制。虽然 *TCP 标准流程是四次挥手 (FIN, ACK, FIN, ACK)*，但在某些教科书模型或特定场景（如 B 没有数据发，直接回复 FIN+ACK）下，可抽象为三次握手过程。
- Step 1 (A -> B): A 发送 DR (Disconnect Request)，启动重传定时器。
- Step 2 (B -> A): B 收到 DR，回复 DR + ACK (确认 A 的释放，同时申请释放自己)。
- Step 3 (A -> B): A 收到 DR + ACK，回复 ACK，并启动 2MSL 定时器 (TIME-WAIT) 后关闭。B 收到 ACK 后关闭。

*最终确认帧 (ACK) 丢失*
- 场景：Step 3 中 A 发出的 ACK 丢失。
- A 此时已发送完所有报文，但 B 没收到确认，B 认为自己的 DR+ACK 丢了。B 超时重传 DR+ACK。A 此时虽然想关闭，但因为有 TIME-WAIT 定时器（或类似机制），A 依然“在线”。A 收到重复的 DR 后，再次发送 ACK。、
- 兜底：如果 A 已经彻底关闭（超过定时器时间），B 会在重传 N 次失败后强制关闭。

*一方响应帧丢失*
- 场景：Step 2 中 B 发出的 DR+ACK 丢失。
- A 发完 DR 后一直没收到回复。A 的定时器超时，A 重传 DR。B 再次收到 DR，重传 DR+ACK。

*双方响应帧都丢失 (Double Loss / Simultaneous Release Failure)*
- 场景：这是一个极端情况，通常指双方同时决定释放，或者多次重传均失败。
- A 发 DR，B 发 DR。两边的包都在网络中丢了。或者 A 收到 B 的 DR 发了 ACK，B 收到 A 的 DR 发了 ACK，结果这两个 ACK 都丢了。
- 双方的定时器都会超时。双方都会重传自己的 DR。如果网络极其糟糕，重传多次仍无法握手成功，“杀死定时器” (Kill Timer) 介入。当超过最大存活时间后，双方强制断开连接，向上层报错。
=== 错误控制
数据链路层保证点到点（Point-to-Point），传输层保证端到端（End-to-End）。网络中的路由器（Router）在存储转发分组时，可能会发生内存错误或软件Bug。数据链路层的 CRC 校验通常只在物理线路两端进行，一旦数据进入路由器内部，链路层的保护就失效了。因此，传输层必须进行端到端的校验和 (Checksum) 检查，这是最后一道防线。

由于网络层（IP）提供的是“尽力而为”的不可靠服务，丢包是常态。为了实现可靠传输，传输层必须引入缓存策略。
1. *发送方缓存 (Sender Buffering)*必须要有
  - 原因：重传机制 (Retransmission)。当你把数据发出去（TPDU）之后，不能立刻把它从内存里删掉。因为网络可能会把这个包弄丢。
  - 机制：发送方必须把已发送但未收到确认（Unacknowledged）的 TPDU 保存在缓存里。只有当收到接收方的 ACK 后，才能释放这部分缓存空间。
2. *接收方缓存 (Receiver Buffering)*通常要有
  - 原因 1：乱序重组 (Reordering)。网络层可能导致先发的数据后到。传输层必须有一个“等候区”，把乱序的包存下来，排好队，再按顺序交给应用层。
  - 原因 2：流量控制 (Flow Control)。这是“上层处理延迟”。如果发送方发得太快（比如 1Gbps），而接收方的应用程序写磁盘很慢（比如 10MB/s），如果没有缓存，数据就会溢出丢失。缓存起到了蓄水池的作用。

*缓冲区的三种设计模式*
- *链状固定大小缓冲区* (Chained Fixed-Size Buffers)
  - 机制：内存被切分成许多大小相同的“块”（比如每块 4KB）。数据来了，就申请一个或多个块，用链表串起来。
  - 管理极简单，不需要复杂的内存分配算法（如 malloc/free 的碎片整理）。
  - 池化管理 (Pooling)：申请和释放速度极快。
  - 内部碎片 (Internal Fragmentation)：如果一个 TPDU 只有 100 字节，却占用了一个 4KB 的块，剩下的空间就浪费了。
- *链状可变大小缓冲区* (Chained Variable-Size Buffers)
  - 机制：根据 TPDU 的实际大小，动态分配刚好够用的内存块，然后用链表串起来。
  - 空间利用率高：几乎没有内部浪费，数据多大就占多大。
  - 管理成本高：频繁地分配不同大小的内存会导致外部碎片，且内存分配器的 CPU 开销较大。
  - 开销：每个缓冲区都需要额外的头部信息（指针、长度计数），如果有很多小包，这些头部也是开销。
- *循环缓冲区* (Circular Buffer / Ring Buffer)
  - 这是现代 TCP 协议栈中最常用的设计
  - 机制：使用一段连续的内存空间，逻辑上首尾相接。有两个指针：Write_Ptr（数据存入位置）和 Read_Ptr（应用层取走数据位置）。数据存满尾部后，自动折返到头部继续存。
  - 完全零碎片：内存利用率极高。
  - 性能极致：不需要频繁分配/释放内存，只移动指针，非常适合高速网络 IO。
  - 天然适配滑动窗口：TCP 的滑动窗口机制本质上就是在这个环形缓冲区上滑动的。
  - 需要预先分配固定大小的大块连续内存。如果连接数非常多，可能会占用过多物理内存。

#remark[
  - 如果是低延迟、小数据场景，固定大小缓存比较快。
  - 如果是高吞吐、大数据流（如 TCP 文件下载），循环缓冲区（配合滑动窗口）是标准的最优解。
]
=== 流量控制
流量控制是为了解决发送方和接收方速度不匹配的问题。如果发送方发得太快，接收方来不及处理（应用层读得慢），接收缓存就会溢出，导致丢包。流量控制让发送方根据接收方的实际接收能力（缓存剩余空间）来调整发送速率。

*可变滑动窗口 (Variable Sliding Window)* 这是 TCP 协议中流量控制的基石。
- 接收窗口 (rwnd - Receive Window)：
  - 接收方会在发给发送方的确认报文（ACK）首部中，放入一个字段叫 Window Size。
  - 这个数值告诉发送方：“我的接收缓存里还剩下多少字节的空间”。
- 发送方: 
  - 发送方维护一个变量 Send Window。
  - Send Window <= rwnd (忽略拥塞控制的情况下)。
  - 如果收到 rwnd = 0，发送方必须停止发送数据，进入等待状态。
#remark[
  流量控制是点对点（端到端），而拥塞控制是全局性。
]
#remark[
  rwnd 是 Receiver Window（接收窗口）的缩写#index[rwnd]
]

#figure(image("/fig/流量控制.png", width: 60%), caption: "基于信用（Credit）机制的动态缓冲区分配协议")
为了防止 Line 16 这种情况导致连接永久挂起，TCP 引入了 坚持定时器 (Persistence Timer) 或称为 零窗口探测 (Zero Window Probe)。
- 当 A 收到 buf=0（零窗口通知）时，A 不会傻等。
- A 会启动一个坚持定时器。
- 如果定时器时间到了，A 还没有收到新的窗口更新（buf > 0），A 就会发送一个只有 1 字节数据的探测报文 (Probe)。
- B 收到探测报文后，必须回复当前的窗口状态（比如 ack=6, buf=4）。
- 这样 A 就重新获得了丢失的窗口更新信息，打破死锁。
=== 复用分用
- 复用 (Multiplexing) [发送端]
  - 定义：传输层从源主机的多个应用进程收集数据块，为每个数据块封装头部信息（主要包含端口号），然后传递给网络层。
  - 核心动作：数据封装。
- 分用 (Demultiplexing) [接收端]
  - 定义：接收方的传输层在收到报文段 (Segment) 后，检查头部信息，将数据精准地交付给运行在主机上的正确应用进程。
  - 核心动作：数据分发。

- 数据单元：传输层交换数据的单位称为 段 (Segment) 或 TPDU。#index[Segment]
- 头部结构：
  - 每个 TCP/UDP 报文段的前 32 bits 中，必须包含 源端口号 (Source Port) 和 宿端口号 (Dest Port)。
  - 端口号 (16位) 是进程的逻辑地址，范围 $0 tilde.op 65535$。
- 层级复用关系：
  - 应用层进程 $arrow.r$ TCP/UDP 复用 $arrow.r$ IP 复用。
  - 这也意味着 IP 数据报携带的数据可能来自 TCP，也可能来自 UDP，网络层通过 Protocol 字段区分，而传输层通过端口细分。

#table(
  columns: (auto, auto, auto),
  inset: 12pt,
  align: (col, row) => if col == 0 { center + horizon } else { left + top },
  fill: (col, row) => if row == 0 { luma(230) } else { white },

  // --- 表头 ---
  [*特性*], [*UDP 分用 (无连接)*], [*TCP 分用 (面向连接)*],

  // --- 标识依据 ---
  [*标识依据*],
  [
    *二元组* \
    (目的 IP, 目的端口)
  ],
  [
    *四元组* \
    (源 IP, 源端口, 目的 IP, 目的端口)
  ],

  // --- 处理逻辑 ---
  [*处理逻辑*],
  [
    只要是寄给这个端口的信，不管是谁发的，都扔进同一个信箱。
  ],
  [
    必须同时确认 “你是谁 (源IP/Port)” 和 “找谁 (目的IP/Port)”，才能映射到专属的通话线路。
  ],

  // --- 服务器 Socket 模型 ---
  [*服务器 Socket*],
  [
    一个端口通常只对应 *一个 Socket* 接收来自所有客户端的数据。
  ],
  [
    *1. 监听 Socket*: 只负责在端口监听握手请求 (SYN)。 \
    *2. 已连接 Socket*: 握手成功后，为每个客户端连接生成专属的新 Socket。
  ]
)
=== 崩溃恢复
从第 $N$ 层（如网络层）的崩溃中恢复，只能由第 $N+1$ 层（如传输层）来完成。
- 底层状态丢失：当第 $N$ 层实体（如路由器或交换机）崩溃并重启后，其内存中的状态信息（State Information）——包括路由表、虚电路表、排队的分组等——通常会全部丢失。底层无法自愈，因为它“忘记”了之前的任务。
- 上层状态保留：只有第 $N+1$ 层（传输层）作为通信的端点，持有通信状态的完整副本（如已发送但未确认的 TPDU）。
- 必要条件：上层实体必须保留足够的状态信息（包括数据副本、序列号、计时器状态），才能在底层恢复服务后重建通信。

根据网络层提供的服务类型（无连接的数据报 vs 面向连接的虚电路），传输层的恢复机制有所不同：
- 网络层提供数据报服务 (Datagram Service)
  - 典型场景：因特网 (Internet/IP)。
  - 网络层状态：无连接，路由器不维护端到端的连接状态。
  - 崩溃影响：路由器崩溃导致其队列中的分组丢失（丢包）。路由器重启后无需握手即可继续转发数据。
  - 传输层恢复动作：*单纯重传 (Retransmission)*。
    - 传输层发送端检测到超时（Timeout）或收到冗余 ACK。
    - 判定数据包在网络中丢失（传输层不区分是因为拥塞还是路由器崩溃）。
    - 从发送缓存中提取丢失的 TPDU 副本进行重传。
    - 无需重建连接，因为底层本来就没有连接。
  - 网络层提供面向连接服务 (Connection-Oriented Service)
    - 典型场景：ATM 网络、X.25、MPLS（部分场景）。
    - 网络层状态：有状态，路由器/交换机维护虚电路表 (Virtual Circuit Table)。
    - 崩溃影响：路由器崩溃导致虚电路 (VC) 状态丢失，逻辑链路中断。后续数据包到达该节点会被丢弃（因为找不到对应的 VC 映射）。
    - 传输层恢复动作：*重建连接 + 重传 (Re-establishment & Retransmission)*。
      - 传输层检测到网络连接中断（通常由网络层向上报错 Reset 或 Link Down）。
      - 步骤一：重建连接。传输层请求网络层建立新的虚电路（NC, Network Connection）。
      - 步骤二：数据重传。在新建立的通道上，重发所有未被确认的 TPDU。
为了实现上述恢复机制，传输层必须具备以下核心功能：
- 全量缓存 (Buffering)：
  - 传输层实体必须为每个连接单独维护发送缓存。
  - 策略：只有收到对端明确的确认（ACK）后，才能删除缓存中的 TPDU 副本。
- 状态同步与幂等性 (Idempotency)：
  - 问题：如果崩溃发生在“确认帧 (ACK)”传输途中，导致发送方误判并重传。
  - 解决：接收方必须通过序列号 (Sequence Number) 识别并丢弃重复的 TPDU，确保数据处理的幂等性（即多次接收同一数据包的效果与接收一次相同），防止数据重复交付给应用层。

== 无连接传输：UDP
UDP在网络层提供的 尽最大努力交付服务 (Best-Effort Service) 特性的基础上增加了*进程到进程的交付（通过端口）*和*报文完整性检查（可选）*两个最基本的功能。它通常被称为“裸露”的传输层协议。#index[UDP]

UDP 报文由 首部 (Header) 和 数据载荷 (Payload) 组成。

UDP 首部开销极小，固定为 8 字节（相比之下 TCP 最小首部为 20 字节）。

#table(
  columns: (auto, auto, 1fr),
  align: (col, row) => (left, center, left).at(col),
  inset: 10pt,
  [*字段名称*], [*长度*], [*说明*],
  [源端口号], [16 bit], [标识发送端的应用进程（回包时使用）。],
  [目的端口号], [16 bit], [标识接收端的应用进程（用于分用）。],
  [长度], [16 bit], [UDP 报文的总长度（首部 + 数据），单位为字节。最小值为 8。],
  [校验和], [16 bit], [用于检测报文在传输过程中是否出现差错。]
)

UDP 利用端口号实现主机内部不同进程间的数据传输：
- 复用 (Multiplexing)：发送方不同的应用进程通过不同的源端口号，将数据封装成 UDP 报文发送到网络层。
- 分用 (Demultiplexing)：接收方网络层收到 UDP 报文后，传输层根据目的端口号将报文交付给对应的应用进程。

校验和机制用于保证报文的完整性，检测传输过程中可能出现的比特翻转。计算校验和时，必须包含三个部分：
UDP 伪首部 (Pseudo Header)、UDP 首部和数据字段。

#remark[
  伪首部并非实际传输的数据，而是是从 IP 首部中提取的关键信息（源 IP、目的 IP、协议号-UDP为17、UDP 长度）。在最常见的 IPv4 环境下，UDP 伪首部的大小固定为 12 字节。如果是 IPv6，伪首部结构不同，长度为 40 字节，但原理一致.

  目的：为了防止 IP 地址错误导致的误投递（即报文传到了错误的主机，但端口号恰好对应）。将 IP 信息加入校验，确保数据不仅端口正确，主机地址也正确。

  从严格的 OSI 七层模型或分层原则来看，传输层（UDP）不应该去读取网络层（IP）的信息，这属于“越层违规”（Layering Violation）。但 TCP/IP 协议栈的设计哲学是实用主义：

  - 如果严格分层，UDP 就不能校验 IP，安全性降低。
  - 如果在 UDP 重复传输 IP，效率降低。
  - 折中方案（伪首部）：在逻辑上（计算时）打破分层，但在物理上（传输时）保持分层。
]

假设要计算两个 16 位整数的校验和：

#example[
  数据 1:  01100110 01100000 (0x6660)
数据 2:  01010101 01010101 (0x5555)

求和:
    01100110 01100000 + 01010101 01010101 = 10111011 10110101 (0xBBB5)

取反 (得到 Checksum):
    01000100 01001010 (0x444A)
]

发送方步骤：
+ 将校验和字段置为 0。
+ 将伪首部、首部、数据看作由 16 位（2 字节）整数组成的序列。
+ 如果数据*字节数*是奇数，需在末尾补一个全 0 字节（仅用于计算，不发送）。
+ 对序列进行二进制求和。
+ *回卷 (End-around carry)*：如果最高位产生进位，需将进位加到最低位。
+ 对最终结果取反码 (One's Complement)，填入 Checksum 字段。

接收方步骤：
+ 对收到的报文（包含伪首部、首部、数据、以及发送方填写的校验和）进行相同的 16 位求和运算。
+ 若结果为 1111 1111 1111 1111 (0xFFFF)，说明无差错。若结果包含 0，说明有差错，通常丢弃报文。

#remark[
  若计算结果为 0x0000，则填入 0xFFFF（因为 UDP 中 0x0000 表示不使用校验和）。
]

#remark[
  无论数据有 100 字节还是 60,000 字节，处理方式都是一样的：
  - 准备一个 32 位的容器（累加器，sum）。
  - 不断地从数据里取 16 位数字，加到 sum 里。
  - 如果 sum 超过了 16 位（溢出），就把溢出的高位加回到低位（回卷，End-around carry）。

  UDP 报文长度字段只有 16 位，所以 UDP 报文最大只能是 $2^16-1 = 65535$ 字节。对于现代 CPU 来说，循环累加 6 万次简直是一瞬间的事，不会造成性能瓶颈。

  理论上，两个 16 位数相加，最大结果确实只需要 17 位. 如果只用 17 或 18 位容器，你每加一两个数，就必须立刻停下来处理进位（回卷），否则容器就爆了。这会导致代码里有大量的判断逻辑，效率极低。而因为 32 位容器足够大，绝对不会溢出。等循环结束了，再统一处理一次进位即可。这叫*延迟进位（Deferred Carries）*。
  ```c
  while (sum >> 16) {
    sum = (sum & 0xFFFF) + (sum >> 16);
}
  ```

在反码数学里，0x0000 和 0xFFFF 加上任何数的效果是一样的。UDP 协议巧妙地利用了这一点，把 0x0000 留作特殊用途（表示“无校验”），而用 0xFFFF 来表示“算出来结果是 0”。数学上它们殊途同归，不会冲突。
]

相比于 TCP，UDP 的设计哲学是“简单”和“高效”，主要体现在：
+ 无连接 (Connectionless)
  - 发送数据前无需三次握手，减少了建立连接的往返时间 (RTT) 延迟。
  - 服务器无需维护连接状态（Buffer、序列号等），支持更多并发客户端。
+ 无拥塞控制与流量控制
  - UDP 按照应用层要求的速率发送数据。
  - 优势：适合对实时性要求高、不能容忍拥塞控制导致速率波动的应用。
  - 风险：若不加限制，高并发 UDP 流可能导致网络严重拥塞。
+ 报头开销小
  - UDP 首部仅 8 字节，有效载荷比（Payload/Packet Size）高。
+ 协议处理简单
  - 不需要维护序列表、重传定时器、拥塞窗口等复杂机制。

基于 UDP 的特性，它适用于以下类型的应用：
+ 容忍丢包但对延迟敏感的应用
  - 流媒体/实时通信：如 Zoom 会议、IP 电话 (VoIP)、在线直播。
  - 理由：对于实时语音，丢失一两帧数据只会出现短暂杂音，但如果为了保证完整性而进行 TCP 重传，会导致声音严重滞后，影响交互体验。
+ 单次请求/响应模式的应用
  - DNS (域名解析)、SNMP (网络管理)。
  - 理由：通常主要包含一个请求和一个响应。若使用 TCP，建立连接的开销（3次握手）超过了数据传输本身的开销。若 UDP 丢包，应用层超时重试即可。
+ 应用层实现可靠性的场景
  - QUIC 协议 (HTTP/3 的底层)。
  - 理由：Google 的 QUIC 协议基于 UDP，但在应用层重新实现了类似 TCP 的可靠传输、拥塞控制机制，从而避免了 TCP 在内核态实现的队头阻塞 (Head-of-Line Blocking) 问题。

== 面向连接的传输：TCP
=== TCP概述
TCP（传输控制协议）的服务模型核心在于：在不可靠的网络层（IP）之上，提供面向连接的、可靠的字节流服务。#index[TCP]

*面向连接 (Connection-Oriented)*

TCP 是有状态的（Stateful）。在数据传输之前，通信双方（Client 和 Server）必须通过三次握手（Three-Way Handshake）在端系统（End Systems）中建立逻辑连接。状态维护：连接并非物理链路，而是双方内核协议栈中维护的一组状态变量。包括：
- *Socket 对*：定义了端点（(SrcIP, SrcPort) 和 (DstIP, DstPort)）。
- *序列号* (Sequence Number)：用于追踪发送和接收的数据流位置。
- *缓存* (Buffers)：独立的发送缓存（Send Buffer）和接收缓存（Receive Buffer）。
- *窗口大小* (Window Sizes)：用于流量控制和拥塞控制的参数（rwnd, cwnd）。

*字节流抽象 (Byte Stream Abstraction)*

TCP 对应用层提供的是非结构化的字节流服务，这与 UDP 的报文（Datagram）服务有本质区别。
- *无边界性* (Unstructured)：TCP 不保留应用层写入的数据边界。应用层写入的 10 次 100 字节数据，可能被 TCP 打包成 1 个 1000 字节的报文段（Segment）发送，也可能分拆发送。
- *缓冲与分段*：TCP 协议栈负责将应用层字节流放入发送缓存，并根据 MSS（最大报文段长度）和网络状况（MTU 路径发现），将数据封装成 TCP Segment 交付给网络层。

*全双工通信 (Full-Duplex Service)*

TCP 连接提供双向的数据传输通道。
- *独立性*：连接的每一端都既是发送方也是接收方。
- *双向流*：如果进程 A 向进程 B 建立 TCP 连接，数据可以同时从 A 流向 B，以及从 B 流向 A。
- *Piggybacking*：接收端的确认信息（ACK）可以“捎带”在发送给对方的数据报文段中，以提高效率。

*可靠性保证 (Reliability Guarantees)*

TCP 通过 ARQ (自动重传请求) 协议族及其变种来实现可靠传输，主要机制包括：
- 数据完整性：通过校验和（Checksum）检测位错误。
- 有序性 (Ordering)：通过序列号（Seq）处理网络层的乱序到达，保证应用层读出的顺序与发送顺序一致。
- 去重 (De-duplication)：通过序列号识别并丢弃重复接收的报文段。
- 肯定确认与重传 (ACK & Retransmission)：接收方发送 ACK 确认已收到的连续字节流；发送方维护定时器（Timer），超时未收到 ACK 则触发重传。

*传输控制机制*

TCP 的服务模型不仅包含数据传递，还强制包含对传输速率的控制，以维护端系统和网络的稳定性：
- 流量控制 (Flow Control)：端到端（End-to-End）机制。
  - 目标：防止发送方的数据淹没接收方的缓冲区。
  - 实现：接收方通过 TCP 头部的 Window 字段通告剩余接收窗口（rwnd），发送方据此动态调整发送量。
- 拥塞控制 (Congestion Control)：端到网络（End-to-Network）机制。
  - 目标：防止过多的数据注入网络，导致中间路由器队列溢出。
  - 实现：发送方维护拥塞窗口（cwnd），根据网络反馈（如丢包、RTT 变化）推断网络拥塞程度并抑制发送速率（如慢启动、拥塞避免算法）。

#remark[
  TCP 服务模型即：通过在端系统维护连接状态（State），利用序列号、ACK 和窗口机制，将底层不可靠的 Packet Switching 网络，抽象为对上层应用透明的、可靠的 Virtual Circuit（逻辑虚电路）。
]
=== TCP报文结构
TCP 报文段（Segment）由 首部（Header） 和 数据（Data payload） 两部分组成。首部通常为 20 字节，但如果包含“选项（Options）”字段，则会变长。
#table(
  columns: (1fr,) * 32, // 将宽度分为32份，对应32位
  stroke: 0.5pt,
  align: center + horizon,
  inset: 0.8em,
  
  // --- 表头刻度 ---
  table.header(
    table.cell(colspan: 32, align: left, stroke: none)[
      #text(size: 8pt)[Bit 0 #h(1fr) 15 #h(1fr) 31]
    ]
  ),

  // --- 第1行 ---
  table.cell(colspan: 16)[*源端口号* \ Source Port (16)],
  table.cell(colspan: 16)[*目的端口号* \ Destination Port (16)],

  // --- 第2行 ---
  table.cell(colspan: 32)[*序号* \ Sequence Number (32)],

  // --- 第3行 ---
  table.cell(colspan: 32)[*确认序号* \ Acknowledgment Number (32)],

  // --- 第4行 ---
  table.cell(colspan: 4)[数据\ 偏移\ (4)],
  table.cell(colspan: 6)[保留\ Res.\ (6)],
  table.cell(colspan: 6)[标志位\ Flags\ (6)],
  table.cell(colspan: 16)[*窗口大小* \ Window Size (16)],

  // --- 第5行 ---
  table.cell(colspan: 16)[*校验和* \ Checksum (16)],
  table.cell(colspan: 16)[*紧急指针* \ Urgent Pointer (16)],

  // --- 第6行 ---
  table.cell(colspan: 32, fill: luma(245))[
    *选项 (长度可变)* \ Options (Variable) \
    _包含 MSS, SACK, Window Scale 等_
  ],
    
  // --- 第7行 ---
  table.cell(colspan: 32, fill: luma(245))[
    *填充 (Padding)* \
    _确保首部长度为32位的整数倍_
  ],

  // --- 数据部分 ---
  table.cell(colspan: 32, inset: 2em)[
    *数据载荷* \ Data Payload \
    _(可选，长度 = Total Length - Header Length)_
  ]
)

+ *源端口与目的端口 (Source & Destination Port)*：实现 TCP 的复用与分用。结合 IP 地址，定义了通信的 socket 对。
+ *序号 (Sequence Number, Seq)*：TCP 是字节流协议，该字段指示本报文段数据载荷的第一个字节在整个字节流中的位置。
  #remark[
    初始序号的选取: 每个TCP实体维护一个32位计数器，该计数器每4微秒增1，建立连接时从中读取计数器当前值（依赖具体实现，见连接管理）

    这句话解释的是为什么初始序号不能是 0，也不能是固定的。核心目的：*防止新旧连接的数据混淆（Time Wait 的一种补充机制）和安全问题。*现代操作系统为了防止黑客预测 ISN 进行攻击（序列号预测攻击），通常会在这个基于时钟的算法上再加一个随机偏移量（Random Offset）。
  ]
+ *确认序号 (Acknowledgment Number, Ack)*:期望收到对方下一个报文段的数据的第一个字节的序号。TCP 使用累积确认 (Cumulative ACK)。
  #note[
  - 数据链路层 (Frame-based)：通常是基于*帧（Frame）*计数的。比如 GBN 协议中，ACK 5 意味着“第5号帧及以前的都收到了”。
  - TCP (Byte-based)：TCP 是基于*字节（Byte）*计数的，而不是报文段个数。
    - 机制：TCP 的 ACK 定义是 Next Byte Expected（期望收到的下一个字节）。
  ]
+ *数据偏移 (Data Offset) / 首部长度*: 指出 TCP 数据部分的起始位置。单位是 32位字 (4字节)。因为 TCP 首部可能有“选项”字段，长度不固定。最小值为 5（即 20 字节），最大值为 15（即 60 字节）。
+ *控制位 (Flags)*:共 8 位（旧标准是 6 位，现代增加了 CWR 和 ECE 用于拥塞控制）。按照顺序，核心标志位如下：
  - URG：紧急，表示数据中有“紧急数据”。刻从缓冲读走数据。
  - ACK：置 1 时，确认序号字段才有效。建立连接后所有报文段该位均需置 1。
  - PSH：推送，提示接收端应用程序立刻从缓冲读走数据。
  - RST：复位，用于强制断开连接（如连接出错或端口未监听）。
  - SYN：同步序号，用于建立连接。SYN=1 表示这是一个连接请求或连接接受报文。
  - FIN：终止连接，表示发送方没有数据要发送了。
+ *窗口大小 (Window Size)*:
  - 流量控制的核心字段。指出了发送本报文段的一方，其接收缓存目前还能接收多少字节的数据（即通告接收窗口 rwnd）。
+ *校验和 (Checksum)*:对首部、数据以及IP 伪首部进行校验，确保数据在传输过程中未受损。
+ *紧急指针 (Urgent Pointer)*:仅当 URG 标志置 1 时有效，指出紧急数据的末尾在报文段中的位置。
+ *选项 (Options)*:选项字段用于扩展 TCP 的功能，长度可变，最长 40 字节。
  #remark[
    *怎么知道这个字段是 MSS 还是 Window Scale？*

    TLV 格式（Type-Length-Value）。 TCP 接收方解析选项时，是像读字节流一样挨个读的。除了特殊的单字节选项（如“无操作 NOP”），大多数选项遵循这个结构：
      - Kind (1字节): 类型代码, 以此区分MSS, Window Scale, SACK等。
      - Length (1字节): 整个选项的长度（包含 Kind 和 Length 本身）。
      - Value (n字节): 具体数据。
  ]
  - *最大报文段长度 (MSS, Maximum Segment Size)*
    - 位置：仅出现在 SYN 报文段中。
    - 定义：TCP 层能够接收的最大数据载荷（Payload）大小，不包含 TCP 首部。
    - 计算：通常 $"MSS" = "MTU" - "IPHeaderLen" - "TCPHeaderLen"$。
    - 缺省值：若未协商，历史缺省值为 536 字节（适配 IPv4 最小 MTU 576 字节）。#index[MSS]
  - *窗口扩大因子 (Window Scale)*
    - 位置：仅出现在 SYN 报文段中。
    - 作用：解决 16 位窗口字段不够用的问题（见上文第 6 点）。一旦在握手时协商确定，连接期间不可更改。
  - *选择确认 (SACK, Selective Acknowledgment)*#index[SACK]
    - 背景：标准 TCP 采用累积确认（Ack N 表示 N 之前全收到）。如果中间丢包（如收到 1, 2, 4, 5，丢了 3），接收方只能 Ack 3。发送方超时后可能重传 3, 4, 5，浪费带宽。
    - SACK Permitted：在 SYN 报文中协商是否允许 SACK。
    - SACK Option：在后续报文中，接收方可以在选项字段里列出“我已经收到的非连续数据块（Block）”。
    - 效果：发送方只需重传那个丢失的 3，而不需要重传 4 和 5。
  - *时间戳 (Timestamps)*
    - 用于计算 RTT（往返时间）以及防止序列号回绕（PAWS）。



#remark[
  *Window Scale 怎么解决 16 位不够用的问题？*
  - 原理：16 位最大表示 65,535。如果双方协商了 Window Scale = 3
  - TCP 首部里的 Window Size 字段填：10,000。
  - 接收方实际认为的窗口大小：$10,000 times 2^3 = 80,000$ 字节。
  - 效果：牺牲了低位的精度（像科学计数法），换取了更大的范围。最大可以将窗口扩展到 1GB 级别（Scale 最大 14）。
]



=== TCP可靠数据传输
TCP 的可靠数据传输（Reliable Data Transfer, RDT）本质上是一种混合型协议：它结合了 回退N步 (GBN) 和 选择重传 (SR) 的特点，经过高度优化以适应复杂的网络环境。TCP 牺牲了一点 SR 的确认粒度，换取了 GBN 的低开销，同时通过缓存乱序包避免了 GBN 的低吞吐量。

- 像 GBN 的地方：
  - *累积确认 (Cumulative ACK)*：TCP 接收方回复 ACK=n，表示“n 以前的所有字节都收到了，我期待 n”。如果中间缺了一个，ACK 只能卡在断点处（这点和 SR 不同，SR 是单独确认）。
  - *单一发送定时器*：TCP 发送方并不像 SR 那样为每个报文段维护一个定时器，而是只维护最早那个未被确认的段的定时器。这极大地节省了 CPU 资源。
- 像 SR 的地方：
  - *接收端缓存失序报文*：标准的 GBN 接收端会丢弃乱序包，但 TCP 会缓存下来。比如收到了 1, 2, 5，TCP 会把 5 存起来，等 3, 4 到了之后再一起拼装提交给应用层。
  - *选择性重传 (实际行为)*：虽然标准 TCP 使用累积 ACK，但结合超时重传只会重传最早那个丢失的段（而不是像 GBN 那样重传所有段），再加上现代 TCP 扩展的 SACK (Selective ACK) 选项，它的实际效率接近 SR。

超时重传是可靠性的底线，但超时时间（RTO, Retransmission Timeout）设置极其讲究。若超时值太小，容易产生不必要的重传, 若超时值太大，则丢包恢复的时间太长。直观上，超时值应大于RTT，但RTT是变化的。

需要实时测量从发出某个报文段到收到其确认报文段之间经过的时间（称*SampleRTT*）由于SampleRTT波动很大，更有意义的是计算*其平均值（称EstimatedRTT）*。


TCP RTO 计算的严格顺序 (RFC 6298)：

1. *更新 RTT 偏差 (DevRTT)*：
   必须使用旧的 SRTT (EstimatedRTT_old) 来计算差值。
   $ "DevRTT" = (1 - beta) times "DevRTT" + beta times abs("SampleRTT" - "EstimatedRTT"_("old")) $

2. *更新平滑 RTT (EstimatedRTT)*：
   现在才更新均值。
   $ "EstimatedRTT" = (1 - alpha) times "EstimatedRTT" + alpha times "SampleRTT" $

3. *计算超时时间 (TimeoutInterval)*：
   $ "TimeoutInterval" = "EstimatedRTT" + 4 times "DevRTT" $

#v(1em)

*“二义性确认”问题：发送方发了包 A，超时重传了 A'，收到 ACK，是确认 A 还是 A'？*这样就会导致SampleRTT测量错误。

#remark[
  根本原因是，TCP 的报文首部设计中，只有序列号（Sequence Number），没有“传输尝试次数”的标识。 ACK 确认的是“数据字节的位置”，而不是“这是第几次尝试发送的这个包”。
]

*Karn 算法的核心*：不要对重传的报文段测量 #index[Karn算法]SampleRTT。只用第一次就发送成功的样本来更新计算。
- 具体逻辑：只有当一个报文段一次发送就成功收到 ACK 时，才用它来计算 SampleRTT 并更新 EstimatedRTT。
- 补充策略：如果发生了重传，说明网络可能拥塞或抖动，此时不更新 RTT，而是采用定时器补偿策略（*指数退避*），直接将超时时间加倍，直到网络恢复正常（收到非重传的 ACK 为止）。这不仅是为了可靠性，更是为了拥塞控制——防止在网络堵死时加剧拥塞。

#table(
  columns: (1fr, 1fr),
  align: left + horizon,
  inset: 8pt,
  stroke: 0.5pt,

  // 表头
  table.header(
    table.cell(fill: luma(230), align: center)[*接收端事件*],
    table.cell(fill: luma(230), align: center)[*接收端动作*],
  ),

  // 第一行
  [收到一个期待的报文段，且之前的报文段均已发送过确认],
  [推迟发送确认，在500ms时间内若无下一个报文段到来，发送确认],

  // 第二行
  [收到一个期待的报文段，且前一个报文段被推迟确认],
  [立即发送确认 (估计RTT的需要)],

  // 第三行
  [收到一个失序的报文段（序号大于期待的序号），检测到序号间隙],
  [立即发送确认 (快速重传的需要)，重复当前的确认序号],

  // 第四行
  [收到部分或全部填充间隙的报文段],
  [若报文段始于间隙的低端，立即发送确认 (推进发送窗口)，更新确认序号]
)
TCP 发送方主要关注三个事件：
+ 从应用层收到数据：封装、定序、发货。如果定时器没开，就开启（指向这个包）。
+ 超时：重传最老的那个未确认包，重启定时器，并执行指数退避。
+ 收到 ACK：
  - 这是累积确认。如果 ACK > SendBase（基序号），说明接收方收到了新数据。
  - 滑动窗口右移。
  - 如果还有未确认数据，重启定时器（针对新的最老包）；如果没有了，关闭定时器。

快速重传 (Fast Retransmit)：切断等待
- 机制：如果发送方收到 3 个重复的 ACK (Duplicate ACKs)（加上原来的 1 个，总共 4 个一样的 ACK），就认为该 ACK 指示的报文段已经丢失。
- 动作：在定时器到期之前，立即重传该报文段。
- 为什么是 3 次？1 次或 2 次重复 ACK 很有可能是乱序造成的（IP 层不保证顺序）。3 次重复 ACK 说明后面又有 3 个包到了，但中间那个还没到，大概率是丢了而不是乱序。

#remark[
  TCP 的可靠传输是一个闭环反馈系统: 流水线保证效率。累积 ACK + 接收端缓存保证容错。RTT估算 + 安全边界保证定时器的准确性。快速重传解决定时器太慢的问题。指数退避保护网络不崩塌。
]
=== TCP流量控制
TCP 流量控制是一种端到端（End-to-End）的速率匹配机制。其核心目的是防止发送方发送数据的速率超过接收方应用程序读取数据的速率，从而避免接收端缓存溢出导致数据丢失。这一机制主要由以下四个模块构成：

==== 流量控制的量化基础：滑动窗口模型
TCP 依靠*接收窗口（Receive Window, rwnd）*来实现流量控制。这是一种基于“剩余信用”的反馈机制。

接收端维护一个接收缓存（RcvBuffer），所有到达但未被应用层读取的数据都暂存在此。接收窗口即缓存中的剩余可用空间。
==== 发送端的发送约束
$
  "rwnd"="RcvBuffuer" - ["LastByteRcvd" - "LastByteRead"]
$
- LastByteRcvd：网络层已交付给 TCP，并存入缓存的最后一个字节的序号。
- LastByteRead：应用进程已经从缓存中取走的最后一个字节的序号。
- 当这个值为 0 时，发送方必须强制停止发送新数据。

==== 零窗口（Zero Window）死锁与解决
当接收端缓存填满时，会向发送端通告 rwnd = 0。此时发送方停止发送。 这就引入了一个潜在的死锁风险：
+ 接收方通告 rwnd = 0，发送方停止。
+ 接收方应用层读取了数据，缓存有了空间（rwnd > 0）。
+ 接收方发送一个“窗口更新（Window Update）”报文段（仅包含 ACK 和新 rwnd，不含数据）。
+ 如果这个窗口更新报文丢失了：发送方不知道窗口已开，继续等待；接收方以为通知到了，等待数据。双方陷入死锁。

TCP 协议规定了*零窗口探测（Zero Window Probe）*机制：#index(initial: "L")[零窗口探测]
+ 启动定时器：当发送方收到 rwnd = 0 的通告时，立即启动“坚持定时器”。
+ 发送探测报文：当定时器超时，发送方发送一个零窗口探测报文段。
  #remark[
    该报文段通常仅包含 1 个字节的数据。根据 RFC 规定，如果不被确认也不会重传，而是持续探测。
  ]
+ 接收方响应：接收方收到探测报文后，必须回送确认报文，其中包含当前的 rwnd 值。
  - 若 rwnd 仍为 0：发送方重设坚持定时器（通常加倍时间）。
  - 若 rwnd > 0：死锁解除，恢复数据传输。

==== 效率优化：糊涂窗口综合症（Silly Window Syndrome）
如果完全机械地遵循上述规则，可能会出现传输效率极低的情况（例如：接收方腾出 1 字节空间就通告 1 字节窗口，发送方就发 1 字节数据，再加上 40 字节的 IP+TCP 首部，开销巨大）。

为了解决这个问题，TCP 在发送端和接收端分别引入了优化策略：
- *接收端策略（Clark 算法逻辑）*,不通告“小窗口”。#index[Clark 算法]
  - 当接收窗口很小时（例如只有几个字节），接收方不应立即通告这个微小的增量，而是继续通告 rwnd = 0（推迟发送窗口更新）。
  - 通告时机：仅当接收窗口显著增加时才通告新的窗口大小。
  #remark[
    显著增加的标准：空闲空间达到 RcvBuffer / 2 或者达到一个 MSS（最大报文段长度）。
  ]
- 发送端策略（Nagle 算法）,不发送“小数据包”。
  - 如果应用层产生的数据长度 $gt.eq$ MSS，立即发送。
  - 如果数据长度 $<$ MSS，且网络中存在未被确认的数据（In-flight Data > 0）：将数据存入发送缓存，暂不发送。
  - 直到满足以下任一条件再发送：缓存数据累积到了 MSS, 或者收到了对之前所有数据的 ACK。
#note[
  作用：Nagle 算法自适应网络延迟，将多个小包合并为一个大包发送，减少网络拥塞和首部开销。
]#index[Nagle 算法]
TCP 的流量控制不仅仅是简单的“满了就停”，而是一个包含死锁预防和效率优化的完整控制系统：
+ 基础：通过 rwnd 动态调整发送量，防止缓存溢出。
+ 保障：通过坚持定时器和零窗口探测，防止死锁。
+ 优化：通过Nagle 算法（发送端）和推迟窗口通告（接收端），防止糊涂窗口综合症导致的带宽浪费。

=== TCP连接管理
TCP 连接管理是确保通信双方能够正确地初始化状态、交换数据并安全释放资源的机制。它主要包括三个阶段：连接建立（三次握手）、数据传输 和 连接释放（四次挥手）。

==== 连接建立：三次握手 (Three-Way Handshake)
TCP 建立连接的过程被称为“三次握手”。这个过程的核心目的是同步双方的初始序列号 (ISN, Initial Sequence Number) 并分配资源（缓存和变量）。

*第一步：客户端发送连接请求 (SYN)*
- 动作：客户端 TCP 向服务器发送一个特殊的报文段。
- 标志位：SYN=1, ACK=0。SYN=1 表示这是一个连接请求报文。
- 序号 (Seq)：客户端随机选择一个初始序号 $x$ (client_isn)。
- 载荷：不包含数据。
- 状态变迁：客户端从 CLOSED 进入 SYN_SENT 状态。
- 注意：尽管不携带数据，但 SYN 报文段消耗一个序号。
#remark[
  SYN_SENT, "SYNchronize segment SENT"（同步报文段已发送）的缩写。表示客户端的*“已发请求，等待接听”*状态
]
*第二步：服务器响应 (SYN + ACK)*
- 动作：服务器收到 SYN 报文后，如果同意建立连接，则发送确认报文。
- 标志位：SYN=1, ACK=1。
  - SYN=1 表示服务器也希望建立连接（反向通道）。
  - ACK=1 表示对客户端请求的确认。
- 序号 (Seq)：服务器随机选择自己的初始序号 $y$ (server_isn)。
- 确认序号 (Ack)：$x + 1$。表示服务器期待收到客户端的下一个字节是 $x+1$。
- 资源分配：服务器在此步骤分配 TCP 缓存和变量。
  #remark[
    由于服务器在收到 SYN 后就分配资源，如果攻击者发送大量 SYN 但不回复后续的 ACK，会耗尽服务器资源，这就是著名的 SYN Flood 攻击。
  ]
- 状态变迁：服务器从 LISTEN 进入 SYN_RCVD 状态。

*第三步：客户端确认 (ACK)*
- 动作：客户端收到服务器的 SYN+ACK 后，发送确认报文。
- 标志位：SYN=0, ACK=1。连接已建立，不再需要 SYN 标志。
- 序号 (Seq)：$x + 1$。
- 确认序号 (Ack)：$y + 1$。表示客户端确认收到了服务器的序号 $y$。
- 载荷：可以携带数据。这是三次握手中唯一可以携带数据的报文段（如果是纯 ACK 报文，则不消耗序号）。
- 资源分配：客户端在此步骤分配缓存和变量。
- 状态变迁：客户端进入 ESTABLISHED（已建立连接）状态。服务器收到该 ACK 后，也进入 ESTABLISHED 状态。

#note[
  为什么需要三次握手，而不是两次？

  主要原因是为了防止已失效的连接请求报文段突然又传送到了服务端，因而产生错误。

  场景：
  - 客户端发出的第一个 SYN 报文在网络中滞留了。
  - 客户端超时重发 SYN，连接建立，传输数据，然后关闭。
  - 随后，那个滞留的“旧 SYN”到达了服务器。

  如果是两次握手：服务器收到旧 SYN，误以为客户端又要建立新连接，于是向客户端发送确认并建立连接。服务器会一直等待客户端发数据，导致资源白白浪费。

  如果是三次握手：服务器收到旧 SYN，回复 SYN+ACK。客户端收到后，发现自己并没有请求建立连接（或者序号对不上），就会发送 RST（复位）报文，终止这个错误的连接。
]

#note[
  初始序号 (ISN) 为什么要随机？

  如果在短时间内建立了两个连接（五元组相同），随机 ISN 可以防止上一个连接的旧报文段干扰新连接（防止由 TCP 序号回绕引起的数据混淆）。

  出于安全性考虑，防止黑客预测序号进行 TCP 序列号预测攻击（Sequence Number Prediction Attack）。#index[ISN]
]

==== 连接释放：四次挥手 (Four-Way Wave)
由于 TCP 是全双工 (Full-Duplex) 的，每个方向的连接都需要单独关闭。
+ FIN (Client -> Server)：客户端发送 FIN=1，表示“我没数据要发了”，进入 FIN_WAIT_1。
+ ACK (Server -> Client)：服务器收到 FIN，回复 ACK。此时 TCP 连接处于半关闭 (Half-Close) 状态：客户端不能发数据，但能收数据；服务器还能继续发数据。
+ FIN (Server -> Client)：服务器数据发完了，也发送 FIN=1，请求关闭。（可以和ACK同时发送给客户端）
+ ACK (Client -> Server)：客户端收到 FIN，回复 ACK，进入 TIME_WAIT 状态。

#note[
  关于 TIME_WAIT 状态（关键点）： 客户端在发送最后一个 ACK 后，必须等待 2MSL (Maximum Segment Lifetime) 时间才能真正关闭。

  - 保证最后一个 ACK 能到达服务器。如果 ACK 丢了，服务器重传 FIN，客户端还能补发 ACK。
  - 防止“已失效的连接请求报文段”出现在本连接中。等待 2MSL 可以让网络中所有残留报文都消失。
]

TCP 连接管理通过三次握手建立可靠的初始状态（同步 Seq，分配资源），通过四次挥手独立关闭双向通道。这种机制在不可靠的网络层之上，保障了端到端连接的鲁棒性。
== TCP拥塞控制
TCP 拥塞控制采用*端到端（End-to-End）*的设计理念，即发送方不依赖网络中间设备（如路由器）的显式反馈，而是通过自身对网络行为的感知（如丢包、延迟）来动态调节数据发送速率。

发送方维护一个核心状态变量：拥塞窗口 (Congestion Window, $"cwnd"$)。
该变量决定了发送方在未收到确认（ACK）的情况下，能向网络中注入的最大数据量。发送速率的限制遵循以下不等式：
$
  "LastByteSent" - "LastByteAcked" lt.eq "cwnd"
$
- $"cwnd"$ 的动态性：$"cwnd"$ 的值并非固定，而是随着发送方感知的网络拥塞程度实时变化。#index[cwnd]
- 速率调节：通过调节 $"cwnd"$，间接调节发送速率（$"Rate" approx "cwnd" \/ "RTT"$）。

#note[
  发送窗口=min(拥塞窗口cwnd, 接收窗口rwnd)
]

发送方将丢包事件作为网络发生拥塞的主要信号。网络拥塞会导致路由器缓冲区溢出（丢包）或排队延迟剧增（导致超时）。TCP 定义了两种不同严重程度的丢包事件：
- 重传定时器超时 (Timeout)：
  - 含义：说明网络交付能力极差，数据包和ACK都长时间滞留或丢失。
  - 判定：严重拥塞。
- 收到3个重复的ACK (3 Duplicate ACKs)：
  - 含义：发送方收到重复ACK，说明虽然有报文段丢失（导致接收方乱序），但后续报文段已到达接收方，网络仍保留了一定的交付能力。
  - 判定：轻度拥塞。

TCP 拥塞控制主要包含四个核心算法：*慢启动 (Slow Start)*、*拥塞避免 (Congestion Avoidance)*、*快重传 (Fast Retransmit)* 和 *快恢复 (Fast Recovery)*

发送方维护一个状态变量 慢启动门限 ($"ssthresh"$)，作为模式切换的分水岭
- 当 $"cwnd" < "ssthresh"$ 时，执行慢启动。
- 当 $"cwnd" gt.eq "ssthresh"$ 时，执行拥塞避免。

=== 慢启动 (Slow Start)
- 设计目的：在连接新建或严重拥塞恢复后，迅速探测网络可用带宽，将 $"cwnd"$ 提升至安全阈值。
- 初始状态：$"cwnd" = 1 "MSS"$。
- 增长策略：指数增长。
  - 每收到一个 ACK，$"cwnd"$ 增加 $1 "MSS"$。
  - 宏观表现为：每经过一个往返时间 ($"RTT"$)，$"cwnd"$ 数值翻倍（$1, 2, 4, 8, dots$）。
  - 终止条件：$"cwnd"$ 达到 $"ssthresh"$。或检测到丢包事件。

=== 拥塞避免 (Congestion Avoidance)

- 设计目的：当接近网络容量极限时，采用保守策略缓慢增加窗口，避免引起网络剧烈振荡。
- 增长策略：加性增 (Additive Increase, AI)。
  - 每收到一个 ACK，$"cwnd"$ 增加 $"MSS" times ("MSS"\/"cwnd")$。
  - 宏观表现为：每经过一个 $"RTT"$，整个窗口大小仅增加 $1 "MSS"$。
  - 表现形式：$"cwnd"$ 随时间呈线性上升趋势。
#remark[
  - 1个RTT (Round Trip Time) 代表的时间跨度是：发送方把当前拥塞窗口 ($"cwnd"$) 内的所有数据包发出去，并收到它们对应的确认 (ACK) 的过程。
  - 假设当前拥塞窗口 $"cwnd"$ 的大小包含 $N$ 个报文段（Segment）。即 $N = "cwnd" \/ "MSS"$。
  - 在一个 RTT 内，如果网络顺畅，发送方会收到 $N$ 个 ACK。

  所以经过这 $N$ 个 ACK 后，总窗口大小只增加 1个 MSS。那么，平摊到每一个 ACK 上，就是cwnd增加$"MSS" times ("MSS"\/"cwnd")$。

]

=== 响应策略

#table(
  columns: (12%, 18%, 45%, 25%), // 设置列宽比例
  inset: 8pt, // 单元格内边距
  align: horizon + left, // 内容对齐方式
  stroke: 0.5pt, // 表格边框线

  // 表头
  [*State*], [*Event*], [*TCP Sender Action*], [*Commentary*],

  // 第一行：慢启动
  [慢启动 \ (SS)],
  [收到新的确认],
  [
    $"cwnd" = "cwnd" + "MSS"$ \
    If ($"cwnd" > "ssthresh"$) \
    #h(1em) set state to "Congestion Avoidance"
  ],
  [每经过一个 $"RTT"$，$"cwnd"$ 加倍],

  // 第二行：拥塞避免
  [拥塞避免 \ (CA)],
  [收到新的确认],
  [$"cwnd" = "cwnd" + "MSS" times ("MSS"\/"cwnd")$],
  [每经过一个 $"RTT"$，$"cwnd"$ 增加一个 $"MSS"$],

  // 第三行：收到3个重复确认
  [SS or CA],
  [收到3个重复的确认],
  [
    $"ssthresh" = "cwnd"/2$ \
    $"cwnd" = "Threshold" + 3$ \
    Set state to "Congestion Avoidance"
  ],
  [$"cwnd"$ 减半，然后线性增长],

  // 第四行：超时
  [SS or CA],
  [超时],
  [
    $"ssthresh" = "cwnd"/2$ \
    $"cwnd" = 1 "MSS"$ \
    Set state to "Slow Start"
  ],
  [$"cwnd"$ 降为一个 $"MSS"$，进入慢启动],

  // 第五行：收到1个重复确认
  [SS or CA],
  [收到一个重复的确认],
  [统计收到的重复确认数],
  [$"cwnd"$ 和 $"ssthresh"$ 都不变]
)

当检测到拥塞时，TCP 根据丢包类型采取不同的响应措施。这是 TCP Tahoe 与 TCP Reno 等现代版本的核心区别。#index[SS]#index[CA]

*超时 (Timeout) - 严重拥塞*

无论 TCP 版本如何，超时均被视为严重事件：
- 调整门限：$"ssthresh" = "cwnd" / 2$（向下取整，且不小于 $2 "MSS"$）。
- 重置窗口：$"cwnd"$ 重置为 $1 "MSS"$。
- 状态迁移：重新进入慢启动阶段。网络“休克”，必须将发送速率降至最低重新“热身”。

*收到3个重复ACK - 轻度拥塞*

此时采用 乘性减 (Multiplicative Decrease, MD) 策略：
- *TCP Tahoe 实现*（早期版本，较激进）：处理方式与超时完全相同：$"ssthresh"$ 减半，$"cwnd"$ 归 1，进入慢启动。
- *TCP Reno 实现*（主流版本，含快恢复）：
  - 快重传：立即重传丢失的报文段，不等待定时器超时。#index(initial: "K")[快重传]
  - 乘性减：$"ssthresh" = "cwnd" / 2$。
  - 快恢复：#index(initial: "K")[快恢复]
    - 将 $"cwnd"$ 设置为 $"ssthresh" + 3 "MSS"$（这里的 $+3$ 代表收到了3个重复ACK，暗示有3个数据包已离开网络被接收端缓存，因此可以适度膨胀窗口）。
    - 后续每收到一个重复ACK，$"cwnd"$ 暂时增加 $1 "MSS"$。
    - 当收到新的（非重复）ACK 时，$"cwnd"$ 设置为 $"ssthresh"$，正式进入拥塞避免阶段（线性增长）。

=== AIMD 机制与锯齿状波形
结合拥塞避免阶段的“加性增”和轻度拥塞时的“乘性减”，构成了 AIMD (Additive Increase Multiplicative Decrease) 控制法则：
- 加性增 (AI)：无丢包时，$"cwnd"$ 线性增长，缓慢试探带宽上限。
- 乘性减 (MD)：丢包时，$"cwnd"$ 指数级收缩（减半），迅速释放网络资源。
#index[AIMD]

这种机制导致 TCP 的 $"cwnd"$ 变化曲线呈现出经典的锯齿状 (Sawtooth) 特征。

=== 性能分析：平均吞吐量

对于一个长期存活的 TCP 连接，忽略短暂的慢启动阶段，仅分析拥塞避免阶段（锯齿波）的性能。
假设 $W$ 为发生丢包时的窗口大小，则：
- 峰值速率：$W / "RTT"$
- 谷值速率：$(W/2) \/ "RTT"$ （丢包后减半）

由于窗口在 $W/2$ 到 $W$ 之间线性增长，其平均吞吐量可近似为：$(0.75 W)\/ "RTT"$


#example[
  一个 TCP 连接使用 $256 "kb/s"$ 的链路，其端到端时延为 $128 "ms"$。经测试发现吞吐率只有 $128 "kb/s"$。问窗口是多少？\
    (忽略 PDU 封装的协议开销及接收方应答分组的发送时间，假定应答分组长度很小)。
]

首先计算往返时间 (RTT)：
$ "RTT" = 2 times D = 2 times 128 "ms" = 256 "ms" = 0.256 "s" $

由于题目给出了具体的带宽限制，且吞吐率未跑满带宽，需使用包含*发送时延*的精确吞吐率模型：

$ "吞吐率" (S) = frac("窗口大小" (W), "发送时延" + "往返时间" ("RTT")) $

其中，发送时延为数据通过链路的时间 $W / B$。代入公式得：

$ S = frac(W, W / B + "RTT") $

设窗口大小 $W$ 的单位为 kbit，代入已知数值：

$ 128 = frac(W, W / 256 + 0.256) $

求解 $W$：得到$W = 65.536 "kbit"$

$ W_("Bytes") = frac(65.536 times 1000, 8) = 8192 "Bytes" $
=== TCP 的公平性 (Fairness)

TCP 拥塞控制试图实现带宽的公平分配。若 $K$ 条 TCP 连接共享带宽为 $R$ 的瓶颈链路，理想情况下每条连接分得 $R/K$。

收敛原理：AIMD 算法使得不同起始状态的连接最终收敛于公平线。如果不公平（某连接占用过多），其遭遇丢包概率大，窗口减半的绝对值也大，从而让出更多空间给低带宽占用的连接（其增长步长相同）。

现实中的不公平性：
  - RTT 差异：RTT 短的连接，窗口增长速度更快（ACK 返回快），抢占更多带宽。
  - 多连接应用：应用层（如 Web 浏览器）通过建立多条并行 TCP 连接来抢占带宽。例如，新应用开启 11 条连接，而旧应用只有 1 条，新应用将获得 $11/12$ 的带宽，破坏了单连接级别的公平性。
